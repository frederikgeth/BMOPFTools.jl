schema_version = "0.1.0"
fixture_id = "unit-base-serialization-001"
knowledge_ids = ["PSK-000014"]
contract_id = "unit_base_serialization_invariance"
kind = "counterexample"
title = "Serialization preserves units but changes the canonical semantic payload"
scope = "Explicit SI unit metadata, base values, and canonical semantic hashes across a serialization round trip."
minimality_status = "hand_minimized_one_hash_mismatch"
source_file = "source.json"
transformed_file = "target.json"
exact_target_file = "exact-target.json"
expected_file = "expected.json"
reproducer = "reproduce.jl"
expected_finding_codes = ["E.CONTRACT.SERIALIZED_PAYLOAD_MISMATCH"]
does_not_establish = ["Unit metadata alone proves physical equivalence.", "A matching hash authenticates the hash computation or all downstream study decisions."]
