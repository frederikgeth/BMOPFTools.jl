# Unit/base and serialization invariance

The negative target retains the SI unit system and base values but changes the
canonical semantic payload hash. Metadata equality therefore cannot conceal a
payload mutation.
