# Solved-network feasibility witness

The result reports `OPTIMAL`, but its independently computed power-balance
residual exceeds the default tolerance. The contract therefore rejects the
claim while retaining solver status as separate evidence.
