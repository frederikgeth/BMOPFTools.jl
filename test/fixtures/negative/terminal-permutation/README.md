# Terminal permutation invariance

This fixture keeps both endpoint terminal maps aligned with the bijection
`[2, 3, 1]`, but perturbs one entry of the target series primitive. A conductor
count or matching labels is therefore insufficient: the package contract must
check the explicit row/column permutation relation.
