# Duplicate OpenDSS New witness

The second `New Linecode.c` edits the existing object. The final resistance and
reactance are 0.2 and 0.02 ohm/km. Tests require PowerIO's stable malformed-source
code, the resulting SI linecode values, and an independent OpenDSS power-flow
comparison against a deck containing only the final declaration. This is parser
behavior evidence, not a scientific preservation claim or a new PSK contract.
