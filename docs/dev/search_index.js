var documenterSearchIndex = {"docs":
[{"category":"section","location":"bounds/#Bounds,-Branches,-and-Feasibility","page":"Bounds, Branches, and Feasibility","text":"Why an optimal power flow (OPF) needs bounds, what the objective silently decides for you, and where these choices quietly break down near voltage collapse.\n\nnote: How to read this page\nThis page is written so two audiences can share it.Building or curating a dataset? Read the Takeaway box in each section and the decision matrix. Skip the collapsible Derivation blocks. Check your objective against the loss-maximization list before you ship.\nValidating your own formulation against the benchmarks? Read everything, expand the Derivation blocks, and then work through Diagnostics & validation and the Known traps gallery.Every collapsible block titled Derivation or Proof sketch is safe to skip on a first read; nothing later depends on having expanded it.","title":"Bounds, Branches, and Feasibility"},{"category":"section","location":"bounds/#Scope:-this-is-a-formulation-aware-discussion","page":"Bounds, Branches, and Feasibility","text":"info: What BMOPFTools ships, and what this page is about\nBMOPFTools ships a single reference optimizer — the nonconvex four-wire IVR-EN engine (solve_opf, solve_feasibility_opf). It does not ship convex relaxations or linear approximations. This page is deliberately formulation-agnostic: it is written for anyone validating a linear, convex, or nonconvex formulation against the benchmarks — including formulations you bring yourself or reach through to_pmd into PowerModelsDistribution. Where it discusses relaxation exactness, the relaxation is yours or the ecosystem's, not one supplied by this library.\n\nEverything below behaves differently across the three families of model that the benchmarks are meant to be solved with across the broader PowerModels ecosystem (Coffrin et al., 2018; Fobes et al., 2020):\n\nNonconvex AC (polar, rectangular, current–voltage). Exact physics, multiple solutions, local optima. This is the family BMOPFTools' own engine implements.\nConvex relaxations (second-order-cone branch-flow / bus-injection models, SDP). A single global optimum, but it equals an AC solution only when the relaxation is exact. Reached here via export to PMD or your own solver, not shipped in-package.\nLinear approximations (LinDistFlow, network/DC-style models). Always convex and well behaved, but blind to the phenomena that motivate this page.\n\nMulticonductor, unbalanced networks sharpen every caveat here: the uniqueness and exactness guarantees available for balanced single-phase networks are weaker or absent in the multiphase setting (Bernstein et al., 2018). When in doubt on an unbalanced feeder, assume the optimistic single-phase result does not carry over.","title":"Scope: this is a formulation-aware discussion"},{"category":"section","location":"bounds/#1.-Without-bounds,-the-problem-is-not-well-posed","page":"Bounds, Branches, and Feasibility","text":"tip: Takeaway\nA power-flow model with free injections has a non-compact feasible set. An optimizer terminates only if the objective is coercive over that set — grows without limit in every unbounded direction. Most natural objectives are not, so the problem is unbounded. This is not a solver bug; it is the model. As the JuMP debugging guide puts it, an unbounded model almost always means a modeling error, because all physical systems have limits (JuMP docs).\n\nIn the bus-injection model the state is the complex voltage vector V in mathbbC^n and the injections are\n\nS_i = V_i sum_j Y_ij^* V_j^* qquad i = 1dotsn \n\nIf the injections S_i are free (no generator limits) and the voltages V_i are free (no magnitude limits), then every voltage profile is feasible — the injections simply adjust to match. The feasible set is all of mathbbC^n: closed, but unbounded.\n\ndetails: Derivation: why an unbounded feasible set defeats the optimizer\nLet mathcalF be the feasible set and f the objective (minimization). A minimizer is guaranteed to exist when mathcalF is closed and f is coercive on mathcalF, i.e. f(x)to+infty whenever xtoinfty with xinmathcalF (a Weierstrass-type argument: sublevel sets are compact).There are two independent ways this fails for an unconstrained power-flow model:State unboundedness. Even on the power-flow manifold, V_i can grow without limit. Nothing in the equations bounds it.\nObjective unboundedness. With several free injections, mathcalF has recession directions — rays along which you can move forever. A linear objective c^top S decreases without bound along any recession direction with c^top d  0.The practical consequence: \"the problem is unbounded\" is really the conjunction non-compact feasible set and non-coercive objective. Fixing either one repairs well-posedness. A strictly convex generation cost can be coercive in the injection coordinates and bound that part of the problem — yet still leave voltage magnitude free. This is why a cost objective alone does not save you: you must still bound the voltages.\n\nThe takeaway for dataset authors: a benchmark instance with no voltage bounds and a non-coercive (e.g. linear, or loss-rewarding) objective is ill-posed by construction, and a solver returning DUAL_INFEASIBLE / INFEASIBLE_OR_UNBOUNDED is reporting that faithfully.","title":"1. Without bounds, the problem is not well posed"},{"category":"section","location":"bounds/#2.-A-single-reference-pins-the-system-—-but-not-the-branch","page":"Bounds, Branches, and Feasibility","text":"tip: Takeaway\nAdding one voltage reference (slack) with all other buses as PQ makes the system square (determined), so solutions are isolated. But square is not unique. Constant-power loads generically admit a high-voltage (operational) and a low-voltage solution; meshed networks can admit many. Uniqueness of the high-voltage solution is a theorem only under loading/R/X conditions. The objective is what selects the physical branch — it is not a tie-breaker, it is the mechanism.\n\nFixing V_1 = V^textref removes the reference degree of freedom; fixing S_i at every other bus closes the system. Counting real equations and unknowns gives a square polynomial system, which generically has finitely many isolated solutions — not one.\n\ndetails: Derivation: the two-bus quadratic and the high/low branch\nTake a source Eangle 0 feeding a load bus through a lossless reactance X, with the load drawing P + jQ. The power-balance relations areP = fracEVXsindelta qquad\nQ = fracEVXcosdelta - fracV^2Xwhere V = V_2 and delta is the angle difference. Eliminating delta (square and add) gives a quadratic in u = V^2:u^2 + left(2QX - E^2right)u + X^2left(P^2 + Q^2right) = 0with rootsV^2 = fracE^22 - QX pm sqrtfracE^44 - X^2 P^2 - X E^2 Q The two signs are the high-voltage and low-voltage solutions. Real solutions exist only while the radicand is non-negative; it vanishes at the saddle-node / nose point (Sections 4 and 5 below). This is the smallest example of the multiplicity that the term \"unique solution\" hides.\n\nUniqueness of the operationally meaningful (high-voltage) solution holds under explicit parametric conditions — loading bounded away from the nose, conditions on the R/X structure — established by Bolognani & Zampieri (2016), Simpson-Porco (2018), Parts I–II, and, for distribution feeders, Wang et al. (2018).\n\nThe second half is the deeper and more useful fact, and it is why the objective matters so much: the high-voltage solution is the last one to vanish as the system is loaded toward its limit, and the standard computational routes — fixed-point iteration, convex relaxation, and energy-function minimization — all return exactly that solution if and only if one exists (Dvijotham, Mallada & Simpson-Porco, 2017). A voltage-maximizing / loss-minimizing objective is the lever that pins you to the physical branch.\n\nwarning: Definition matters\n\"Single voltage source\" here means one bus that fixes both angle and magnitude, with every other bus PQ. Any PV / voltage-regulating unit reintroduces reactive degrees of freedom and Q-limits, and the determined-system argument no longer applies as stated.","title":"2. A single reference pins the system — but not the branch"},{"category":"section","location":"bounds/#3.-Bounds-turn-power-flow-into-OPF-—-even-as-a-feasibility-problem","page":"Bounds, Branches, and Feasibility","text":"tip: Takeaway\nOperational limits — voltage box, thermal/ampacity, generator capability — make the feasible set compact and the question \"does an operating point exist?\" well posed. That question is an OPF feasibility problem (OPF with a constant objective). But beware: feasibility checked through a relaxation is only a one-sided certificate.\n\nOnce you impose underlinevleV_ileoverlinev, branch limits S_ijleoverlineS_ij, and generator capability underlineS^gle S^gleoverlineS^g, the feasible set becomes a compact semialgebraic set. Finding any point in it is the OPF feasibility problem — equivalently a load-flow-feasibility or restoration problem. The bounds are precisely what restored well-posedness in Section 1.\n\ndetails: Proof sketch: relaxations certify infeasibility, not feasibility\nWrite the AC feasible set mathcalF_textAC and a convex relaxation mathcalF_textrel. By construction a relaxation enlarges the feasible set:mathcalF_textAC subseteq mathcalF_textrel Therefore:mathcalF_textrel = varnothing Rightarrow\nmathcalF_textAC = varnothing\n— relaxation infeasibility is a valid certificate of AC infeasibility.\nmathcalF_textrel neq varnothing notRightarrow\nmathcalF_textAC neq varnothing\n— a feasible relaxation says nothing, on its own, about AC feasibility.Kocuk, Dey & Sun (2016) show that even a two-bus, one-generator system can exhibit all three outcomes: the SDP relaxation can be exact, inexact, or feasible while the OPF instance is infeasible. This is the hinge into the next section.","title":"3. Bounds turn power flow into OPF — even as a feasibility problem"},{"category":"section","location":"bounds/#4.-The-feasibility-boundary-is-the-collapse-manifold","page":"Bounds, Branches, and Feasibility","text":"tip: Takeaway\nWith constant-power (nonlinear) loads, the edge of the AC feasible region is the voltage-collapse / loadability boundary. To study collapse you must not impose a voltage lower bound that clips the low-voltage branch — but then you forfeit the compactness of Section 3 and reopen the branch-selection problem of Section 2. Relaxations enlarge the feasible set past the true boundary, so they cannot certify operability near collapse.\n\nVoltage collapse is a saddle-node bifurcation: the high- and low-voltage solutions coalesce and disappear at the nose, and the power-flow Jacobian is singular there (Dobson & Lu, 1993; Simpson-Porco, Dörfler & Bullo, 2016). Beyond the nose the AC equations have no real solution — genuine infeasibility. So with constant-power loads, feasibility silently encodes the stability limit.\n\nwarning: Collapse is not exactly the nose, once loads are voltage-dependent\nThe saddle-node point coincides with the maximum-power (nose) point only for pure constant-power loads. With ZIP / voltage-dependent loads the saddle-node bifurcation can occur away from the nose, so the static power-flow feasibility boundary is only a proxy for the actual stability limit, and the proxy degrades as load models become more voltage-sensitive (see Van Cutsem & Vournas, 1998 for the canonical treatment). Continuation power flow traces the full P–V curve through the nose and is the right tool when the margin itself is the question.\n\ndetails: Derivation: what a voltage lower bound does to the branches\nReturn to the two-bus quadratic of Section 2. The high- and low-voltage roots areV^2_pm = fracE^22 - QX pm sqrtunderbracetfracE^44 - X^2 P^2 - X E^2 Q_textradicand rhorho  0\n: two solutions (operational V_+, collapsed V_-).\nrho = 0\n: the saddle-node — a single double root; the loadability limit.\nrho  0\n: no real solution — infeasible AC, i.e. past collapse.Imposing Vgeunderlinev removes V_- (and, near the nose, part of the feasible V_+ range too). That is exactly what you want for operational studies and exactly what you must not do for collapse studies. A relaxation, by contrast, replaces the equality defining the manifold with an inequality, so its feasible region can extend into the rho  0 region — reporting \"feasible\" for a loading the network cannot serve.","title":"4. The feasibility boundary is the collapse manifold"},{"category":"section","location":"bounds/#5.-The-objective-decides-everything","page":"Bounds, Branches, and Feasibility","text":"tip: Takeaway\nA relaxation is exact, and the nonconvex problem stays on the high-voltage branch, when the objective is non-decreasing in every nodal generation — equivalently, in line losses. Loss / cost / slack-power minimization satisfies this. An objective that rewards higher losses or currents both drags the exact model toward the low-voltage branch and breaks relaxation exactness, so the relaxed answer stops corresponding to any real operating point.\n\nIn the branch-flow relaxation the squared-current variable ell_ij satisfies the relaxed inequality\n\nell_ij ge fracS_ij^2v_i\n\nwhich equals the true physics only when it binds. A loss-minimizing objective pushes ell_ij down onto the binding face — recovering an AC-feasible point. A loss-maximizing objective pulls ell_ij up off the face — the cone goes slack and the solution is no longer physical (Gan, Li, Topcu & Low, 2015; Yuan & Paolone, 2020).\n\ndetails: Proof sketch: monotonicity ⇒ exactness; and the standard escape hatches\nThe exactness arguments of Low (2014), Part II and Gan et al. (2015) run by contradiction: if the cone constraint were slack at the optimum, one could decrease ell_ij, which decreases losses and (with an objective non-decreasing in generation/losses) cannot worsen the objective, contradicting optimality. The physical reading is that reducing a line's loss increases every upstream reverse flow.The recurring technical conditions in the exactness literature — no generator lower bounds, or allowing load over-satisfaction (Sojoudi & Lavaei, 2012; Gan et al., 2015) — are exactly the assumptions that simultaneously guarantee boundedness (Section 1), keep you on the high-voltage branch (Section 2), and hold you away from collapse (Section 4). They are one set of conditions wearing three hats. When the relaxation is not exact, it is generally impossible to attach a physical meaning to the solution.\n\nA useful identity ties this back to feasibility: slack power = losses + net load, so minimizing slack injection is loss minimization. This is why a bare feasibility problem is so often quietly turned into a min-slack (loss-min) problem — it makes the problem well-posed and branch-correct in one move.\n\nThe objectives that violate the monotonicity test, and the reformulations that fix them, are catalogued in Objectives that imply loss maximization.","title":"5. The objective decides everything"},{"category":"section","location":"bounds/#6.-Practical-recommendations-by-task","page":"Bounds, Branches, and Feasibility","text":"note: Operational OPF (economic dispatch, Volt/VAR, hosting capacity done right)\nUse a loss- or cost-minimizing objective (non-decreasing in generation), full operational bounds, and a relaxation if you want a global optimum — then verify exactness (Diagnostics). Linear models are fine for screening.\n\nnote: Feasibility / restoration studies\nSolve OPF with a constant objective, or min-slack, with full bounds. Remember the one-sided certificate: a feasible relaxation does not prove AC feasibility; confirm with a nonconvex solve or primal_feasibility_report.\n\nnote: Loadability / collapse studies\nThis is a nonconvex / continuation problem. Drop the voltage lower bound that clips the lower branch, expect multiplicity, and do not trust any relaxation as a feasibility oracle in this regime. Use BMOPFTools' nonconvex solve_opf (continuation around it) here, not a relaxation reached via to_pmd. Maximizing loadability in an SOC model and believing the number is the single most common error this page exists to prevent.\n\n\n\nSee also: Decision matrix · Objectives that imply loss maximization · Diagnostics & validation · Known traps · References","title":"6. Practical recommendations by task"},{"category":"section","location":"results/#OPF-result-dictionary","page":"OPF result dictionary","text":"solve_opf returns a plain Dict{String,Any} in SI units (V, A, W, var). The structure mirrors the input network dict: top-level keys match the network's component types, and within each component the result is keyed by the component id, then by terminal name (or winding label for transformers).\n\nresult = solve_opf(net)\n\nresult[\"termination_status\"]   # \"LOCALLY_SOLVED\", \"INFEASIBLE\", …\nresult[\"objective\"]            # total generation cost\nresult[\"solve_time\"]           # wall-clock time in seconds\n\nresult[\"bus\"][\"b1\"][\"1\"][\"vm\"] # phase-1 voltage magnitude at bus b1 (V)\nresult[\"line\"][\"l1\"][\"1\"][\"cr_fr\"]  # real part of current at from-terminal \"1\" (A)\nresult[\"generator\"][\"g1\"][\"1\"][\"pg\"]  # active power on phase \"1\" (W)","title":"OPF result dictionary"},{"category":"section","location":"results/#Infeasible-solutions","page":"OPF result dictionary","text":"When the solver terminates without finding a feasible point (termination_status is neither \"LOCALLY_SOLVED\", \"OPTIMAL\", nor \"ALMOST_LOCALLY_SOLVED\"), every numeric field in the result is set to NaN. The termination_status and solve_time fields are always valid.","title":"Infeasible solutions"},{"category":"section","location":"results/#Top-level-fields","page":"OPF result dictionary","text":"Key Type Description\ntermination_status String JuMP termination status (e.g. \"LOCALLY_SOLVED\", \"INFEASIBLE\", \"TIME_LIMIT\")\nobjective Float64 Objective value in cost units (matches the generator cost model)\nsolve_time Float64 Solver wall-clock time (s)\nbus Dict Per-bus, per-terminal voltage results\nline Dict Per-line, per-conductor current results\nswitch Dict Per-switch, per-conductor current results\nload Dict Per-load, per-phase current and absorbed power\ngenerator Dict Per-generator, per-phase current and produced power\ninverter Dict Per-inverter, per-phase current and produced power\ntransformer Dict Per-transformer, per-winding-side currents\nvoltage_source Dict Per-source, per-phase slack current and imported power\ninitialisation Dict Per-bus, per-terminal Ipopt start values (see below)\nlosses Dict Network-total active/reactive losses; per-element losses live under each line/transformer (see below)","title":"Top-level fields"},{"category":"section","location":"results/#bus-—-voltages","page":"OPF result dictionary","text":"result[\"bus\"][bus_id][terminal] => Dict\n\nAll voltages are phase-to-ground (i.e. referenced to the global ground at potential 0 V). The rectangular components vr/vi are the primary variables solved by the OPF; vm and va are derived for convenience.\n\nField Unit Description\nvr V Real part of complex voltage\nvi V Imaginary part of complex voltage\nvm V Voltage magnitude: √(vr² + vi²)\nva rad Voltage angle: atan(vi, vr)\n\nGrounded terminals (listed in perfectly_grounded_terminals or fixed by the voltage source neutral) are present in the result with vr = vi = vm = 0, va = 0.\n\nThe neutral terminal voltage is an explicit variable. In a balanced network it is close to zero; in an unbalanced network it reflects the neutral shift.","title":"bus — voltages"},{"category":"section","location":"results/#line-—-terminal-currents","page":"OPF result dictionary","text":"result[\"line\"][line_id][terminal_name] => Dict\n\nConductors are keyed by terminal name taken from terminal_map_from of the input line. A line with terminal_map_from = [\"1\",\"2\",\"n\"] produces result keys \"1\", \"2\", \"n\".\n\nThe reported quantities are the total per-end currents: the series current plus that end's π-model shunt half-section, expressed as the current flowing out of the bus into the branch. This is exactly the quantity the thermal magnitude limit is enforced on (see Lines).\n\nField Unit Description\ncr_fr A Real part of total current leaving the from-bus into the line\nci_fr A Imaginary part of total current leaving the from-bus\ncr_to A Real part of total current leaving the to-bus into the line\nci_to A Imaginary part of total current leaving the to-bus\ncm_fr A Current magnitude at from-end: √(cr_fr² + ci_fr²)\ncm_to A Current magnitude at to-end: √(cr_to² + ci_to²)\n\nKCL sign convention: current is positive flowing out of the bus into the branch. Internally the OPF solves a single series-current variable per conductor with c_to = −c_fr; the shunt currents are linear functions of the bus voltages and are added here to form the totals.\n\ncm_fr ≠ cm_to when the linecode has a nonzero shunt admittance (π model), because the two ends draw different shunt currents. For purely series lines (no shunt) the totals reduce to the series current and cm_fr = cm_to.\n\nBesides the per-conductor terminal keys, the line dict carries two reserved keys: \"ground\" — the device's net current into earth (cg_r, cg_i, cgm [A]), nonzero only with a phase-to-ground π-shunt — and \"loss\", the element's active/reactive losses (see losses).","title":"line — terminal currents"},{"category":"section","location":"results/#switch-—-switch-currents","page":"OPF result dictionary","text":"result[\"switch\"][switch_id][terminal_name] => Dict\n\nConductors are keyed by terminal name from terminal_map_from. Open switches have all currents fixed to zero; they appear in the result as cr = ci = cm = 0.\n\nField Unit Description\ncr A Real part of current (positive into bus at from-side)\nci A Imaginary part of current\ncm A Current magnitude: √(cr² + ci²)\n\nSwitches are ideal (lossless and voltage-coupling): cr_from = -cr_to exactly. Only the from-side current is stored.","title":"switch — switch currents"},{"category":"section","location":"results/#load-—-absorbed-power","page":"OPF result dictionary","text":"result[\"load\"][load_id][phase_terminal] => Dict\n\nLoads are keyed by phase terminal name (the neutral terminal, if present, carries no independent current variable and is absent from the result). For a WYE load with terminal_map = [\"1\",\"2\",\"3\",\"n\"] the result has keys \"1\", \"2\", \"3\".\n\nThe power fields confirm that the constant-power constraints were satisfied at the solved operating point.\n\nField Unit Description\ncrd A Real part of phase current drawn by the load\ncid A Imaginary part of phase current drawn by the load\npd W Active power absorbed: Δvr·crd + Δvi·cid\nqd var Reactive power absorbed: Δvi·crd − Δvr·cid\n\nΔv = v_phase − v_neutral for WYE/SINGLEPHASE; `Δv = vpos − vnegacross the DELTA element (terminalkto terminalk mod n + 1). For a feasible solvepd ≈ pnom[k]andqd ≈ q_nom[k]`.","title":"load — absorbed power"},{"category":"section","location":"results/#generator-—-produced-power","page":"OPF result dictionary","text":"result[\"generator\"][gen_id][phase_terminal] => Dict\n\nSame terminal indexing as load — phase terminals only, neutral absent.\n\nField Unit Description\ncrg A Real part of phase current injected by the generator\ncig A Imaginary part of phase current injected by the generator\npg W Active power produced: Δvr·crg + Δvi·cig\nqg var Reactive power produced: Δvi·crg − Δvr·cig","title":"generator — produced power"},{"category":"section","location":"results/#inverter-—-produced-power","page":"OPF result dictionary","text":"result[\"inverter\"][inverter_id][phase_terminal] => Dict\n\nInverters are keyed by phase terminal name, following the inverter's topology:\n\nFOUR_LEG — keyed by each phase terminal in terminal_map (the neutral, the last terminal, carries the summed return current and is absent).\nTHREE_LEG (delta) — keyed by the first terminal of each conductor pair (k, k mod n + 1); there is no neutral.\nSINGLE_PHASE — a single key, the phase terminal terminal_map[1] (referenced against terminal_map[2]).\n\nField Unit Description\ncri A Real part of phase current injected by the inverter\ncii A Imaginary part of phase current injected by the inverter\npg W Active power produced: Δvr·cri + Δvi·cii\nqg var Reactive power produced: Δvi·cri − Δvr·cii\n\nΔv is the topology-appropriate voltage difference: phase-to-neutral for FOUR_LEG, phase-to-reference for SINGLE_PHASE, and across the conductor pair for THREE_LEG. Sign convention matches the generator: positive pg/qg is power injected into the network. See Inverters for the constraint model (box q_min/q_max bounds, constant-power-factor coupling, and the s_max apparent-power circle).","title":"inverter — produced power"},{"category":"section","location":"results/#transformer-—-winding-currents","page":"OPF result dictionary","text":"result[\"transformer\"][xfmr_id][\"fr\"|\"to\"][k] => Dict\n\nTransformer results use winding-side keys \"fr\" and \"to\", each mapping to a positional index string (\"1\", \"2\", ...) rather than a terminal name. The two winding sides may have different terminal maps (e.g. a wye_delta transformer has a neutral terminal on the wye side but not on the delta side), so a shared terminal-name key is not well-defined.\n\nTo map position back to terminal: position k corresponds to terminal_map_from[k] (from-side) or terminal_map_to[k] (to-side) in the input network.\n\nnote: Regulator subtypes index by winding, not terminal\nFor single_phase and single_phase_autotransformer the neutral is the return path, so positions index only the phase conductors. For open_delta_regulator the positions index the two regulators (\"1\", \"2\"), not the four bus terminals — the shared-phase straight-through wire current is internal and not reported. In these cases position k does not align with terminal_map[k].\n\nField Unit Description\ncr A Real part of winding current\nci A Imaginary part of winding current\ncm A Current magnitude: √(cr² + ci²)\n\nFor ideal transformers the apparent power S = V·I* is conserved across windings (up to the ideal turns ratio). Series winding impedances (r_series_from, x_series_from, etc.) cause a small difference.\n\nAlongside the \"fr\"/\"to\" winding keys, the transformer dict carries a \"ground\" entry (net current into earth: cg_r, cg_i, cgm [A]) and a \"loss\" entry (see losses).","title":"transformer — winding currents"},{"category":"section","location":"results/#results-losses","page":"OPF result dictionary","text":"Losses are computed exactly from the terminal-power identity S_loss = 1ᵀ S_from + 1ᵀ S_to, summed over every conductor the element drives (phases and neutral, both winding sides), using the per-device ledger of terminal-current injections built during model construction. Because the injected currents sum to zero internally, the result is independent of the ground voltage reference.\n\nThe top-level losses dict holds the network totals over all lines and transformers:\n\nField Unit Description\np_loss W Total active power dissipated (≥ 0 for a passive network)\nq_loss var Net reactive absorption; negative when the network is net-capacitive (line charging, capacitor shunts)\n\nThe same identity is attached per element under each line and transformer as a \"loss\" sub-dict:\n\nresult[\"line\"][line_id][\"loss\"]         => Dict\nresult[\"transformer\"][xfmr_id][\"loss\"]  => Dict\n\nField Unit Description\np_loss W Active power dissipated by this element (≥ 0; a negative value is non-physical and is flagged by W.SOL.NEG_LOSS)\nq_loss var Net reactive absorption (negative = element is net-capacitive, e.g. line charging or transformer magnetising)\ns_through VA Throughput scale `Σ\n\nSwitches are ideal (lossless) and carry no \"loss\" entry. The per-element sub-dicts are present only when the result was produced with the loss ledger (i.e. by solve_opf); they may be absent in results from other solvers.","title":"losses — active/reactive losses"},{"category":"section","location":"results/#voltage_source-—-slack-current-and-grid-injection","page":"OPF result dictionary","text":"result[\"voltage_source\"][source_id][phase_terminal] => Dict\n\nThe voltage source is the network's current slack: it fixes terminal voltages and injects the slack current that closes KCL at the source bus (see Voltage source as current slack). Results are keyed by phase terminal (neutral excluded — it carries the summed return current).\n\nField Unit Description\ncr A Real part of slack current injected at the phase terminal\nci A Imaginary part of slack current\ncm A Current magnitude: √(cr² + ci²)\nps W Active power imported into the network: Δvr·cr + Δvi·ci\nqs var Reactive power imported: Δvi·cr − Δvr·ci\n\nPositive ps/qs is power flowing into the network from the source. To obtain the total active power drawn from the grid, sum ps over all phases:\n\nsrc    = result[\"voltage_source\"][\"source\"]   # your source id\np_grid = sum(v[\"ps\"] for v in values(src))","title":"voltage_source — slack current and grid injection"},{"category":"section","location":"results/#initialisation-—-Ipopt-start-values","page":"OPF result dictionary","text":"result[\"initialisation\"][bus_id][terminal] => Dict\n\nThe start values set on the vr/vi JuMP variables before optimize! is called. These are captured immediately after the warm-start setup and before the solver overwrites them. The section is always present (even for infeasible results), so it can be read without checking termination_status first.\n\nField Unit Description\nvr_init V Real part of the start voltage\nvi_init V Imaginary part of the start voltage\nvm_init V Start voltage magnitude: √(vr_init² + vi_init²)\nva_init rad Start voltage angle: atan(vi_init, vr_init)\n\nGrounded terminals are recorded as all-zero regardless of the start-value setting, because they are fixed in the JuMP model and the solver ignores their start value.\n\nConvergence diagnostics. profile_solution reads this section and emits warnings when the start values differ substantially from the solved values:\n\nW.SOL.INIT_LEVEL_MISMATCH — a terminal where vm_init / vm_solved is outside [0.1, 10]. The most common cause is applying the source voltage magnitude to buses on a different voltage level (e.g. a 6.35 kV source voltage applied to a 230 V LV bus via flat initialisation). The solver may still converge but the path is longer and local-minimum risk is higher.\nW.SOL.INIT_LARGE_ERROR — a phase terminal where the init error exceeds 20 % of the solved voltage magnitude. The start was a poor approximation.\nI.SOL.INIT_NEUTRAL_NONZERO — a neutral terminal with vm_init > 0. Neutral start values should be zero; a non-zero value indicates an initialisation inconsistency.\n\nTo compare start and solved voltages manually:\n\ninit  = result[\"initialisation\"]\nbuses = result[\"bus\"]\nfor (bid, t_dict) in init\n    for (t, ivals) in t_dict\n        vm_s = buses[bid][t][\"vm\"]\n        vm_i = ivals[\"vm_init\"]\n        vm_s > 0 && println(\"$bid.$t  ratio=$(round(vm_i/vm_s, digits=2))\")\n    end\nend","title":"initialisation — Ipopt start values"},{"category":"section","location":"results/#Coordinate-spaces-and-sign-conventions","page":"OPF result dictionary","text":"Quantity Space Sign\nBus voltage Rectangular (vr/vi), phase-to-ground —\nLine current Rectangular, referenced to from-bus direction Positive leaving the bus into the line\nLoad current Rectangular, phase current only Positive flowing from bus into load\nGenerator current Rectangular, phase current only Positive flowing from generator into bus\nPower (all) P = Re(V · I*), Q = Im(V · I*) Positive = absorbed (load) / produced (generator)\n\nAll phase-to-neutral voltage differences use the solved neutral voltage (explicit JuMP variable), not a forced-zero assumption. This is the correct reference for 4-wire networks where the neutral is not perfectly grounded.","title":"Coordinate spaces and sign conventions"},{"category":"section","location":"results/#Per-unit-scaling","page":"OPF result dictionary","text":"When solve_opf is called with per_unit=true the result is automatically converted back to SI before being returned. The caller always receives SI units regardless of the internal solver representation.","title":"Per-unit scaling"},{"category":"section","location":"methodology/#Methodology-notes","page":"Methodology notes","text":"The physics and linear algebra behind the provenance and integrity checks. Numbered citations refer to the references at the bottom.","title":"Methodology notes"},{"category":"section","location":"methodology/#Impedance-matrix-invariants-vs-fingerprints","page":"Methodology notes","text":"The central design principle: enforce invariants — properties no legitimate transformation of the data can break — as hard checks, and treat typical properties as soft provenance fingerprints.\n\nFor the series impedance Z = R + jX of a multiconductor line in wire coordinates [1, 5]:\n\nReciprocity (Z symmetric) holds for any passive network — hard (E.PROV.NONRECIPROCAL).\nPassivity (R ⪰ 0) holds by the dissipation argument, and survives Kron reduction: elimination of grounded conductors is a Schur complement, and Schur complements of accretive matrices remain accretive [10, 11] — hard (E.PROV.NONPASSIVE).\nInductive realisability (X ⪰ 0, positive diagonals): the inductance matrix is PSD by the magnetic-energy argument; matrices with both R, X PSD are sectorial, and sectorial matrices are closed under Schur complementation — so X-PSD is also Kron-invariant (W.PROV.X_NOT_PSD, W.PROV.X_NONINDUCTIVE).\nMutual resistance sign: Carson's earth-return correction [4] makes off-diagonal resistance positive (≈ π²·f·μ₀/4 ≈ 0.049 Ω/km at 50 Hz), and nearly equal across conductor pairs — a useful fingerprint of geometry-derived data, but only typical: reductions and fitting can perturb it (I.PROV.NEGATIVE_MUTUAL_R, soft).","title":"Impedance matrix invariants vs fingerprints"},{"category":"section","location":"methodology/#Sequence-derived-matrices-and-Z/Z-recovery","page":"Methodology notes","text":"A line parameterised from sequence values and transformed back to phase coordinates (TF spec Appendix A.2; [2] Eq. 148) is perfectly balanced:\n\nZ = Zₛ·I + Zₘ·(J − I),   Zₛ = (Z₀ + 2Z₁)/3,   Zₘ = (Z₀ − Z₁)/3\n\nGeometric (Carson) parameterisations of untransposed lines cannot produce this, because phase-pair spacings differ. The classifier inverts the construction — Z₁ = Zₛ − Zₘ, Z₀ = Zₛ + 2Zₘ — and reports the implied sequence parameters (I.PROV.SEQ_DERIVED). The verdict tiers:\n\ntier structure reading\ndecoupled diagonal positive-sequence only; phases independent\nexactly_balanced rtol ≤ 10⁻⁹ sequence-derived or transposition assumed\nnear_balanced rtol ≤ 10⁻² possibly physical (twisted/bundled symmetric construction)\ndistinct otherwise consistent with first-principles geometry\n\nThe near_balanced tier exists precisely because bundled LV cables (e.g. the adapted ENWL library [6, 7, 8]) are physically near-symmetric — they must not be mistaken for sequence-derived data.","title":"Sequence-derived matrices and Z₁/Z₀ recovery"},{"category":"section","location":"methodology/#The-Maxwell-sign-pattern","page":"Methodology notes","text":"Shunt capacitance in wire coordinates is the Maxwell capacitance matrix C = P⁻¹ (P the potential-coefficient matrix from geometry): positive diagonals, non-positive off-diagonals (coefficients of electrostatic induction), nonnegative row sums (capacitance to ground), symmetric PSD — in fact a Stieltjes matrix [5]. OpenDSS Cmatrix is exactly this form; B = ωC inherits it.\n\nThe classic data error is entering the pairwise capacitance cij = −Cij with the wrong sign. Severity calibration:\n\nPSD / nonnegative diagonals are invariant — Stieltjes (symmetric M-) matrices are closed under Schur complementation (grounded-screen elimination) and under bundling congruences [11] — so violations are warnings (W.PROV.B_SIGN).\nA positive mutual with PSD intact cannot arise from a clean geometry → P⁻¹ → reduction pipeline; it marks fitted, averaged or otherwise processed provenance (I.PROV.B_OFFDIAG, informational). This calibration was validated empirically on the bundled-cable library, where small positive mutuals (+0.0011 vs −0.0114 siblings) occur in PSD matrices.","title":"The Maxwell sign pattern"},{"category":"section","location":"methodology/#Kron-reduction,-grounding-and-earthing-zones","page":"Methodology notes","text":"A 4-wire network whose every neutral is solidly grounded admits exact Kron reduction of the neutral; conversely, sparse neutral grounding makes the explicit 4-wire model essential [6, 7]. The checks formalise this:\n\nwires per voltage level: 3-wire LV is flagged as likely Kron-reduced (LV is physically 4-wire); 3-wire MV is physical and silent.\nneutral continuity graph (lines + closed switches carrying the neutral; transformer windings do not pass it): every section must reach a grounding — perfect grounding, a grounding shunt, or a source that pins the neutral. Floating sections leave the zero-sequence path undefined.\nearthing zones: per galvanic island (transformer windings are galvanic separations) the star-point earthing and the count of downstream neutral electrodes classify the likely earthing system. Downstream neutral electrodes can only exist in multi-earthed systems (TN-C-S/PME/MEN); a source-earthed-only 4-wire zone is genuinely ambiguous between TN-S and TT, because the protective-earth side of installations is not representable in a power-flow data model — the tag states the ambiguity rather than guessing. MV zones use MV vocabulary (solidly/impedance-earthed/isolated).","title":"Kron reduction, grounding and earthing zones"},{"category":"section","location":"methodology/#Voltage-reference-per-galvanic-island","page":"Methodology notes","text":"Each galvanically isolated island needs at least one voltage reference or its voltages are defined only up to a shift — the nodal admittance matrix is rank-deficient. This is the IEEE 123-bus \"bus 610\" defect that OpenDSS silently patches with a phantom shunt [2, 12]. A shunt anchors an island only if its admittance has nonzero row sums (Y·1 ≠ 0): a pure delta capacitor bank does not.","title":"Voltage reference per galvanic island"},{"category":"section","location":"methodology/#OpenDSS-default-fingerprints","page":"Methodology notes","text":"When a .dss file omits a property, OpenDSS substitutes a documented default [9]; after conversion these are indistinguishable from deliberate data except by value. The fingerprint table:\n\nfield default consequence if accidental\nline constants r1=0.058, x1=0.1206, r0=0.1784, x0=0.4047 Ω/kft a fictitious 60 Hz overhead line (≈336 ACSR)\nnormamps 400 A un-engineered thermal limits\ntransformer xhl / %r 7 % / 0.2 per winding placeholder impedance\nload pf 0.88 reactive demand never specified\nbasekv / kv 115 / 12.47 kV US default voltages\nVsource MVAsc3, X1R1 2000 MVA, 4 fictitious fault level\nline length 1.0 un-set lengths\n\nThe length check distinguishes scattered exact-1.0 values (default leak) from universal ones (a deliberate length-normalised convention, reported in the convention statement instead) — the adapted ENWL library [8] is the canonical universal case, and its single 1 m source jumper per feeder is the canonical scattered true-positive.\n\nLimit of the method: defaults that PMD resolves at parse time (e.g. basefreq mismatches) leave no trace in the converted data and cannot be fingerprinted downstream.","title":"OpenDSS default fingerprints"},{"category":"section","location":"methodology/#Regulators-and-autotransformers","page":"Methodology notes","text":"OpenDSS has no first-class regulator branch; modelers encode them either as a near-1:1 wye transformer plus a RegControl (which does not convert), or — per the EPRI autotransformer guidance [9] — as two windings on the same bus (common + ~10 % series winding), kVA rated at the series winding, with a negligible-impedance jumper. Both freeze a control device into a fixed branch; the second bundles three benchmark pitfalls of [2] in one pattern (self-loop topology, deliberate tiny impedance, a rating that reads as 10× overload). Detection (W.PROV.REGULATOR_PATTERN): the self-loop is near-conclusive on its own; the 1:1 form requires corroboration (same-voltage-level endpoints, x_pu < 0.5 %, or a non-unity tap) and is restricted to wye-wye-derived subtypes, since regulators are never delta-coupled — which keeps genuine 1:1 phase-shifting interconnectors silent.\n\nThis heuristic concerns imported data where a regulator was hacked into a transformer + shunt. The data model itself has dedicated regulator objects — single_phase_autotransformer (a fixed-tap step voltage regulator with the autotransformer shared-neutral coupling) and open_delta_regulator (monolithic open-delta with the galvanic straight-through of the shared phase) — so a regulator authored natively needs no such pattern-matching. See the OPF reference for their constraints and the conventions for the field set. The from_dss converter does not yet emit these subtypes (regulators arriving from OpenDSS are still detected via the pattern above rather than converted).","title":"Regulators and autotransformers"},{"category":"section","location":"methodology/#Benchmark-readiness","page":"Methodology notes","text":"Raw utility-derived feeders are power-flow cases, not OPF benchmarks: they ship without costed generation (degenerate objective), without voltage bounds, sometimes without thermal limits [2, 3, 14]. The transmission community closed the analogous gap with curated, well-posed benchmark libraries such as PGLib-OPF [13]; the readiness check exists to bring the same discipline to distribution cases. The readiness check encodes the augmentation recipe of the PSCC study [3] — explicit slack generation (loss-minimisation objective), dispatchable DERs with diverse costs (cost symmetry creates dispatch degeneracy, I.INT.UNIFORM_GEN_COST), voltage envelopes including the sequence/phase-to-neutral bounds whose presence measurably improves NLP robustness [3], and cross-section-derived current ratings.\n\nBeyond the structural augmentation check (I.BENCH.AUGMENTATION), four per-network degeneracy flags catch subtler problems that survive augmentation:\n\nW.BENCH.GEN_NO_DOF — generators with p_min ≈ p_max on every phase are fixed injections, not decision variables.  They add model size without contributing to benchmark difficulty.\nW.BENCH.GEN_ZERO_COST — dispatchable generators with a zero cost vector make the objective flat in their dispatch direction; the optimal solution is primal non-unique and solver-dependent.\nW.BENCH.GEN_DEGENERATE_COST — pairs of dispatchable generators on the same bus or one hop apart with identical cost coefficients allow free power redistribution between them.  Benchmarks with this property have multiple optima and produce inconsistent comparisons across solvers.\nI.BENCH.LOAD_ZERO_PNOM — loads with zero real-power setpoint are electrically inert and likely represent missing or placeholder data.","title":"Benchmark readiness"},{"category":"section","location":"methodology/#refs","page":"Methodology notes","text":"M. Deakin, A. Pandey, F. Geth, Mathematical Model and Data Model for Up-To-Four-Wire Distribution System OPF, IEEE Task Force on Benchmarking Multiconductor OPF for Distribution Systems, draft V0.2, 2026.\nF. Geth, A. C. Chapman, R. Heidari, J. Clark, \"Considerations and design goals for unbalanced optimal power flow benchmarks,\" Electric Power Systems Research 235 (2024) 110646.\nF. Geth, F. Pacaud, R. Heidari, \"Solving Three-Phase Distribution OPF with Nonlinear Programming,\" PSCC 2026, Limassol.\nJ. R. Carson, \"Wave propagation in overhead wires with ground return,\" Bell System Technical Journal 5 (1926) 539–554.\nW. H. Kersting, Distribution System Modeling and Analysis, CRC Press, 2002.\nS. Claeys, F. Geth, G. Deconinck, \"Optimal power flow in four-wire distribution networks: Formulation and benchmarking,\" Electric Power Systems Research 213 (2022) 108522.\nF. Geth, R. Heidari, A. Koirala, \"Computational analysis of impedance transformations for four-wire power networks with sparse neutral grounding,\" Proc. ACM e-Energy '22, 2022, 105–113.\nR. Heidari, F. Geth, S. Claeys, Four-wire low voltage power network dataset, CSIRO Data Collection, 2024, doi:10.25919/jaae-vc35; original data: ENWL Low Voltage Network Solutions project (LCNF closedown report, 2014); cable impedances: A. J. Urquhart, M. Thomson, Cable Impedance Data, figshare, 2019.\nR. C. Dugan, T. E. McDermott, \"An open source platform for collaborating on smart grid research,\" Proc. IEEE PES General Meeting, 2011; EPRI OpenDSS documentation, incl. Modeling Regulators as Autotransformers.\nF. Dörfler, F. Bullo, \"Kron reduction of graphs with applications to  electrical networks,\" IEEE Trans. Circuits and Systems I 60 (2013)  150–163.\nF. Zhang (ed.), The Schur Complement and Its Applications, Springer,  2005 (incl. D. Carlson, T. L. Markham, \"Schur complements of diagonally  dominant matrices,\" Czech. Math. J. 29 (1979) for the M-matrix  closure).\nM. Bazrafshan, N. Gatsis, \"Comprehensive modeling of three-phase  distribution systems via the bus admittance matrix,\" IEEE Trans. Power  Systems 33 (2018) 2015–2029.\nS. Babaeinejadsarookolaee et al., \"The power grid library for  benchmarking AC optimal power flow algorithms,\" arXiv:1908.02788, 2019.\nF. Geth, M. Vanin, D. Van Hertem, \"Data quality challenges in existing  distribution network datasets,\" CIRED 2023, Rome.","title":"References"},{"category":"section","location":"tutorial_vvwo/#Tutorial:-Volt-VAr-Watt-*optimisation*","page":"VVWO tutorial","text":"Most introductions to Volt-var / Volt-watt (VV/VW) droop present it as a power-flow feature: a smart inverter follows an AS/NZS 4777.2 droop curve, and a simulator such as OpenDSS iterates an outer control loop until the inverter set-points and the network voltages agree. That is incremental control — the control law and the network are solved in alternation.\n\nThis tutorial shows the other half of the picture. The very same droop can be written as constraints inside an optimal power flow (OPF) and solved simultaneously with the four-wire network equations — non-incremental control. Two things then become possible that a simulation-only tool cannot do:\n\nthe inverter set-points and voltages are found in one consistent solve, with no outer iteration to converge (or oscillate); and\nyou can add hard limits — a voltage ceiling, a neutral-rise cap, thermal ratings, an export envelope — that reshape the optimal dispatch.\n\nThe framing follows Mhanna, Geth, Quiertant & Mancarella, \"Volt-VAr-Watt Optimization in Four-Wire Low-Voltage Networks: Exact Nonlinear Models and Smooth Approximations\" (IEEE Trans. Power Systems, 2026). BMOPFTools encodes the piecewise-linear droop with the paper's softplus (smooth-ReLU) surrogate so that Ipopt differentiates it exactly — see Optimal power flow for the encoding details.\n\nThe complete, runnable script is examples/vvwo_tutorial.jl:\n\njulia --project=test examples/vvwo_tutorial.jl","title":"Tutorial: Volt-VAr-Watt optimisation"},{"category":"section","location":"tutorial_vvwo/#The-setup","page":"VVWO tutorial","text":"We use the real LV1_14bus feeder (11 kV / 433 V, two single-phase customers on phases 1 and 2). To create the over-voltage that VV/VW exists to solve, we put a PV cluster at each customer connection and operate the feeder the way real LV networks are run at the edge of hosting capacity:\n\nthe feeder head is tapped to 1.05 pu (utilities run the LV head high to cover downstream voltage drop — which is precisely when midday PV export over-voltages the far end);\nthe service drops are modelled at 30 m (the raw dataset ships ~6 m drops, too short to develop a realistic rise); and\neach PV cluster is 45 kVA, free to run to nameplate (≈ eight of the study's 5.25 kVA rooftop inverters behind one pole-top).\n\nBecause the loads — and therefore the PV — sit on different phases, the export is itself unbalanced, which is where four-wire modelling (explicit neutral, no Kron reduction) earns its keep.\n\nThe objective minimises priced grid import, so with PV priced at zero the OPF maximises export until a constraint stops it.","title":"The setup"},{"category":"section","location":"tutorial_vvwo/#Three-scenarios","page":"VVWO tutorial","text":"","title":"Three scenarios"},{"category":"section","location":"tutorial_vvwo/#A-—-unity-power-factor,-no-limits","page":"VVWO tutorial","text":"PV runs at unity power factor and no network limits are imposed. The OPF simply maximises export.\n\nfor (_, inv) in net[\"inverter\"]\n    inv[\"q_min\"] = [0.0]; inv[\"q_max\"] = [0.0]   # unity PF baseline\nend\n\nmax V (φ-n) : 1.1139 pu        P_total = 90.0 kW   Q_total =  -0.0 kvar   export = 30.7 kW\n\nThe worst phase-to-neutral voltage is 1.114 pu, well over the 1.10 pu limit. A power-flow tool would report this over-voltage faithfully — and stop there.","title":"A — unity power factor, no limits"},{"category":"section","location":"tutorial_vvwo/#B-—-AS/NZS-4777.2-droop-in-the-constraints","page":"VVWO tutorial","text":"Now attach a (blank) Volt-var / Volt-watt control profile to each inverter and let augment_case fill it from the \"Australia A\" preset:\n\nnet[\"control_profile\"] = Dict(\"vvw\" =>\n    Dict(\"volt_var\" => Dict{String,Any}(), \"volt_watt\" => Dict{String,Any}()))\nfor (_, inv) in net[\"inverter\"]; inv[\"control_profile\"] = \"vvw\"; end\n\ncfg = deepcopy(BMOPFTools.load_config())\ncfg[\"augment\"][\"smart_inverter\"][\"enabled\"] = true     # fill from AS/NZS 4777.2 Aus A\n\nmax V (φ-n) : 1.1042 pu        P_total = 81.0 kW   Q_total = -38.9 kvar   export = 26.8 kW\n\nThe inverters now absorb reactive power (Volt-var) and curtail active power (Volt-watt), pulling the voltage down — solved in one shot with the network, no outer iteration. Note the voltage still sits at 1.104 pu: the 4777.2 curve is a deadband droop, not a hard guarantee. That is exactly why the next step matters.","title":"B — AS/NZS 4777.2 droop in the constraints"},{"category":"section","location":"tutorial_vvwo/#C-—-hard-limits-only-an-optimiser-can-enforce","page":"VVWO tutorial","text":"Keep the droop, and add limits to the LV buses: a 1.10 pu phase-to-neutral ceiling, a neutral-to-ground cap, and (via augment_case) thermal ratings.\n\nfor b in lv_buses(net)\n    net[\"bus\"][b][\"vpn_max\"] = fill(1.10 * 230.0, 3)\n    net[\"bus\"][b][\"vpn_min\"] = fill(0.90 * 230.0, 3)\n    net[\"bus\"][b][\"vn_max\"]  = 0.10 * 230.0          # four-wire-only limit\nend\n\nmax V (φ-n) : 1.1000 pu        P_total = 77.9 kW   Q_total = -37.4 kvar   export = 25.4 kW\n\nThe OPF co-optimises the droop and the network limits: the voltage is held at exactly 1.10 pu by curtailing a little more PV. This binding constraint — impossible in a simulation-only tool — is the whole point of an OPF.","title":"C — hard limits only an optimiser can enforce"},{"category":"section","location":"tutorial_vvwo/#Summary","page":"VVWO tutorial","text":"scenario max V (pu) P total (kW) Q total (kvar) export (kW)\nA — unity PF 1.1139 90.0 −0.0 30.7\nB — droop 1.1042 81.0 −38.9 26.8\nC — droop + limits 1.1000 77.9 −37.4 25.4\n\nThe droop (B) trades export for voltage support relative to the naive maximum (A); the hard limit (C) trades a little more to guarantee the ceiling. Adding a binding constraint can only raise the objective (less export) — the signature of an optimisation that respects the network physics and the operating envelope at once.","title":"Summary"},{"category":"section","location":"tutorial_vvwo/#Appendix:-per-phase-vs-averaged-voltage-reference","page":"VVWO tutorial","text":"A three-phase (FOUR_LEG) inverter can respond to each phase's own voltage magnitude (voltage_ref = \"PER_PHASE\", the default) or to the mean of the three (voltage_ref = \"AVERAGE\", like a single three-phase unit that regulates on its average terminal voltage). On an unbalanced bus the two laws dispatch reactive power very differently:\n\nPER_PHASE   V(φ-n) = [1.073 1.109 1.104] pu   Q = [-2.28 -4.99 -4.61] kvar\nAVERAGE     V(φ-n) = [1.065 1.119 1.106] pu   Q = [-4.10 -4.10 -4.10] kvar\n\nWith PER_PHASE, each phase reacts to its own voltage, so the lightly-loaded high-voltage phase absorbs most and the heavily-loaded phase least. With AVERAGE, every phase reacts to the common mean, giving balanced reactive injection. The choice is set per inverter with the voltage_ref field.","title":"Appendix: per-phase vs averaged voltage reference"},{"category":"section","location":"bounds/references/#References","page":"References","text":"Consolidated bibliography for the Bounds, Branches, and Feasibility note. Inline citations throughout the note link directly to the source; this page is the formal list.","title":"References"},{"category":"section","location":"bounds/references/#Power-flow-solvability,-uniqueness,-and-the-high-voltage-solution","page":"References","text":"S. Bolognani and S. Zampieri, \"On the existence and linear approximation of the power flow solution in power distribution networks,\" IEEE Transactions on Power Systems, vol. 31, no. 1, pp. 163–172, 2016. DOI\nJ. W. Simpson-Porco, \"A theory of solvability for lossless power flow equations — Part I: Fixed-point power flow,\" and \"Part II: Conditions for radial networks,\" IEEE Transactions on Control of Network Systems, vol. 5, no. 3, 2018. arXiv (Part I)\nK. Dvijotham, E. Mallada, and J. W. Simpson-Porco, \"High-voltage solution in radial power networks: existence, properties, and equivalent algorithms,\" IEEE Control Systems Letters, vol. 1, no. 2, pp. 322–327, 2017. arXiv\nC. Wang, A. Bernstein, J.-Y. Le Boudec, and M. Paolone, \"Explicit conditions on existence and uniqueness of load-flow solutions in distribution networks,\" IEEE Transactions on Smart Grid, vol. 9, no. 2, pp. 953–962, 2018. DOI\nA. Bernstein, C. Wang, E. Dall'Anese, J.-Y. Le Boudec, and C. Zhao, \"Load flow in multiphase distribution networks: existence, uniqueness, non-singularity and linear models,\" IEEE Transactions on Power Systems, 2018. DOI","title":"Power flow solvability, uniqueness, and the high-voltage solution"},{"category":"section","location":"bounds/references/#Convex-relaxations-and-exactness","page":"References","text":"S. H. Low, \"Convex relaxation of optimal power flow — Part I: Formulations and equivalence; Part II: Exactness,\" IEEE Transactions on Control of Network Systems, vol. 1, no. 1–2, 2014. DOI (Part II)\nL. Gan, N. Li, U. Topcu, and S. H. Low, \"Exact convex relaxation of optimal power flow in radial networks,\" IEEE Transactions on Automatic Control, vol. 60, no. 1, 2015. arXiv\nS. Sojoudi and J. Lavaei, \"Physics of power networks makes hard optimization problems easy to solve,\" IEEE PES General Meeting, 2012. DOI\nB. Kocuk, S. S. Dey, and X. A. Sun, \"Inexactness of SDP relaxation and valid inequalities for optimal power flow\" (two-bus characterization of the three approximation outcomes), IEEE Transactions on Power Systems, vol. 31, no. 1, pp. 642–651, 2016. DOI\nZ. Yuan and M. Paolone, \"Properties of convex optimal power flow model based on power loss relaxation\" (objective-monotonicity ⇒ exactness), Electric Power Systems Research, 2020. arXiv\nJ.-L. Lupien and A. Lesage-Landry, \"Ex post conditions for the exactness of optimal power flow conic relaxations,\" 2023. arXiv\nD. K. Molzahn and I. A. Hiskens, \"A survey of relaxations and approximations of the power flow equations,\" Foundations and Trends in Electric Energy Systems, 2019.\nL. Bobo, A. Venzke, and S. Chatzivasileiadis, \"Second-order cone relaxations of the optimal power flow for active distribution grids,\" 2020. arXiv","title":"Convex relaxations and exactness"},{"category":"section","location":"bounds/references/#Voltage-collapse-and-loadability","page":"References","text":"I. Dobson and L. Lu, \"New methods for computing a closest saddle-node bifurcation and worst-case load power margin for voltage collapse,\" IEEE Transactions on Power Systems, vol. 8, no. 3, pp. 905–913, 1993. DOI\nJ. W. Simpson-Porco, F. Dörfler, and F. Bullo, \"Voltage collapse in complex power grids,\" Nature Communications, vol. 7, 10790, 2016. DOI\nT. Van Cutsem and C. Vournas, Voltage Stability of Electric Power Systems, Springer, 1998 (canonical treatment of ZIP loads, the nose point, and continuation power flow). DOI","title":"Voltage collapse and loadability"},{"category":"section","location":"bounds/references/#Software-and-the-PowerModels-ecosystem","page":"References","text":"C. Coffrin, R. Bent, K. Sundar, Y. Ng, and M. Lubin, \"PowerModels.jl: an open-source framework for exploring power flow formulations,\" PSCC, 2018. DOI\nD. M. Fobes, S. Claeys, F. Geth, and C. Coffrin, \"PowerModelsDistribution.jl: an open-source framework for exploring distribution power flow formulations,\" Electric Power Systems Research, 2020. arXiv","title":"Software and the PowerModels ecosystem"},{"category":"section","location":"bounds/references/#Optimization-model-debugging","page":"References","text":"JuMP, \"Debugging\" tutorial and \"Solutions\" manual. Debugging · Solutions\nE. Kalvelagen, \"The best way to debug infeasible models,\" Yet Another Math Programming Consultant, 2018. Link\nYALMIP, \"Infeasible or unbounded\" and \"Debugging unbounded models.\" 1 · 2\nGAMS, \"Execution errors and performance.\" Link\nPyomo, \"Model debugging.\" Link\nAIMMS, \"Debug infeasible or unbounded results.\" Link","title":"Optimization model debugging"},{"category":"section","location":"tutorial_ders/#Tutorial:-Placing-DERs-and-reading-the-*binding-constraint*","page":"DER placement tutorial","text":"Hosting-capacity and DER-coordination studies all turn on one question: when the cheap distributed generation wants to run, which network constraint stops it?\n\nA power-flow tool can only answer it after the fact — inject some power, solve, and check whether a voltage or a current came out too high. An optimal power flow (OPF) answers it directly: it dispatches the DERs against the network physics and the operating envelope at once, and the active constraint set of the optimum is the hosting-capacity answer. Which constraint binds — and which DER is curtailed to respect it — is the modeling content, not the objective value.\n\nThis tutorial builds that OPF from a raw LV feeder using the library's recipe-driven DER placement (add_inverters / add_generators), then runs three scenarios on the same feeder and DER fleet that each make a different constraint bind. The throughline:\n\nB and C share a feeder, a DER fleet, an operating point, and a limit set. They differ by a single knob — the service-cable ampacity — yet the active constraint flips from voltage to thermal, and the optimal dispatch flips with it.\n\nThe framing follows the network-aware curtailment / dynamic-hosting-capacity view of Badmus & Pandey [1], on the four-wire IVR-EN model of Deakin, Pandey & Geth [2]; see also Positioning & ecosystem for where BMOPFTools sits relative to the wider distribution-OPF ecosystem.\n\nThe complete, runnable script is examples/place_and_solve_ders.jl:\n\njulia --project=test examples/place_and_solve_ders.jl","title":"Tutorial: Placing DERs and reading the binding constraint"},{"category":"section","location":"tutorial_ders/#The-setup","page":"DER placement tutorial","text":"We use the real LV1_14bus feeder (11 kV / 433 V, two single-phase customers on phases 1 and 2). Instead of hand-writing each DER, we declare them with a recipe and let the library choose buses from the network's own semantics and record every field it writes:\n\nINV_RECIPE = InverterRecipe(\n    strategy = :load_following,   # one PV inverter per load bus\n    s_fraction = 5.0,             # s_max = 5.0 × local load  (≈ 50 kVA cluster)\n    s_to_p_ratio = 0.90,          # p_avail = 0.9 × s_max — leaves VA headroom\n    cost_basis = :uniform, der_cost_uniform = 0.2)   # cheaper than the slack (1.0)\n\nGEN_RECIPE = GeneratorRecipe(\n    strategy = :load_following,\n    der_p_fraction = 0.5,         # p_max = 0.5 × local load  (≈ 5 kW)\n    cost_basis = :uniform, der_cost_uniform = 0.3)   # dearer than PV, cheaper than slack\n\nA load-following recipe drops one PV inverter on each load bus, and a thinner generator co-locates with it. Both are priced below the slack, so the OPF sees a layered merit order — cheap PV → mid-priced generator → expensive slack import:\n\n  INV  pv_b2656   bus=b2656   topo=SINGLE_PHASE s_max=[50000.0] VA\n  INV  pv_b3230   bus=b3230   topo=SINGLE_PHASE s_max=[50000.0] VA\n  GEN  der_b2656  bus=b2656   cfg=SINGLE_PHASE  p_max=[5000.0] W\n  GEN  der_b3230  bus=b3230   cfg=SINGLE_PHASE  p_max=[5000.0] W\n\nBecause the loads — and therefore the PV — sit on different phases, the export is itself unbalanced, which is where four-wire modelling (explicit neutral, no Kron reduction) earns its keep. augment_case then fills the standards-grounded gaps: the inverter P²+Q²≤s_max² circle and its EN 50549-1 reactive box, the generator reactive limits, the IEC 60228 thermal limit, and a per-phase slack price. We add the EN 50160 phase-to-neutral ceiling (vpn_max = 1.10 pu) explicitly so we control exactly the limit we mean.","title":"The setup"},{"category":"section","location":"tutorial_ders/#Three-scenarios","page":"DER placement tutorial","text":"","title":"Three scenarios"},{"category":"section","location":"tutorial_ders/#A-—-cheap-DERs,-no-network-limit","page":"DER placement tutorial","text":"The economic baseline: head at nominal, no voltage ceiling, no thermal limit. The OPF is then a pure cost sort.\n\nmax V (φ-n) : 1.0716 pu     thermal: 90 %     export = 70.8 kW\npv_b2656 = 45.0 kW   pv_b3230 = 45.0 kW   der_b2656 = 5.0 kW   der_b3230 = 5.0 kW\n→ BINDING : generation bounds only\n\nEvery DER runs to its bound and the surplus (70.8 kW) is exported — the slack simply absorbs negative power. The only active constraints are the imposed generation limits; the network is not yet in the way. This is the merit order working exactly as priced.","title":"A — cheap DERs, no network limit"},{"category":"section","location":"tutorial_ders/#B-—-head-run-high,-healthy-cable-voltage-binds","page":"DER placement tutorial","text":"Now tap the feeder head to 1.05 pu (utilities run the LV head high to cover downstream drop — precisely when midday PV export over-voltages the far end) and impose the EN 50160 ceiling:\n\nfor b in lv_buses(net)\n    net[\"bus\"][b][\"vpn_max\"] = fill(1.10 * 230.0, 3)\n    net[\"bus\"][b][\"vpn_min\"] = fill(0.90 * 230.0, 3)\nend\n\nmax V (φ-n) : 1.1000 pu     thermal: 90 %     export = 54.5 kW\npv_b2656 = 45.00 kW  (Q = -21.79 kvar)     pv_b3230 = 37.09 kW  (Q = -21.79 kvar)\nder_b2656 =  0.18 kW                        der_b3230 =  0.00 kW\n→ BINDING : voltage (vpn_max)\n\nThe voltage is held at exactly 1.10 pu and the dispatch changes completely. Two things happen, co-optimised in a single solve:\n\nThe inverters absorb reactive power for voltage support. Each PV sits on its apparent-power circle: at s_max = 50 kVA the reactive headroom at full active power is\nQ_textavail = sqrts_max^2 - P^2 = sqrt50^2 - 45^2 approx 218 textkvar\nwhich is exactly the −21.79 kvar both inverters draw. The P²+Q²≤s_max² circle trades active headroom for reactive absorption — reactive support is not free.\nThe merit order is respected on the way down. The OPF sheds the dearer generator (der_* → ~0) before curtailing the cheaper PV, and curtails the PV only on the worst phase (pv_b3230, 45 → 37 kW) — the unbalanced curtailment a three-wire model could not represent.\n\nA simulation-only tool would report the over-voltage of scenario A and stop; here the limit reshapes the optimal dispatch.","title":"B — head run high, healthy cable → voltage binds"},{"category":"section","location":"tutorial_ders/#C-—-same-case,-a-thin-cable-thermal-binds-instead","page":"DER placement tutorial","text":"Keep everything — head, feeder, DER fleet, the 1.10 pu ceiling — and change only one thing: give the two service drops a realistically derated 16 mm² ampacity (i_max = 90 A) instead of their healthy rating.\n\nmax V (φ-n) : 1.0839 pu     thermal: 100 %    export = 43.5 kW\npv_b2656 = 32.43 kW     pv_b3230 = 32.44 kW     der_b2656 = der_b3230 = 0.0 kW\n→ BINDING : thermal (i_max)\n\nThe voltage is now slack (1.084 pu, below the ceiling) and the thermal limit binds at 100 % of i_max. The OPF curtails the PV to hold the conductor current at its rating — a different binding constraint, curtailing a different amount on a different basis than the voltage case, even though nothing else about the problem changed.","title":"C — same case, a thin cable → thermal binds instead"},{"category":"section","location":"tutorial_ders/#Summary","page":"DER placement tutorial","text":"scenario max V (pu) thermal (%) export (kW) ΣP (kW) binding\nA — no limits 1.0716 90 70.8 100.0 generation bounds only\nB — voltage 1.1000 90 54.5 82.3 voltage (vpn_max)\nC — thermal 1.0839 100 43.5 64.9 thermal (i_max)\n\nB and C differ by a single knob — the service-cable ampacity — yet the active constraint flips from voltage to thermal, and with it which DER is curtailed and how much exports. Adding or tightening a binding constraint can only raise the objective (less export); which constraint binds is the signature of an optimisation that respects the network physics and the operating envelope at once.","title":"Summary"},{"category":"section","location":"tutorial_ders/#Appendix:-the-hosting-capacity-curve-and-the-constraint-crossover","page":"DER placement tutorial","text":"If one knob can flip the binding constraint, what does sweeping it look like? Hold the scenario-C setup fixed and sweep only the cable ampacity:\n\n  i_max(A)    max V(pu)   export(kW)   binding\n        60       1.0726         29.0   thermal (i_max)\n        90       1.0839         43.5   thermal (i_max)\n       120       1.0952         58.0   voltage + thermal\n       160       1.1000         66.8   voltage + thermal\n       220       1.1000         67.7   voltage (vpn_max)\n       600       1.1000         67.7   voltage (vpn_max)\n\nWhile the cable is thin the thermal limit binds and every extra amp of rating buys more export. Around 120–160 A both constraints are active at once — the crossover. Past it the voltage ceiling takes over: the voltage pins at 1.10 pu, export plateaus at 67.7 kW, and uprating the copper no longer buys hosting capacity, because the limit is now a voltage problem, not a thermal one.\n\nThat crossover is exactly the network-aware hosting-capacity question of [1], read straight off the OPF's active constraint set — the practical payoff of solving the dispatch and the network together.","title":"Appendix: the hosting-capacity curve and the constraint crossover"},{"category":"section","location":"tutorial_ders/#refs-ders","page":"DER placement tutorial","text":"E. O. Badmus, A. Pandey, ANOCA: AC Network-aware Optimal Curtailment Approach for Dynamic Hosting Capacity, IEEE Conference on Decision and Control (CDC), Milan, Italy, 2024.\nM. Deakin, A. Pandey, F. Geth, Mathematical Model and Data Model for Up-To-Four-Wire Distribution System OPF, IEEE Task Force on Benchmarking Multiconductor OPF for Distribution Systems, draft V0.2, 2026.","title":"References"},{"category":"section","location":"opf/#Optimal-power-flow","page":"Optimal power flow","text":"Despite the name, the ambition of this Task Force extends well beyond the classical OPF problem of minimising generation cost subject to network constraints.  The unifying theme of the benchmark problems targeted here is the need to accurately represent distribution network physics rather than any particular objective function.  Generation cost minimisation is a convenient and well-posed starting point — it admits a unique solution, is straightforward to compare across solvers, and is equivalent to loss minimisation under mild conditions — but the same network physics underpins a much broader class of distribution-network-constrained optimisation problems of practical relevance: maximum load delivery, conservation voltage reduction (CVR), Dynamic Operating Envelopes (DOEs) for distributed energy resources, and distribution system state estimation (DSSE).\n\nWhat these problems share is not a common objective but a common requirement: a faithful, conductor-level representation of an unbalanced network subject to a selectable set of bounds.  Voltage bounds, current limits, and power constraints are therefore optional in the data model, reflecting the fact that different problem formulations will activate different subsets of the feasible region.  The intent is a reusable foundation across all such problem classes, not merely infrastructure for a single OPF problem definition.\n\n\n\nBMOPFTools ships a four-wire rectangular current–voltage (IVR-EN) optimal power flow engine as a Julia package extension.  It activates automatically when both JuMP and Ipopt are loaded:\n\nusing BMOPFTools, JuMP, Ipopt\n\nnet    = parse_bmopf(\"mynetwork.json\")\nresult = solve_opf(net)\n\nA full mathematical derivation is available in docs/math-model.tex.\n\n","title":"Optimal power flow"},{"category":"section","location":"opf/#Mathematical-model","page":"Optimal power flow","text":"","title":"Mathematical model"},{"category":"section","location":"opf/#Network-representation","page":"Optimal power flow","text":"The network is a graph of buses b in mathcalB, each with a set of terminals mathcalT_b.  Terminals are named strings (e.g. \"a\", \"b\", \"c\", \"n\").  The neutral terminal n_b is identified by the explicit neutral_terminal bus field, or by the naming convention: a terminal named \"n\" or \"N\" (case-insensitive) is treated as neutral.\n\nTerminals declared in perfectly_grounded_terminals are perfectly grounded: their voltage is fixed to zero and they do not appear in the KCL system. The set of grounded pairs is mathcalG_textnd.\n\nPhase terminals mathcalT_b^phi subseteq mathcalT_b are all terminals that are not the neutral.\n\n","title":"Network representation"},{"category":"section","location":"opf/#Variables","page":"Optimal power flow","text":"All variables are real-valued and in SI units (V and A).\n\nVariable Index Description\nv^r_bt v^i_bt (bt)inmathcalBtimesmathcalT_b Rectangular voltage at terminal\nc^r_ellk c^i_ellk line ell, conductor k Series current, from-side\ntildec^r_ellk tildec^i_ellk line ell, conductor k Series current, to-side\nc^rd_dk c^id_dk load d, phase k Load current\nc^rg_gk c^ig_gk generator g, phase k Generator current\nc^rn_nk c^in_nk inverter n, phase k Inverter current\nc^rs_vk c^is_vk voltage source v, phase k Source slack current\nc^rx_xsigmak c^ix_xsigmak transformer x, side sigma, conductor k Transformer winding current\n\nLoad, generator, inverter, and source current variables cover phase conductors only; neutral return current is implicit in KCL. Inverters add an analogous current variable per phase — see Inverters below.\n\n","title":"Variables"},{"category":"section","location":"opf/#Objective","page":"Optimal power flow","text":"Minimise total active-power generation cost (linear in current variables):\n\nmin sum_g in mathcalG sum_k=1^mathcalT_g^phi\n  c^g_k cdot bigl(Delta v^r_k  c^rg_gk + Delta v^i_k  c^ig_gkbigr)\n\nwhere c^g_k (currency/W) is the per-phase linear cost coefficient — the cost field is a vector with one entry per phase term, indexed by k — and Delta v_k is the phase-to-neutral (WYE) or line-to-line (DELTA) voltage at generator g's k-th phase terminal (see Generators below). The same per-phase cost vector prices the voltage source and inverters.\n\n","title":"Objective"},{"category":"section","location":"opf/#Constraints","page":"Optimal power flow","text":"","title":"Constraints"},{"category":"section","location":"opf/#Grounding","page":"Optimal power flow","text":"v^r_bt = 0 quad v^i_bt = 0 qquad forall(bt) in mathcalG_textnd","title":"Grounding"},{"category":"section","location":"opf/#Voltage-sources","page":"Optimal power flow","text":"Each source terminal t_k is fixed to the specified rectangular value:\n\nv^r_bt_k = V^s_vk costheta^s_vk qquad\nv^i_bt_k = V^s_vk sintheta^s_vk\n\nThe voltage source is also the network's current slack: it injects a free current (c^rs_vk c^is_vk) into KCL at each phase terminal (with the summed return at the neutral), so power balance at the source bus is met by the source itself — no auxiliary generator is required. The slack current is unbounded by default; optional per-phase bounds make it a bounded grid connection, and an optional cost prices imported power (see Voltage source as current slack below).\n\nThe source-bus neutral is additionally fixed to zero (v^r_bn = v^i_bn = 0) without being added to mathcalG_textnd, so that KCL is still enforced there and the grid generator's neutral return current can satisfy it.","title":"Voltage sources"},{"category":"section","location":"opf/#Voltage-magnitude-bounds","page":"Optimal power flow","text":"v_min/v_max are per-phase arrays (phase-to-ground), one entry per phase terminal in terminal_names order. The k-th entry bounds the k-th phase terminal, applied at every ungrounded, non-source phase terminal:\n\nbigl(v^b_textminkbigr)^2\nleq\nbigl(v^r_bt_kbigr)^2 + bigl(v^i_bt_kbigr)^2\nleq\nbigl(v^b_textmaxkbigr)^2\nqquad t_k in mathcalT_b^phi\n\nThe phase index k is kept aligned to the array even when a phase is grounded/source-fixed (those terminals are skipped without shifting k). The neutral terminal is not bounded phase-to-ground; instead it has its own optional maximum-only cap vn_max (when present and ungrounded): bigl(v^r_bnbigr)^2 + bigl(v^i_bnbigr)^2 leq bigl(v^b_ntextmaxbigr)^2. Otherwise the neutral voltage is determined by KCL.","title":"Voltage magnitude bounds"},{"category":"section","location":"opf/#Lines","page":"Optimal power flow","text":"KVL (series voltage drop, conductor k of line ell with total impedance matrix mathbfR_ell + jmathbfX_ell in Ω):\n\nv^r_b^textfr t^textfr_k - v^r_b^textto t^textto_k\n= sum_j bigl(R_ellkjc^r_ellj - X_ellkjc^i_elljbigr)\n\nv^i_b^textfr t^textfr_k - v^i_b^textto t^textto_k\n= sum_j bigl(R_ellkjc^i_ellj + X_ellkjc^r_elljbigr)\n\nThe impedance matrices capture full mutual coupling between conductors.\n\nSeries current balance (to-side series current equals the negative of the from-side):\n\ntildec^r_ellk = -c^r_ellk qquad tildec^i_ellk = -c^i_ellk\n\nπ-model shunt currents (from linecode G_fr/B_fr/G_to/B_to fields, scaled by line length; linear in voltage variables, no new JuMP variables):\n\nI^textshr_k(b) = sum_j bigl(G_kjv^r_bt_j - B_kjv^i_bt_jbigr)\nqquad\nI^textshi_k(b) = sum_j bigl(G_kjv^i_bt_j + B_kjv^r_bt_jbigr)\n\nKCL contributions: -(c^r_ellk + I^textshr_k(b^textfr)) at the from-bus, -(tildec^r_ellk + I^textshr_k(b^textto)) at the to-bus.\n\nThermal current limit on the total current (series + shunt) at each end:\n\nbigl(c^r_ellk + I^textshr_k(b^textfr)bigr)^2\n+ bigl(c^i_ellk + I^textshi_k(b^textfr)bigr)^2\nleq bigl(I^textmax_ellkbigr)^2\n\nbigl(tildec^r_ellk + I^textshr_k(b^textto)bigr)^2\n+ bigl(tildec^i_ellk + I^textshi_k(b^textto)bigr)^2\nleq bigl(I^textmax_ellkbigr)^2\n\nnote: Current-limit box bounds\nWherever a magnitude limit I^textmax applies directly to a current variable — switch, generator, and transformer winding currents — the implied box -I^textmax leq c^rc^i leq I^textmax is also placed on the variable (it follows from c leq I^textmax and is redundant with the cone, but bounds the variable from the start, helping the NLP solver).For lines the cone limits the total current I^texttot = c_ellk + I^textsh_k (series + π-shunt), not the series variable itself, so the series box must absorb the shunt contribution:c^r_ellkc^i_ellk le I^texttotmax_ellk\n  = I^textmax_ellk + textstylesum_j Y^textsh_kjV^textmax_jfrom c_ellk = I^texttot - I^textsh_k le I^textmax + sum_j Y^textsh_kjV^textmax_j (triangle inequality), where Y^textsh_kj = sqrtG_kj^2 + B_kj^2 is the from-side π-shunt admittance and V^textmax_j is a hard to-ground voltage-magnitude bound on from-terminal j. This box is added only when such a V^textmax exists for every terminal feeding row k — i.e. a phase-to-ground bound v_max, a phase-to-neutral bound vpn_max with a grounded neutral, or vpn_max together with a neutral-to-ground bound vn_max. With only a vpn_max on a floating neutral (no vn_max) the to-ground voltage is unbounded, so the series variable is left free rather than risk an unsound box. A transformer's from-side winding with a no-load shunt is the one remaining cone-on-an-expression case; it is left cone-only for now (the same construction would apply).","title":"Lines"},{"category":"section","location":"opf/#Switches","page":"Optimal power flow","text":"A closed switch enforces zero voltage drop:\n\nv^r_b^textfrt^textfr_k = v^r_b^texttot^textto_k qquad\nv^i_b^textfrt^textfr_k = v^i_b^texttot^textto_k\n\nAn open switch has its current variables fixed to zero; no voltage coupling is imposed.","title":"Switches"},{"category":"section","location":"opf/#Standalone-shunts","page":"Optimal power flow","text":"A shunt object represents an admittance matrix mathbfY^sh = mathbfG^sh + jmathbfB^sh (S) connected between a set of bus terminals and ground.  The current it draws is linear in the voltage variables (no new JuMP variables):\n\nI^textshr_k = sum_j bigl(G^sh_kjv^r_bt_j - B^sh_kjv^i_bt_jbigr)\nqquad\nI^textshi_k = sum_j bigl(G^sh_kjv^i_bt_j + B^sh_kjv^r_bt_jbigr)\n\nKCL contribution: -I^textshr_k (current leaves the bus to ground). Grounded terminals are absent from the voltage variable dict and contribute zero.","title":"Standalone shunts"},{"category":"section","location":"opf/#Loads","page":"Optimal power flow","text":"For every load sub-load k, define the voltage drop across the sub-load (phase-to-neutral for WYE/SINGLE_PHASE; line-to-line for DELTA):\n\nDelta v^r_k = v^r_bt^phi_k - v^r_bn_b qquad\nDelta v^i_k = v^i_bt^phi_k - v^i_bn_b\n\nThe realized power is always bilinear in these voltage drops and the load current variables (exact, no approximation):\n\nP_k = Delta v^r_k  c^rd_dk + Delta v^i_k  c^id_dk qquad\nQ_k = Delta v^i_k  c^rd_dk - Delta v^r_k  c^id_dk\n\nThe load model field determines the right-hand-side value that P_k and Q_k are pinned to.","title":"Loads"},{"category":"section","location":"opf/#Squared-voltage-drop-variable","page":"Optimal power flow","text":"All voltage-dependent models introduce a scalar auxiliary variable per sub-load:\n\nW_k = (Delta v^r_k)^2 + (Delta v^i_k)^2\n\nW_k\n\nis bounded: (f cdot V^textnom_k)^2 leq W_k leq (c cdot V^textnom_k)^2 with floor fraction f = 05 and ceiling fraction c = 15.  These conditioning bounds are deliberately wider than any supply standard; the bus voltage-magnitude bounds are the operative engineering constraints.\n\nWhen a constant-current term is present, a further auxiliary variable s_k = sqrtW_k is introduced with s_k^2 = W_k, s_k geq 0.","title":"Squared-voltage-drop variable"},{"category":"section","location":"opf/#Model-types","page":"Optimal power flow","text":"model P_k pinned to Q_k pinned to Quadratic?\nconstant_power (default) P^textnom_k Q^textnom_k yes\nconstant_current P^textnom_k cdot s_k  V^textnom_k Q^textnom_k cdot s_k  V^textnom_k yes (with s_k)\nconstant_impedance P^textnom_k cdot W_k  (V^textnom_k)^2 Q^textnom_k cdot W_k  (V^textnom_k)^2 yes\nzip P^textnom_k (alpha^Z_k W_k(V^textnom_k)^2 + alpha^I_k s_kV^textnom_k + alpha^P_k) analogous with beta yes (with s_k if alpha^I_k neq 0)\nexponential P^textnom_k (W_k(V^textnom_k)^2)^gamma^P_k2 analogous with gamma^Q_k only if gamma in 012\n\nInteger-exponent routing: exponential loads with gamma in 0 1 2 are automatically routed to the constant-power, constant-current, or constant-impedance quadratic path respectively, keeping the formulation quadratic.  The data-analysis pass (load_model_analysis) flags these loads with I.LOAD.EXP_ZIP_EQUIVALENT.\n\nv_nom is required for all models except constant_power.  It is the terminal voltage magnitude at which p_nom/q_nom are specified: phase-to-neutral (V) for WYE, line-to-line (V) for DELTA.  It may be a scalar (shared across all sub-loads) or a per-sub-load array.\n\nDELTA loads use line-to-line voltage drops: Delta v^r_k = v^r_bt_k - v^r_bt_k^+, Delta v^i_k = v^i_bt_k - v^i_bt_k^+ (indices cyclic).","title":"Model types"},{"category":"section","location":"opf/#generators-section","page":"Optimal power flow","text":"Same bilinear form as WYE loads, with power bounds instead of equalities and injected (positive) sign convention:\n\nP^gtextmin_gk\nleq Delta v^r_k  c^rg_gk + Delta v^i_k  c^ig_gk\nleq P^gtextmax_gk\n\nQ^gtextmin_gk\nleq Delta v^i_k  c^rg_gk - Delta v^r_k  c^ig_gk\nleq Q^gtextmax_gk","title":"Generators"},{"category":"section","location":"opf/#Inverters","page":"Optimal power flow","text":"Inverters use the same bilinear current/power model as generators, with the per-phase active and reactive powers\n\nP_nk = Delta v^r_k  c^rn_nk + Delta v^i_k  c^in_nk\nqquad\nQ_nk = Delta v^i_k  c^rn_nk - Delta v^r_k  c^in_nk\n\nThe voltage difference Delta v_k depends on the inverter topology:\n\nFOUR_LEG — phase-to-neutral, Delta v_k = v_bt_k - v_bt_n, one current per phase conductor; the neutral is the last terminal in terminal_map.\nTHREE_LEG — line-to-line (delta), Delta v_k = v_bt_k - v_bt_k^+ with cyclic index k^+ = (k bmod n) + 1; no neutral current.\nSINGLE_PHASE — phase-to-reference, Delta v = v_bt_1 - v_bt_2, a single current.\n\nEach phase k is constrained by an active-power box and, when an apparent-power rating S^max_nk is given, an apparent-power circle:\n\nP^min_nk leq P_nk leq P^max_nk\nqquad\nP_nk^2 + Q_nk^2 leq bigl(S^max_nkbigr)^2 \n\nReactive power is governed in one of two mutually exclusive ways:\n\nBox bounds (default): Q^min_nk leq Q_nk leq Q^max_nk. These are normally filled by the augmentation pass before the OPF runs.\nConstant power factor: when the inverter references a control_profile with a signed power_factor.pf, Q is coupled to P by the exact equality\noperatornamesign(mathrmpf)  Q_nk\n+ tanbigl(arccosmathrmpfbigr)  P_nk = 0\nwith mathrmpf  0 lagging (absorbing VAr) and mathrmpf  0 leading (injecting VAr).\nVolt-var droop: when the control_profile declares a volt_var sub-object, Q is pinned to a piecewise-linear function of the phase voltage magnitude U_nk = Delta v_k, Q_nk = Q^textbase_nk f^mathrmVV(U_nk) (an equality — the inverter follows the curve).\n\nActive power follows either the box upper bound above or, when the control_profile declares a volt_watt sub-object, a Volt-watt curtailment cap P_nk leq P^textbase_nk f^mathrmVW(U_nk).","title":"Inverters"},{"category":"section","location":"opf/#Piecewise-linear-droop-encoding","page":"Optimal power flow","text":"Each characteristic f through non-decreasing breakpoints (bar x_i bar y_i), clamped flat outside the range, is written as a sum of shifted/scaled rectified-linear (ReLU) terms,\n\nf(U) = bar y_1 + sum_i a_i operatornameReLU(U - bar x_i)\n\nwhere each interior segment contributes a (+a_i bar x_i) / (-a_i bar x_i+1) pair so the slope telescopes. For a gradient-based solver the kinked ReLU is replaced by the smooth softplus surrogate operatornameReLU^varepsilon(x) = varepsilonlog(1+e^xvarepsilon), evaluated with the numerically stable log1pexp/logistic from StatsFuns.jl and registered as a JuMP nonlinear operator (analytic derivatives) so Ipopt differentiates it exactly. varepsilon to 0 recovers the exact ReLU; the relative smoothing is the volt_var_watt_eps keyword of solve_opf. The Smooth droop encoding tech note derives the closed-form derivatives, the varepsilonlog 2 error bound, and the numerically stable log1pexp/logistic evaluation in full.\n\nBreakpoint voltages are SI volts (phase-to-neutral) and are scaled into model units at build time, so the droop is identical in SI and per-unit mode. Droop is applied for SINGLE_PHASE and FOUR_LEG only; a THREE_LEG (delta) inverter has too few degrees of freedom for a per-phase droop, so a profile on it is ignored (box bounds retained) with a warning. Regional default characteristics (e.g. AS/NZS 4777.2:2020 \"Australia A\" for Queensland) are injected by augment_case from the [augment.smart_inverter] config section.\n\nBy default each phase responds to its own magnitude U_nk. Setting the inverter field voltage_ref to \"AVERAGE\" (default \"PER_PHASE\") instead feeds every phase the mean of the phase magnitudes, bar U_n = tfrac1msum_k U_nk, as the common reference for both the Volt-var and Volt-watt curves — modelling inverters that regulate on the average terminal voltage rather than per phase. The setting only affects multi-phase FOUR_LEG inverters; on a SINGLE_PHASE inverter it is a no-op and emits a warning.\n\nThe inverter current variables enter KCL with the same sign convention as generators (injection positive into the bus); for FOUR_LEG the negated phase current is also added to the neutral terminal.","title":"Piecewise-linear droop encoding"},{"category":"section","location":"opf/#Transformers","page":"Optimal power flow","text":"All transformer constraints are linear.  The turns ratio for the four two-winding subtypes is N = V^textref_textfr  V^textref_textto (SI volts).  The two regulator subtypes (single_phase_autotransformer, open_delta_regulator) instead use a fixed-tap effective ratio n_texteff derived from tap_ratio and regulator_type (see below).\n\n\n\nsingle_phase — Γ-equivalent model\n\nSeries impedance R_x = R_1 + N^2 R_2, X_x = X_1 + N^2 X_2 is referred to the HV (from) side, where R_1 X_1 (r/x_series_from, Ω on HV base) are the HV winding values and R_2 X_2 (r/x_series_to, Ω on LV base) are the LV winding values.  For each per-phase pair index k:\n\nv^r_b^textfrt^textfr_k - Nv^r_b^texttot^textto_k\n= R_xc^rx_xtextfrk - X_xc^ix_xtextfrk\n\nNc^rx_xtextfrk + c^rx_xtexttok = 0 quadtext(and imaginary)\n\nThe no-load shunt G_0 + jB_0 (g_no_load, b_no_load, S) sits at the HV terminals (phase-to-ground).  The total HV terminal current entering the bus is series + shunt:\n\nI^textfrterm_xk =\n  c^rx_xtextfrk\n  + G_0v^r_b^textfrt^textfr_k\n  - B_0v^i_b^textfrt^textfr_k\n\nWhen all loss fields are absent or zero the model reduces to the ideal v^r_b^textfrt^textfr_k = Nv^r_b^texttot^textto_k.\n\n\n\ncenter_tap — T-model with per-leg secondary impedance\n\nTerminal map: terminal_map_from = [t_ph, t_n] (HV phase, HV neutral), terminal_map_to = [t₁, tₙ, t₂] (leg-1, center-tap neutral, leg-2). V^textref_textto is the per-leg voltage (e.g. 120 V for a 120-0-120 V unit), so N = V^textref_textfrV^textref_textto = 60 for a 7.2 kV / 120 V unit.\n\nEach leg has its own secondary impedance branch (Z_2 = R_2 + jX_2). For leg ell in 1 2 with LV current c^rx_xell and leg terminals (t_a t_b):\n\nbigl(v^r_b^textfrt^textph - v^r_b^textfrt^textnbigr)\n- Nbigl(v^r_b^texttot_a - v^r_b^texttot_bbigr)\n= R_1c^rx_xs - X_1c^ix_xs\n  - Nbigl(R_2c^rx_xell - X_2c^ix_xellbigr)\n\nwhere c^rx_xs is the HV series current (variable index 1 on fr side).  The -N Z_2 I_ell sign follows from the T-model star-node elimination: LV currents are defined flowing into the transformer from the bus, i.e. opposite to the direction through the winding branch from the star node.\n\nCurrent coupling (both leg currents into the transformer):\n\nNc^rx_xs + c^rx_xell_1 + c^rx_xell_2 = 0\nquadtext(and imaginary)\n\nCenter-tap KCL (variable index 2 on to side):\n\nc^rx_xn + c^rx_xell_1 + c^rx_xell_2 = 0\nquadtext(and imaginary)\n\nThe no-load shunt G_0 + jB_0 is placed at the HV phase terminal t^textph (same convention as single_phase).  The HV series current returns through t^textn; the shunt is phase-to-ground and does not pass through the HV neutral.\n\nnote: Leakage from OpenDSS XHL/XLT/XHT\nFor a 3-winding OpenDSS unit, the per-pair leakage values must be star-converted before storing in x_series_from/x_series_to:x_series_from = (XHL + XHT − XLT) / 2 × Vhv² / (100 · s_rating)\nx_series_to   = (XHL + XLT − XHT) / 2 × Vlv² / (100 · s_rating)Using XHL/2 for both (the 2-winding formula) forces equal leg voltages regardless of load imbalance and is incorrect for center_tap.\n\n\n\nWye–delta (Yd) / Delta–wye (Dy) — effective turns ratio:\n\nn_texteff = begincases sqrt3N  textYd  Nsqrt3  textDy endcases\n\nLoss model (per-winding T). Matching the OpenDSS / PMD reference, each winding carries its own series impedance — R^textwX^textw (r/x_series_from, wye winding) and R^textdX^textd (r/x_series_to, delta winding) — and a g/b_no_load core-loss shunt sits at the from-side (HV) phase terminals. g_no_load is the total core-loss conductance, split equally across the from-side phases and stamped phase-to-ground; it is referred to the line-to-neutral stamping voltage V_textLN = v_textreffromsqrt 3, so that the total core loss g_textno_loadV_textLN^2 = textnoloadlosscdot S_textrated matches OpenDSS. The legacy single r_series/x_series is read as R^textw = R_textseries, R^textd = 0, recovering the ideal delta. The series drop enters the voltage equation behind the ideal transform:\n\nVoltage (delta line-to-line = wye phase-to-neutral × n_texteff, less the winding series drop, indices cyclic):\n\nv^r_textdelt_k - v^r_textdelt_k^+\n= n_texteffbigl(v^r_textwyet^phi_k - v^r_textwyen_textwyebigr)\n  - bigl(R^textw c^rx_xtextwyek - X^textw c^ix_xtextwyekbigr)\n  - n_texteffbigl(R^textd c^rx_xtextdelk - X^textd c^ix_xtextdelkbigr)\n\nWhen all impedance fields are zero this collapses to the ideal transform.\n\nCurrent (transpose of voltage transform, power-conservative):\n\nn_texteff  c^rx_xtextdelk\n= c^rx_xtextwyek - c^rx_xtextwyek^-\n\nStar-point KCL at the wye neutral:\n\nc^rx_xtextwyen + sum_k c^rx_xtextwyek = 0\n\n\n\nsingle_phase_autotransformer — step voltage regulator\n\nA fixed-tap regulator modelled as an autotransformer: the series and common windings share a node, so from and to are galvanically tied (not isolated). With fixed tap ratio a (tap_ratio, regulated/source) the effective from→to ratio is\n\nn_texteff = begincases 1a  textType B (standard SVR default)  a  textType A endcases\n\nThe voltage and current-coupling constraints are the single_phase YY form with N = n_texteff and a series impedance R_x = R_1 + n_texteff^2 R_2, X_x = X_1 + n_texteff^2 X_2:\n\nbigl(v^r_b^textfrt^textph - v^r_b^textfrt^textnbigr)\n- n_texteffbigl(v^r_b^texttot^textph - v^r_b^texttot^textnbigr)\n= R_xc^rx_xtextfr - X_xc^ix_xtextfr\n\nn_texteffc^rx_xtextfr + c^rx_xtextto = 0 quadtext(and imaginary)\n\nThe galvanic tie shows up in the shared-neutral KCL — both the series and the to-side return close at the common neutral (unlike the isolated YY, whose from-neutral carries only the from-side return):\n\nI_n + c^rx_xtextfr + c^rx_xtextto = 0\nLongleftrightarrow\nI_n + (1 - n_texteff)c^rx_xtextfr = 0\n\nA sign error here would produce negative transformer losses. A lossless ideal regulator (R=X=G=B=0) collapses to v_textto = n_texteffv_textfr.\n\n\n\nopen_delta_regulator — monolithic open-delta\n\nTwo single-phase autotransformer windings connected line-to-line across the phase pairs implied by connection (ABBC/BCAC/CABA); per-regulator taps tap_ratio = [a_1, a_2] give n_texteffj as above. For each regulator j spanning from-phase pair (p q) and the matching to-phase pair:\n\nbigl(v^r_b^textfrt_p - v^r_b^textfrt_qbigr)\n- n_texteffjbigl(v^r_b^texttot_p - v^r_b^texttot_qbigr)\n= R_xjc^rx_xtextfrj - X_xjc^ix_xtextfrj\n\nn_texteffjc^rx_xtextfrj + c^rx_xtexttoj = 0\n\nKCL injects each regulator's line current at the two phases it spans (+I at one, -I at the other). The phase common to both regulators (B in the ABBC arrangement) is a galvanic straight-through — a zero-impedance wire with its own current variable, enforcing\n\nv_b^textfrt_textshared = v_b^texttot_textshared\n\nThis is the physically-correct \"common neutral\" model of Yan et al. (2018): the shared phase passes through unchanged while the two regulated line-to-line voltages are boosted by their taps. Without it the line-to-line voltages are still correct but the per-phase reference floats (the unphysical \"unspecified neutral\" model). See the derivation note docs/transformer_admittance_derivation.md for the matching bus-admittance form.","title":"Transformers"},{"category":"section","location":"opf/#Kirchhoff's-Current-Law","page":"Optimal power flow","text":"KCL is enforced at every ungrounded terminal. Each component accumulates its signed current contribution (positive = into bus) into per-terminal expressions kappa^r_bt and kappa^i_bt:\n\nkappa^r_bt = 0 qquad kappa^i_bt = 0\nqquad forall(bt) notin mathcalG_textnd\n\nSign conventions:\n\nComponent Terminal KCL contribution\nLine from-side from terminal -c^r_ell (leaves)\nLine to-side to terminal +c^r_ell (enters, since tildec = -c)\nLoad WYE phase terminal -c^rd (consumed)\nLoad WYE neutral terminal +c^rd (return)\nLoad DELTA positive terminal -c^rd\nLoad DELTA negative terminal +c^rd\nGenerator WYE phase terminal +c^rg (injects)\nGenerator WYE neutral terminal -c^rg (return)\nVoltage source phase terminal +c^rs (slack injects)\nVoltage source neutral terminal -c^rs (return)\nTransformer each terminal -c^rx (winding current leaves)\n\n","title":"Kirchhoff's Current Law"},{"category":"section","location":"opf/#source-slack","page":"Optimal power flow","text":"The voltage source fixes its terminal voltages and closes KCL at the source bus through its own slack current (c^rs_vk c^is_vk) — there is no separate slack generator and no _auto_slack injection. This mirrors the OpenDSS/PMD Vsource, which is both a voltage reference and an (implicit) unbounded power injection.\n\nBecause the terminal voltages are fixed, the per-phase power is linear in the slack current:\n\nP^s_vk = Delta v^r_k  c^rs_vk + Delta v^i_k  c^is_vk qquad\nQ^s_vk = Delta v^i_k  c^rs_vk - Delta v^r_k  c^is_vk\n\nwhere Delta v is the phase-to-neutral voltage at the source bus. Optional fields on the voltage_source object shape the slack:\n\np_min/p_max/q_min/q_max — per-phase box bounds. Absent ⇒ unbounded (pure power-flow slack); present ⇒ a bounded grid connection.\ncost — a per-phase vector of linear active-power prices (one entry per phase term) added to the objective; exact since the source voltage is fixed.\n\nThe source-bus neutral is fixed to zero and carries the summed slack return current, so neutral KCL is satisfied without a neutral voltage reference.\n\nThis makes the source play three roles with one object: unbounded power-flow slack (no bounds, no cost), bounded grid connection (bounds), and priced import/export (cost). The augmentation pass sets cost on the source by default (see Augmentation).\n\nnote: Independent generators at the source bus\nThe voltage source is already the slack, so an unbounded generator co-located at the source bus creates a second free current injection at a fixed-voltage bus — a degenerate dispatch split. The pre-flight check flags this (W.PRE.SOURCE_BUS_GENERATOR for unbounded, I.PRE.SOURCE_BUS_GENERATOR for bounded); model such limits/cost on the voltage source instead.\n\n","title":"Voltage source as current slack"},{"category":"section","location":"opf/#Warm-start-initialisation","page":"Optimal power flow","text":"Both solvers seed Ipopt with phase-correct voltage start values (rectangular v_nom·∠angle) so the NLP converges to the physical solution without a load-flow pre-solve. Phase terminals use the canonical three-phase angles (0°, −120°, +120°) taken from the voltage source. Split-phase zones are special-cased: a zone fed by a center_tap transformer (see I.PROV.SPLIT_PHASE_ZONE) has its two legs initialised anti-phase — θ and θ+180° about the centre-tap neutral, where θ is the feeding MV phase angle — rather than 120° apart. Without this, every centre-tap secondary starts with a 60° leg error.","title":"Warm-start initialisation"},{"category":"section","location":"opf/#Feasibility-relaxation","page":"Optimal power flow","text":"solve_feasibility_opf adds an elastic slack current (c^rvarepsilon_bt c^ivarepsilon_bt) at every ungrounded, non-source terminal, making KCL always satisfiable:\n\nkappa^r_bt + c^rvarepsilon_bt = 0 qquad\nkappa^i_bt + c^ivarepsilon_bt = 0\n\nThe cost objective is replaced by the ell_2^2 norm of all slack injections:\n\nmin sum_(bt) Biglbigl(c^rvarepsilon_btbigr)^2\n                       + bigl(c^ivarepsilon_btbigr)^2Bigr\n\nAll device models — load, generator, inverter, shunt, transformer, switch, and the voltage-source slack — are built identically to solve_opf. The only differences are the deliberate ones: operational network bounds (voltage magnitude/sequence, line thermal-angle, bus limits) are not hard constraints here, and the objective is the slack norm rather than cost. Voltage bounds are evaluated post-solve by diagnose_infeasibility.\n\nA zero-slack solution certifies physical feasibility.  Non-zero slacks at (b t) reveal where the network cannot balance KCL without external intervention.\n\nfopf   = solve_feasibility_opf(net)\ndiag   = diagnose_infeasibility(fopf, net)\n\nprintln(diag[\"is_feasible\"])            # false if network is overloaded\nprintln(diag[\"total_infeasibility_A\"])  # L2 norm of all slacks (A)\n\n","title":"Feasibility relaxation"},{"category":"section","location":"opf/#API-reference","page":"Optimal power flow","text":"","title":"API reference"},{"category":"function","location":"opf/#BMOPFTools.solve_opf","page":"Optimal power flow","text":"solve_opf(net::Dict{String,Any}; optimizer=Ipopt.Optimizer, t_index::Int=1,\n          per_unit::Bool=false, s_base::Float64=1e6,\n          volt_var_watt_eps::Float64=2e-3) -> Dict{String,Any}\n\nSolve the four-wire rectangular current-voltage (IVR-EN) optimal power flow on a BMOPF network dict. Requires JuMP and Ipopt to be loaded in the calling environment before calling this function.\n\nWhen per_unit=true the model is built and solved in per-unit (Vbase propagated from the source bus through transformers; Sbase = s_base VA, default 1 MVA). All results are returned in SI units regardless.\n\nSmart-inverter Volt-var / Volt-watt\n\nAn inverter whose control_profile declares a volt_var and/or volt_watt sub-object follows a voltage-dependent droop: Volt-watt caps active power, P_k ≤ p_base · f^VW(|U_k|), and Volt-var pins reactive power to the curve, Q_k = q_base · f^VV(|U_k|). Each piecewise-linear characteristic is encoded as a sum of shifted/scaled smooth-ReLU (softplus) terms so the model stays differentiable for Ipopt; volt_var_watt_eps is the relative corner-smoothing (smaller → sharper kinks, larger → smoother). Breakpoint voltages are SI volts (phase-to-neutral) regardless of per_unit. Supported for SINGLEPHASE and FOURLEG inverters; for THREELEG (delta) the droop is ignored (box bounds retained) with a warning. Default characteristics for a region (e.g. AS/NZS 4777.2:2020 \"Australia A\" for Queensland) can be injected by `augmentcasevia the[augment.smart_inverter]` config section.\n\nReturns a results dict with keys:\n\n\"termination_status\" — JuMP termination status string\n\"objective\" — optimal objective value (total generation cost, W·/W)\n\"solve_time\" — wall-clock solve time (s)\n\"bus\" — per-bus voltage results: \"vr\", \"vi\", \"vm\", \"va\" per terminal\n\"line\" — per-line from/to current results per conductor\n\"generator\" — per-generator P/Q dispatch results\n\"voltage_source\" — per-source current injection results\n\"initialisation\" — per-bus, per-terminal Ipopt start values: \"vr_init\", \"vi_init\", \"vm_init\", \"va_init\" (SI, same units as \"bus\"). Always present. Pass to profile_solution to diagnose convergence issues.\n\n\n\n\n\n","title":"BMOPFTools.solve_opf"},{"category":"function","location":"opf/#BMOPFTools.solve_pf","page":"Optimal power flow","text":"solve_pf(net::Dict{String,Any}; optimizer=Ipopt.Optimizer, t_index::Int=1,\n         per_unit::Bool=false, s_base::Float64=1e6) -> Dict{String,Any}\n\nDetermined four-wire rectangular current-voltage (IVR-EN) power flow on a BMOPF network dict. Same device models as solve_opf but with no operational bounds and no objective: fixed source voltages, constant-power injections, and exact KCL fully determine the nodal state.\n\nDevice current/thermal limits and voltage bounds are intentionally ignored — the power flow reports whatever results from the physics; use solve_opf or a post-solve validation pass when limits must hold.\n\nGenerators must be fixed setpoints (p_min == p_max and q_min == q_max); a non-degenerate range is rejected, since a power flow has no objective to choose a dispatch within the range. Inverters under a control_profile are voltage- dependent and remain determined.\n\nRequires JuMP and Ipopt (same as solve_opf). The result dict matches solve_opf's structure plus \"is_power_flow\" => true.\n\n\n\n\n\n","title":"BMOPFTools.solve_pf"},{"category":"function","location":"opf/#BMOPFTools.solve_feasibility_opf","page":"Optimal power flow","text":"solve_feasibility_opf(net::Dict{String,Any}; optimizer=nothing, t_index::Int=1)\n    -> Dict{String,Any}\n\nFeasibility-relaxed variant of solve_opf. Adds elastic slack current injections at every non-source bus terminal so that KCL can always be satisfied, then minimises the L2² norm of those slacks.\n\nBecause the problem is always feasible, the solver always converges. Non-zero slacks in the result indicate where the network cannot satisfy its physical constraints. Use diagnose_infeasibility to interpret the result.\n\nRequires JuMP and Ipopt (same as solve_opf).\n\nAdditional result keys beyond solve_opf:\n\n\"slack_injections\"        — per-bus, per-terminal cs_r, cs_i, cs_mag (A)\n\"total_slack_magnitude_A\" — L2 norm of all slack injections (A)\n\"is_feasibility_opf\"      — always true, used by diagnose_infeasibility\n\n\n\n\n\n","title":"BMOPFTools.solve_feasibility_opf"},{"category":"function","location":"opf/#BMOPFTools.diagnose_infeasibility","page":"Optimal power flow","text":"diagnose_infeasibility(fopf_result, net; top_n=10, slack_threshold=1e-3)\n    -> Dict{String,Any}\n\nInterpret the result of solve_feasibility_opf and identify the root cause and location of network infeasibility.\n\nArguments\n\nfopf_result — output dict from solve_feasibility_opf\nnet          — the same BMOPF network dict passed to solve_feasibility_opf\ntop_n        — maximum number of buses to report in the ranked list\nslack_threshold — minimum per-bus slack magnitude (A) to count as infeasible\n\nReturns a dict with keys:\n\n\"is_feasible\"            — true if total slack < slack_threshold\n\"total_infeasibility_A\"  — L2 norm of all slack injections (A)\n\"n_infeasible_buses\"     — number of buses with per-bus slack > threshold\n\"top_buses\"              — list of up to top_n dicts, ranked by slack\n\"failure_mode_summary\"   — count of voltage_bound vs power_balance buses\n\n\n\n\n\n","title":"BMOPFTools.diagnose_infeasibility"},{"category":"section","location":"analysis/#Analysis-and-reports","page":"Analysis & reports","text":"analyze runs fifteen passes over a network dict and returns a SummaryReport holding per-pass result dicts (report.results) and the combined finding log (report.findings). For time-series networks the snapshot at t_index is materialised first.","title":"Analysis & reports"},{"category":"section","location":"analysis/#The-passes","page":"Analysis & reports","text":"results key function computes\n:inventory inventory_analysis component counts, totals (load P/Q, generation capacity), per-type breakdowns\n:voltage_levels voltage_level_analysis BFS voltage propagation from sources through transformer ratios; level clustering; transformer transitions; level-crossing violations\n:connectivity connectivity_analysis connected components, radial/meshed (physical branch count, parallel-aware), degree statistics, tree depth, dangling buses, galvanic-zone phase topology (split-phase / SWER tagging)\n:diversity diversity_analysis parameter spread per category (CV, duplicate tuples), phase imbalance, symmetry score\n:operational operational_analysis total load/generation, transformer utilisation at nominal load (downstream BFS), line thermal-limit coverage\n:load_models load_model_analysis load model breakdown by type, voltage-dependent load count, exponential loads that are ZIP-equivalent (integer exponents), nonlinear loads on buses without a lower voltage bound\n:provenance provenance_analysis impedance classification (balance tiers, passivity, sign structure), wires per level / Kron likelihood, neutral grounding structure, earthing-system tags, OpenDSS default fingerprints, regulator patterns, the convention statement\n:preflight infeasibility_preflight generation adequacy, voltage-bound coverage, bound-pair conflicts (v/vpn/vpp/vpos, p, q), topological risk\n:schema schema_check fields present but not in the data model (catalogued, not rejected)\n:completeness completeness_check required fields per component type, incl. transformer subtypes; optional-field coverage\n:domain_rules domain_rules_check numerical plausibility: bounds signs, power factors, costs, impedance diagonals, transformer step ratios, zero limits/lengths, angle units, load model coefficient validity\n:redundancy redundancy_check zero loads/shunts, mergeable series lines (junction-aware), unused/duplicate linecodes\n:integrity integrity_check referential integrity, dimension consistency, padded matrices, voltage reference per galvanic island, wye-without-neutral, low-impedance lines, generator cost symmetry\n:spec spec_conformance_check TF-spec rules the JSON Schema cannot express: single source, configuration/arity, transformer map arities, terminal types, matrix storage\n:benchmark benchmark_readiness_check objective well-posedness, slack-only detection, bound/limit coverage, augmentation suggestions\n\nNote on transformer utilisation: the downstream-load estimate excludes only the transformer under analysis; per-phase-banked units (parallel siblings on the same bus pair) defeat the radial assumption and the figure becomes an upper bound.","title":"The passes"},{"category":"section","location":"analysis/#The-report","page":"Analysis & reports","text":"render writes nine sections (terminal with ANSI colour, or Markdown via a .md path / render_markdown):\n\nComponent inventory\nVoltage levels — level table + transformer transitions\nConnectivity & topology\nDiversity & variance\nLoading & operational summary\nInfeasibility pre-flight\nProvenance & model conventions — the convention statement, wires per level, neutral grounding, linecode classification, OpenDSS default fingerprints, earthing system per galvanic zone\nSpec conformance & benchmark readiness — incl. structural integrity and augmentation suggestions\nData quality summary — every finding, grouped by severity\n\nThe header repeats the convention statement, e.g.\n\nConvention   MV_6.4kV: 4-wire; LV_250V: 4-wire; 4 grounding point(s)\n\nso a case's modeling assumptions are visible before any numbers.","title":"The report"},{"category":"section","location":"analysis/#Findings","page":"Analysis & reports","text":"Each Finding carries severity, a stable code, the producing section, component type/id, a human message and an optional machine-readable detail dict. Severity semantics:\n\nERROR — will compromise OPF correctness or prevent execution;\nWARNING — degrades result quality or indicates suspicious data;\nINFO — provenance/context worth knowing; not necessarily actionable.\n\nFilter with errors / warnings / infos, and match on f.code — message text is not stable. The complete catalogue with triggers and rationale is in the finding-code reference.","title":"Findings"},{"category":"section","location":"positioning/#Positioning-and-ecosystem","page":"Positioning & ecosystem","text":"BMOPFTools is infrastructure for distribution-system optimization research: it constructs, validates, converts, and benchmarks conductor-level network models. It is not positioned as another distribution OPF solver — although it ships a reference four-wire IVR-EN OPF (solve_opf) used to validate cases and profile solutions, the product is the model and the tooling around it, not the optimizer.","title":"Positioning & ecosystem"},{"category":"section","location":"positioning/#The-transmission/distribution-maturity-gap","page":"Positioning & ecosystem","text":"Power-system software is often discussed as one ecosystem, but transmission and distribution have matured very differently.\n\nTransmission optimization has decades of shared conventions and benchmarks — MATPOWER, PowerModels.jl, and PGLib-OPF among them — which let researchers compare algorithms on common test systems with well-understood problem definitions, and reproduce each other's results.\n\nDistribution optimization remains fragmented. Tools such as PowerModelsDistribution.jl, ppOPF, Open-DSOPF, and various OpenDSS- and GridLAB-D-based workflows solve overlapping problems — Volt/VAr optimization, DER coordination, network reconfiguration, hosting-capacity analysis, distribution OPF — but rely on different data models, feeder representations, assumptions, formulations, and conversion pipelines. As a result, reproducing results and fairly comparing methods is hard.","title":"The transmission/distribution maturity gap"},{"category":"section","location":"positioning/#What-BMOPFTools-is","page":"Positioning & ecosystem","text":"Rather than competing with optimization frameworks, BMOPFTools strengthens the ecosystem around them. It focuses on:\n\nNetwork representation — a faithful, conductor-level (up-to-four-wire) data model (conventions);\nData interoperability — OpenDSS ingestion via from_dss and export to PowerModelsDistribution via to_pmd (conversion guide);\nAnalytical model construction & validation — provenance, data-quality, and spec-conformance analysis (analysis & reports);\nBenchmark generation — structural repair and standards-grounded gap-filling that turn raw utility feeders into well-posed cases (case augmentation);\nReproducible workflows — a stable, code-addressable diagnostic vocabulary (finding-code reference).","title":"What BMOPFTools is"},{"category":"section","location":"positioning/#How-it-relates-to-existing-tools","page":"Positioning & ecosystem","text":"BMOPFTools sits between the simulation tools that produce network data and the optimization frameworks that consume it — supplying the shared representation and benchmark cases that let those frameworks be compared. It is complementary to all of the below, not a replacement for any.\n\nProject / tool Focus Relationship to BMOPFTools\nMATPOWER Transmission OPF conventions & benchmarks Inspiration — BMOPFTools aims to bring comparable reproducibility to distribution\nPowerModels.jl Optimization formulations (transmission) Complementary formulation layer; BMOPFTools supplies the network model\nPowerModelsDistribution.jl Distribution OPF formulations & solution methods Complementary and interoperable (to_pmd export); BMOPFTools supplies benchmark cases\nppOPF, Open-DSOPF Distribution OPF implementations Complementary; BMOPFTools provides shared representations & benchmark feeders for comparison\nOpenDSS, GridLAB-D, Power Grid Model, PowerFactory, PSS®SINCAL, CYME, Synergi Electric Simulation & engineering analysis Data sources; BMOPFTools ingests their models and produces an optimization-ready representation","title":"How it relates to existing tools"},{"category":"section","location":"positioning/#The-benchmarking-gap,-and-the-opportunity","page":"Positioning & ecosystem","text":"The throughline above is a missing layer of shared benchmark infrastructure for distribution optimization: feeder conversion is lossy, assumptions are inconsistent, data models are incompatible, and reproducibility is limited, so published methods are difficult to compare fairly.\n\nTransmission optimization matured rapidly once the community converged on common benchmark systems and data formats. Distribution likely needs the same shift. BMOPFTools is intended to help create the conditions for it — reusable benchmark feeders, common network representations, transparent model transformations, and cross-framework interoperability — so the broader distribution-optimization ecosystem can mature, without replacing the optimization frameworks at its core.","title":"The benchmarking gap, and the opportunity"},{"category":"section","location":"augmentation/#Case-augmentation","page":"Case augmentation","text":"","title":"Case augmentation"},{"category":"section","location":"augmentation/#From-a-faithful-import-to-a-meaningful-benchmark","page":"Case augmentation","text":"A distribution network freshly imported from OpenDSS is physically faithful but optimization-meaningless. It describes conductors, transformers and loads exactly — but it carries nothing for an optimizer to respect or decide. There are no voltage bounds, so no operating point can be infeasible. There are no thermal limits, so no line can bind. There is no dispatchable generation, so the only \"decision\" is for the slack to import whatever the loads draw. Run an OPF on a raw import and the solver returns the power-flow solution: the optimisation is trivial because the model contains nothing to optimise against.\n\nCase augmentation is the bridge from a valid network to a meaningful OPF instance. It is the same gap that the AC transmission community closed with PGLib-OPF — a curated benchmark library exists precisely because raw network data lacks the generation limits, costs and thermal ratings an OPF needs, and those must be added deliberately and reproducibly rather than guessed per study (ref. 1). BMOPFTools brings that discipline to the unbalanced four-wire distribution setting with three composable, audited operations:\n\nfix_case — structural repairs. Remove inert elements, drop disconnected islands, collapse near-zero-impedance lines to switches, strip redundant bounds. Make the graph sound before anything is written onto it.\nadd_generators / add_inverters — deliberate DER placement. Create dispatchable generator elements (or richer inverter-interfaced inverter elements) at semantically/topologically chosen buses so the OPF has something to decide. Never random; every placement is explained.\naugment_case — standards-grounded gap-filling. Inject voltage bounds, infer thermal limits, price the slack, and derive reactive capability. Fills only what is missing; never overwrites.\n\nEach returns a (net′, TransformationManifest) pair and never mutates its input. The manifest is the heart of the contract: every value written is recorded with the rule that produced it and a confidence tag (standards-derived vs :synthetic), so the resulting case is self-documenting and the modeler can see at a glance exactly which numbers are defensible defaults and which are design choices worth revisiting (see A starting point for fine-tuning).","title":"From a faithful import to a meaningful benchmark"},{"category":"section","location":"augmentation/#Why-this-sequence","page":"Case augmentation","text":"The three operations are a pipeline, not a menu. The order\n\nfix_case ──► add_generators ──► augment_case ──► solve_opf\n   │              │                   │\n repair       place DERs         fill gaps\n topology     (p/p_max/cost)     (bounds, limits,\n                                  q_min/q_max, slack cost)\n\nis deliberate, and each edge exists for a reason:\n\nfix_case first — bounds and generators placed on a broken topology are wasted or actively wrong. A DER stranded on an islanded bus injects into nothing; a thermal limit on a zero-impedance loop is meaningless. Repair the graph — drop the islands, collapse the switch-like lines, remove inert loads — before annotating it, so every later pass operates on the network that will actually be solved.\nadd_generators before the final augment_case — placement writes only p_min/p_max/cost. The reactive capability of each DER (q_min/q_max) is then filled by augment_case's generation pass from a single power-factor rule. Reversing the order would leave the freshly-placed DERs with active-power bounds but no reactive capability — silently restricting the very flexibility you added them to study. Keeping reactive logic in one place (augment_case) is why generators go in first.\naugment_case last — gap-filling is idempotent and only ever fills missing fields, so it should see the final element set: the repaired topology plus the new DERs. Run last, it bounds everything once and completely.\n\nThe placement step (add_generators, or add_inverters for converter-interfaced DERs) is genuinely optional. If you only need a feasible power-flow benchmark (CVR, state estimation, maximum-load-delivery studies) you can run fix_case → augment_case and skip placement entirely — the slack remains the only injector. Add DERs when you want the dispatch itself to be the object of study.","title":"Why this sequence"},{"category":"section","location":"augmentation/#Recommended-workflow","page":"Case augmentation","text":"using BMOPFTools\n\nnet = parse_bmopf(\"my_feeder.json\")          # or from_dss\n\nnet1, fix_mf = fix_case(net)                 # 1. structural repairs first\nnet2, der_mf = add_generators(net1;          # 2. place DERs (optional)\n                 recipe = GeneratorRecipe(strategy = :load_following))\nnet3, aug_mf = augment_case(net2)            # 3. fill bounds, limits, q, slack cost\n\nrender_manifest(fix_mf)                      # inspect what was repaired\nrender_manifest(der_mf)                      # inspect every placement\nrender_manifest(aug_mf)                      # inspect what was added\n\nwrite_bmopf(\"my_feeder_ready.json\", net3)\n\nresult  = solve_opf(net3)\nreport′ = profile_solution(net3, result)\n\nPassing the pre-computed analyze result to augment_case avoids re-running voltage-level and provenance analyses internally:\n\nreport  = analyze(net2)\nnet3, aug_mf = augment_case(net2; analysis=report.analysis)\n\nThe four augment_case passes below run in order. Each can be disabled independently via the AugmentationRecipe apply_* flags.  No pass ever overwrites an existing value — augmentation only fills gaps.","title":"Recommended workflow"},{"category":"section","location":"augmentation/#Pass-1-—-Voltage-bounds","page":"Case augmentation","text":"Sets v_min/v_max, vpn_min/vpn_max, vpp_min/vpp_max, and vneg_max on buses that lack them. vpn_* are written as per-phase arrays and vpp_* as per-pair arrays (see Conventions); the OPF consumes them in those shapes.\n\nBounds are expressed as fractions of a declared supply voltage — the nominal voltage defined by the relevant power-quality standard, which may differ from the transformer rated voltage in the network model (e.g. a 240 V transformer in a grid declared at 230 V per EN 50160).  The declared voltage is resolved per bus in priority order:\n\nbus[\"v_declared\"] — explicit field set at import time (e.g. authored in the BMOPF JSON)\nthe optional voltage-snap pass (see below) — writes v_declared by snapping v_nom to a standard level; never overrides an explicit value\nAugmentationRecipe fields v_declared_lv / v_declared_mv / v_declared_hv — regional fallback (e.g. v_declared_lv = 230.0 for Europe/Australia)\nv_nom from voltage_level_analysis — last resort when none of the above is set","title":"Pass 1 — Voltage bounds"},{"category":"section","location":"augmentation/#Pass-0-—-Voltage-level-snapping-(optional)","page":"Case augmentation","text":"Imported cases frequently carry LV transformers rated 240 or 250 V, so the derived v_nom is non-standard and the bounds would be referenced to it. When enabled, augment_case first snaps each bus's v_nom to the nearest standardised level (IEC 60038 / ANSI C84.1) and writes the result as v_declared, so the bounds below reference the standard voltage (e.g. 230 V).\n\nConfigured in the TOML config (off by default — no behaviour change unless opted in):\n\n[augment.voltage_snap]\nenabled   = true        # opt-in\npreset    = \"IEC_50Hz\"  # \"IEC_50Hz\" | \"ANSI_60Hz\" | \"none\"\ntolerance = 0.10        # snap only if |v_nom / std − 1| ≤ tolerance\nlevels    = []          # extra phase-to-neutral volts, merged with the preset\n\nA value outside every tolerance band is left unchanged, so genuinely non-standard buses (e.g. a real 277 V LV) are preserved. Levels are phase-to-neutral (per-conductor) volts — the same basis as v_nom; custom levels follow the same convention (line-to-line ÷ √3). A bus that already carries an explicit v_declared is never re-snapped. Each snap is recorded in the manifest as a v_declared entry with rule IEC60038_snap. Pass the config via augment_case(net; config=load_config(\"my.toml\")).","title":"Pass 0 — Voltage-level snapping (optional)"},{"category":"section","location":"augmentation/#Solver-regularisation-bounds-(v_min-/-v_max)","page":"Case augmentation","text":"v_min and v_max are hyperparameters, not power-quality guarantees. They widen the feasible set so the solver can find a feasible point even when the operating point sits at the edge of the physical window.  The same values are applied to all buses regardless of voltage level.\n\nField Default (pu of v_declared) Purpose\nv_min 0.85 Lower regularisation bound — disable with v_min_pu = nothing\nv_max 1.15 Upper regularisation bound — disable with v_max_pu = nothing\n\nSource buses receive v_min/v_max only — vpn/vpp/vneg bounds are meaningless there because the voltage source pins the terminal voltages.","title":"Solver regularisation bounds (v_min / v_max)"},{"category":"section","location":"augmentation/#Power-quality-bounds","page":"Case augmentation","text":"Applied to all non-source buses.  Percentage windows follow EN 50160:2010.\n\nFour-wire buses (have a neutral terminal) receive vpn bounds, since customers experience the phase-to-neutral voltage.  vpp bounds are also set when ≥ 2 phase terminals are present.\n\nBound Level Default (pu of v_declared) Standard\nvpn_min/vpn_max LV (≤ 1 kV) 0.90 / 1.10 EN 50160:2010 §3.5/§3.6, 95 %-of-week ±10 %\nvpn_min/vpn_max MV (1–35 kV) 0.94 / 1.06 DSO planning practice ±6 %, budgets for LV voltage drop\nvpp_min/vpp_max LV 0.90 / 1.10 EN 50160:2010 §3.5, same ±10 % band\nvpp_min/vpp_max MV 0.94 / 1.06 Same ±6 % band as vpn\nvpp_min/vpp_max HV (> 35 kV) 0.95 / 1.05 Transmission planning ±5 %\n\nThree-wire buses (no neutral terminal) receive vpp bounds only.\n\nSingle-phase buses (one phase terminal + neutral) receive vpn but not vpp or vneg_max.\n\nUnassigned buses (islanded from all voltage sources) are skipped; the manifest records a note.\n\nBound Applies to Default Standard\nvneg_max Four-wire, ≥ 2 phases, non-source 0.02 × vpn_declared EN 50160:2010 §3.5 — VUF ≤ 2 %","title":"Power-quality bounds"},{"category":"section","location":"augmentation/#Pass-2-—-Thermal-limits","page":"Case augmentation","text":"Infers i_max for linecodes that lack it by matching the diagonal series resistance R₁₁ against an IEC 60228:2004 / IEC 60364-5-52:2009 lookup table.\n\nThe table covers cross-sections from 4 mm² to 240 mm²:\n\nCross-section (mm²) R₁₁ (mΩ/m at 20 °C) Underground XLPE (A) Overhead AAC (A)\n4 4.950 34 —\n6 3.300 41 —\n10 1.980 57 70\n16 1.240 76 95\n25 0.787 104 130\n35 0.559 134 160\n50 0.396 170 200\n70 0.283 220 260\n95 0.209 277 320\n120 0.164 326 375\n150 0.132 386 430\n185 0.107 451 490\n240 0.082 541 600\n\nSources: IEC 60228:2004 (AC resistance at 20 °C), IEC 60364-5-52:2009 Table B.52 (ampacity at 70 °C conductor temperature, single-circuit in-ground or in-air installation).\n\nThe match uses a 15 % relative tolerance on R₁₁.  If no table row falls within tolerance the linecode is skipped and the manifest records \"R₁₁ outside lookup range\".\n\nProvenance confidence gating.  The inference is only reliable when R₁₁ comes from a first-principles geometry model (Carson/Pollaczek).  The pass checks the provenance_analysis verdict for each linecode:\n\nVerdict Confidence Included by default?\ndistinct :high Yes\nnear_balanced :medium Yes (default threshold)\nexactly_balanced :low No\ndecoupled :low No\n\nLower the threshold with AugmentationRecipe(thermal_min_confidence=:low) to infer limits even from sequence-derived matrices, or raise it to :high to restrict to geometry-derived data only.\n\nNeutral conductor rating.  The neutral conductor is assigned the same i_max as the phase conductors.  IEC 60364-5-52:2009 §523 permits a reduced neutral cross-section above 16 mm² under balanced loading conditions, but the appropriate reduction is installation- and load-specific and is not applied automatically.  Set the neutral entry of i_max in the linecode manually, or pass a pre-computed i_max vector (which will not be overwritten) if a derated rating is required.","title":"Pass 2 — Thermal limits"},{"category":"section","location":"augmentation/#Pass-3-—-Generation","page":"Case augmentation","text":"Slack cost.  The voltage source is itself the network's current slack, so no slack generator is created. If a source has no cost, a per-phase cost is written onto the voltage_source (default 1.0 $/kWh) so imported power is priced in the objective. No flow bounds are added, so the slack stays unconstrained and the OPF can always find a feasible point. Controlled by the recipe's apply_slack_generator / slack_cost fields (names kept for backwards compatibility).\n\nReactive bounds.  For each generator that has p_max defined but lacks q_min/q_max, symmetric reactive bounds are derived from the recipe power-factor setting:\n\nQ_max = P_max × tan(arccos(pf))\n\nDefault pf = 0.90 (EN 50549-1:2019, LV grid-connected DERs): Qmax ≈ 0.484 × Pmax.  Set q_capability_pf = 0.95 for IEEE 1547-2018 (ANSI) deployments: Qmax ≈ 0.329 × Pmax.","title":"Pass 3 — Generation"},{"category":"section","location":"augmentation/#pass-4-inverter-dispatch-bounds","page":"Case augmentation","text":"inverter elements carry a richer model than generators (an apparent-power nameplate s_max, a topology, a prime_mover, and an optional smart-inverter control_profile). This pass fills their active and reactive dispatch box from the nameplate, leaving the nameplate itself untouched. Controlled by the recipe's apply_inverter flag; disabled with apply_inverter = false.\n\nActive power. If p_max is absent it is derived per phase — from p_avail (split equally across phases) or from the per-phase s_max ratings. For PV prime movers p_min = 0 is also injected when absent, since PV cannot absorb active power.\n\nReactive power. If the inverter references a control_profile carrying a power_factor sub-object, q_min/q_max are left absent — the OPF enforces the exact PF coupling Q = f(P) as an equality constraint instead. Otherwise, for inverters lacking explicit q_min/q_max, symmetric bounds are derived from p_max using the recipe's inverter_default_pf (EN 50549-1:2019 default cos φ = 0.90), exactly as for generators:\n\nQ_max = P_max × tan(arccos(pf)),   Q_min = -Q_max\n\nThis pass only bounds inverters that already exist in the case. To place inverters in the first place, see Adding inverters below; the intended order is add_inverters → augment_case, so this pass turns each placed nameplate into a full dispatch box.","title":"Pass 4 — Inverter dispatch bounds"},{"category":"section","location":"augmentation/#fix_case-—-structural-repairs","page":"Case augmentation","text":"Seven passes run in order.  Each is independently controlled by the corresponding apply_* flag in FixRecipe.  All passes default to true except the two that change power-system semantics, which default to false and must be opted into explicitly.\n\n# Pass Default Notes\n1 Largest connected component true Drops buses, lines, loads, generators, and shunts that have no path to a voltage source.\n2 Simplify network true Merges consecutive same-linecode series lines; removes dangling stub lines (wraps simplify_network).\n3 Remove zero loads true Deletes loads with p_nom = q_nom = 0 on all phases — electrically inert.\n4 Low-impedance lines → switches true Replaces lines with total series impedance |Z| < threshold (default 10⁻⁴ Ω) with closed switches.\n5 Source bus bounds true Strips all voltage bounds from source buses — redundant because the voltage source pins the terminal voltages exactly.\n6 Adjacent current bounds false Infers i_max for lines/switches that lack it from directly adjacent elements (one hop). Transformers contribute s_rating / (√3 × V_ref); lines and switches propagate their own i_max. Takes the minimum over all adjacent bounds.\n7 Perfect grounding false Promotes grounding shunts whose 1/|Y₁₁| < threshold (default 0.1 Ω) to perfectly_grounded_terminals and removes the shunt. Changes OPF physics (forces V_n = 0).","title":"fix_case — structural repairs"},{"category":"section","location":"augmentation/#augment_case-—-standards-grounded-gap-filling","page":"Case augmentation","text":"","title":"augment_case — standards-grounded gap-filling"},{"category":"section","location":"augmentation/#TransformationManifest","page":"Case augmentation","text":"","title":"TransformationManifest"},{"category":"section","location":"augmentation/#Serialising-the-manifest","page":"Case augmentation","text":"The manifest is intended to travel alongside the augmented case file so the pair (case.json, case_manifest.json) is self-documenting:\n\nusing JSON3\n\nnet′, manifest = augment_case(net)\nwrite_bmopf(\"feeder_aug.json\", net′)\nopen(\"feeder_aug_manifest.json\", \"w\") do io\n    JSON3.write(io, manifest_to_dict(manifest))\nend","title":"Serialising the manifest"},{"category":"section","location":"augmentation/#Adding-generators-(DER-placement)","page":"Case augmentation","text":"augment_case deliberately only gap-fills standards-grounded bounds; it does not create generation.  Without dispatchable generators the OPF is trivial (the slack imports everything).  add_generators is a separate, opt-in pass that places dispatchable generator elements using the semantic and topological knowledge the library already computes — never randomly — so the OPF becomes a meaningful optimisation.  Like fix_case/augment_case it never mutates its input and returns a (net′, manifest) pair recording every field written.\n\nIt writes only bus, terminal_map, configuration, p_min, p_max, and cost; reactive bounds are intentionally left to augment_case's generation pass (this is the ordering constraint from Why this sequence).","title":"Adding generators (DER placement)"},{"category":"section","location":"augmentation/#Why-diverse-strategies-matter","page":"Case augmentation","text":"There is no single correct way to place DERs — the right placement depends on the question the benchmark is meant to probe, and a different question puts a different constraint on the binding edge of the feasible set. This mirrors how synthetic-network and hosting-capacity practice treats DER scenarios as a designed input: EPRI's DRIVE hosting-capacity method deliberately contrasts distributed placement (many small injections spread across the feeder, representative of residential rooftop PV) against centralised placement (a single large interconnection), because the two stress the network in opposite ways (ref. 2); and the NREL SMART-DS effort treats DER siting and sizing as configurable scenario knobs rather than a fixed fact of the network (ref. 3). Offering several placement strategies is therefore a matter of experimental design, not optional decoration — it lets one base feeder generate a family of benchmark instances that exercise distinct physics.\n\nChoose the strategy from the research question:\n\nResearch question Strategy What it stresses (binds first)\nRealistic prosumer dispatch, CVR, self-consumption :load_following Where demand already is; mild, distributed counter-flow\nVoltage-rise limits, embedded-generation hosting capacity :topology_targeted, topology_mode = :leaves Worst-case voltage rise at feeder ends, far from the source\nBulk reverse power flow, upstream thermal limits :topology_targeted, topology_mode = :near_source or :hosting_capacity Transformer/head-of-feeder loading and reverse flow\n\nThe point of supporting all three is that a single base feeder can be turned into a family of benchmark instances — a voltage-rise case, a thermal-headroom case, a realistic-dispatch case — each making a different constraint the active one. That diversity is what makes the resulting set useful for comparing OPF formulations and solvers, not just solving one network once.","title":"Why diverse strategies matter"},{"category":"section","location":"augmentation/#The-recipe-knobs","page":"Case augmentation","text":"Placement is controlled by a GeneratorRecipe:\n\nstrategy — :load_following (one DER per load bus, phasing inherited from the load), :hosting_capacity (DERs sized to a fraction of the feeding transformer's s_rating), or :topology_targeted (topology_mode = :leaves for embedded generation at feeder ends, or :near_source for bulk injection).\nfilters — voltage_levels (e.g. [:LV]), min_local_load_va, and skip_source_buses compose over any strategy.\nsizing — size_basis (:fraction_of_local_load, :fraction_of_transformer_rating, :fraction_of_downstream_load, :fixed_tiers) with der_p_fraction.\ncost — cost_basis (:cheaper_than_slack, :tiered_by_level, :uniform); pricing DERs below the slack makes dispatch non-trivial — the solver uses local generation until a voltage or thermal constraint binds.\n\nEvery generator field written is recorded as a TransformEntry with rule DER_PLACEMENT/<strategy> and confidence :synthetic (a design choice, not a standard), and the run emits I.DER.PLACED / W.DER.NO_CANDIDATES / W.DER.OVERSUPPLY findings into the manifest's findings_after. Because every placement is tagged :synthetic, the manifest doubles as the list of knobs a modeler will most likely want to tune (see below).","title":"The recipe knobs"},{"category":"section","location":"augmentation/#adding-inverters-der-placement","page":"Case augmentation","text":"add_inverters is the inverter-interfaced counterpart to add_generators. Where a generator is a thin active-power object, an inverter carries a richer model — an apparent-power nameplate s_max, a topology (FOUR_LEG / THREE_LEG / SINGLE_PHASE), a prime_mover, and an apparent-power circle P² + Q² ≤ s_max² in the OPF. Use it when the benchmark should model PV/storage converters with explicit VA headroom rather than plain dispatchable generation.\n\nThe division of labour mirrors generators exactly: placement places, augment bounds. add_inverters writes only the nameplate — bus, terminal_map, topology, prime_mover, s_max, p_avail, cost — and Pass 4 of augment_case then derives the p_max/p_min/q_min/q_max dispatch box from that nameplate. The intended order is therefore the same one-line pipeline with add_inverters in the generator slot:\n\nnet1, _   = fix_case(net)\nnet2, imf = add_inverters(net1;\n              recipe = InverterRecipe(strategy = :load_following))\nrender_manifest(imf)               # every placement is explained\nnet3, _   = augment_case(net2)     # fills p_max/p_min/q_min/q_max on the new inverters\n\nPlacement is controlled by an InverterRecipe. The strategy, filter and cost knobs are identical in meaning to GeneratorRecipe (so the strategy-diversity guidance applies unchanged), with these inverter-specific additions:\n\nprime_mover — :PV (the MVP default; drives p_min = 0 in the augment pass). Battery and grid-forming inverters are planned but not yet placed.\ninverter_topology — :infer (FOURLEG when the host load has a neutral terminal, else SINGLEPHASE), or a forced :FOUR_LEG / :THREE_LEG / :SINGLE_PHASE.\nsizing targets s_max, not p_max: size_basis (same four bases as generators) with s_fraction sets the apparent-power nameplate, and s_to_p_ratio sets p_avail = s_to_p_ratio × s_max (1.0 = unity-rated PV; below 1.0 leaves reactive headroom at full irradiance).\n\nEvery field written is recorded as a :synthetic TransformEntry with rule INVERTER_PLACEMENT/<strategy>, and the run emits I.INV.PLACED / W.INV.NO_CANDIDATES / W.INV.OVERSUPPLY findings into the manifest's findings_after.\n\nnote: I/O converter support is pending\nsolve_opf dispatches placed inverters once augment_case has filled their P/Q box — the OPF engine fully models inverters (apparent-power circle, topology-dependent voltage reference, constant-PF coupling). What is not yet wired is the I/O layer: to_pmd/from_dss do not yet map inverter elements, so inverters cannot round-trip through PowerModelsDistribution or OpenDSS.","title":"Adding inverters (DER placement)"},{"category":"section","location":"augmentation/#starting-point","page":"Case augmentation","text":"The three passes do not claim to produce the one true benchmark. They produce a defensible default — every value is either copied from a published standard or flagged as a deliberate synthetic choice — and the manifest is the map of which is which. That distinction is the whole point: it tells a modeler exactly where to spend their fine-tuning effort.\n\nRead the manifest back by confidence tag:\n\nStandards-derived entries (EN 50160 voltage windows, IEC 60364 ampacities, EN 50549-1 reactive capability) are safe to keep unless the case targets a jurisdiction with different rules — in which case override the relevant recipe field (v_declared_lv, q_capability_pf, …) and re-run.\n:synthetic entries (every DER placement, the slack price) are design choices. These are the first things to revisit: the placement strategy, the der_p_fraction sizing, the cost_basis. Change them and you change what the OPF decides.\n\nThe intended loop is therefore iterative, not one-shot:\n\nnet3, mf = add_generators(net2;\n             recipe = GeneratorRecipe(strategy = :topology_targeted,\n                                      topology_mode = :leaves))\nrender_manifest(mf)                      # see every synthetic placement & its driver\n# … decide the feeder-end DERs are oversized; lower the fraction and re-run …\nnet3, mf = add_generators(net2;\n             recipe = GeneratorRecipe(strategy = :topology_targeted,\n                                      topology_mode = :leaves,\n                                      der_p_fraction = 0.3))\n\nBecause each pass is pure and re-runnable, and because the manifest records the rule behind every field, a modeler can converge on a case tailored to their study without ever reverse-engineering an opaque, pre-cooked benchmark file.","title":"A starting point for fine-tuning"},{"category":"section","location":"augmentation/#Why-augment?-—-rationale-and-literature","page":"Case augmentation","text":"The approach here is not ad hoc; it follows established practice in two adjacent communities.\n\nCurated benchmarks, not raw data. The AC transmission community learned that raw network snapshots make poor optimisation benchmarks: they lack generation limits, costs and thermal ratings, so an OPF over them is under-constrained and non-comparable across studies. PGLib-OPF answered this by curating a library in which \"all networks have reasonable values for key parameters — generation injection limits, generation costs, and branch thermal limits\" (ref. 1). augment_case does the four-wire-distribution equivalent, with the added discipline that the source of every value (which standard, or \"synthetic\") is recorded rather than baked in silently. The companion design-goals analysis for unbalanced OPF benchmarks argues the same point specifically for distribution: a benchmark must make its bounds and assumptions explicit to be reproducible (ref. 4).\n\nStandards as the source of defaults. Where a value can be grounded in a published standard, it should be — so the default is auditable rather than arbitrary. Voltage windows follow EN 50160:2010, conductor ampacities follow IEC 60364-5-52:2009 over IEC 60228:2004 resistances, and DER reactive capability follows EN 50549-1:2019 (with IEEE 1547-2018 as the ANSI alternative, whose minimum 44 % injecting / 44 % absorbing reactive capability motivates the q_capability_pf = 0.95 preset) (ref. 5). This is why augment_case separates standards-derived fills from synthetic ones in the manifest.\n\nDER scenarios as designed inputs. Where a value cannot be standardised — chiefly where to place generation and how large to make it — the literature treats it as a deliberately varied scenario rather than a fixed fact. EPRI's DRIVE hosting-capacity methodology contrasts distributed against centralised DER placement precisely because they stress a feeder differently (ref. 2), and NREL's SMART-DS programme exposes DER siting/sizing as configurable scenario knobs over a fixed base network (ref. 3). add_generators follows this practice directly: its strategies (:load_following, :topology_targeted leaves/near-source, :hosting_capacity) are the BMOPF analogues of those scenario choices, and tagging them :synthetic is the honest acknowledgement that they are design decisions, not measurements.","title":"Why augment? — rationale and literature"},{"category":"section","location":"augmentation/#Limitations","page":"Case augmentation","text":"The following augmentation tasks are not handled by augment_case:\n\nDER placement — handled by the separate add_generators pass documented above, not by augment_case, because where to place generators and how to size them is a deliberate design choice rather than standards-derivable.\nZero-sequence voltage bound (vzero_max) — the appropriate limit depends on the earthing system (TN, TT, IT have fundamentally different zero-sequence behaviour) and cannot be standardised without knowing the system's neutral earthing arrangement.  Use provenance_analysis earthing zone results to choose a value if needed.\nReactive load (q_nom) — existing reactive demand is preserved as-is. If loads were imported with pf = 0.88 OpenDSS defaults (flagged by I.PROV.OPENDSS_DEFAULT_PF) the values should be reviewed before treating them as benchmark data.\nTransformer s_max — s_rating already plays this role; the solver uses it directly.  No separate s_max augmentation is needed.","title":"Limitations"},{"category":"section","location":"augmentation/#augrefs","page":"Case augmentation","text":"Numbered citations above refer to this list. Power-quality and ampacity standards (EN 50160:2010, EN 50549-1:2019, IEC 60228:2004, IEC 60364-5-52:2009) are cited inline at their point of use in the tables above.\n\nS. Babaeinejadsarookolaee et al., \"The power grid library for benchmarking AC optimal power flow algorithms,\" arXiv:1908.02788, 2019. (PGLib-OPF, maintained by the IEEE PES Task Force on Benchmarks for Validation of Emerging Power System Algorithms.)\nElectric Power Research Institute, Distribution Resource Integration and Value Estimation (DRIVE), EPRI, Palo Alto, CA, 2016 — hosting-capacity methodology contrasting distributed vs centralised DER placement.\nB. Palmintier et al., \"SMART-DS: Synthetic models for advanced, realistic testing — distribution systems and scenarios,\" National Renewable Energy Laboratory (NREL), Golden, CO, 2017.\nF. Geth, A. C. Chapman, R. Heidari, J. Clark, \"Considerations and design goals for unbalanced optimal power flow benchmarks,\" Electric Power Systems Research 235 (2024) 110646.\nIEEE Std 1547-2018, IEEE Standard for Interconnection and Interoperability of Distributed Energy Resources with Associated Electric Power Systems Interfaces, IEEE, 2018.","title":"References"},{"category":"function","location":"augmentation/#BMOPFTools.fix_case","page":"Case augmentation","text":"fix_case(net; recipe=FixRecipe()) -> (net′, TransformationManifest)\n\nApply structural repairs to a BMOPF network dict, returning an independent deep copy together with a TransformationManifest recording every change.\n\nnet is never mutated.  Passes run in order; each is independently controlled by the corresponding apply_* flag in recipe.\n\nPasses (in order)\n\nLargest connected component — drop buses, lines, loads, generators, and shunts that belong to components unreachable from any voltage source.\nSimplify network — merge consecutive same-linecode series lines and remove dangling stub lines (wraps simplify_network).\nRemove zero loads — delete loads with p_nom = q_nom = 0 on all phases.\nLow-impedance lines → switches — replace lines whose total per-conductor series impedance is below recipe.low_impedance_threshold_ohm with closed switches.\nSource bus bounds — remove all voltage bounds (v_min/v_max, vpn_*, vpp_*, vneg_max, etc.) from source buses; they are redundant because the voltage source fixes the terminal voltages exactly.\nAdjacent current bounds (opt-in, default off) — for each line or switch lacking i_max, infer an upper bound from directly adjacent elements (lines, switches, transformers) at either endpoint bus.\nPerfect grounding (opt-in, default off) — promote grounding shunts whose equivalent resistance is below recipe.perfect_grounding_threshold_ohm to perfectly_grounded_terminals entries and remove the shunt.\n\nExample\n\nnet  = parse_bmopf(\"feeder.json\")\nnet′, fix_mf  = fix_case(net)\nnet″, aug_mf  = augment_case(net′)\nrender_manifest(fix_mf)\nresult = solve_opf(net″)\n\n\n\n\n\n","title":"BMOPFTools.fix_case"},{"category":"type","location":"augmentation/#BMOPFTools.FixRecipe","page":"Case augmentation","text":"FixRecipe\n\nParameters controlling which repair passes fix_case runs and their thresholds.  Lossless, semantics-preserving passes default to true; the one pass that changes power-system physics (apply_perfect_grounding) defaults to false and must be opted into explicitly.\n\n\n\n\n\n","title":"BMOPFTools.FixRecipe"},{"category":"function","location":"augmentation/#BMOPFTools.augment_case","page":"Case augmentation","text":"augment_case(net; recipe=default_recipe(), analysis=nothing, config=_DEFAULT_CONFIG)\n    -> (net′::Dict{String,Any}, manifest::TransformationManifest)\n\nDerive a benchmark-ready version of net by injecting standards-grounded default bounds and constraints for any fields that are absent.\n\nnet is never mutated.  The returned net′ is an independent deep copy.\n\nArguments\n\nnet      — a BMOPF network dict (from parse_bmopf or              from_dss)\nrecipe   — an AugmentationRecipe controlling which passes run              and what defaults to use; see default_recipe\nanalysis — output of analyze or a dict containing at least              \"voltage_levels\" and \"provenance\" sub-results; if nothing              the relevant sub-analyses are run internally\nconfig   — tunable thresholds (see load_config); currently drives              optional voltage-level snapping ([augment.voltage_snap]), which              snaps each bus's derived nominal to a standard IEC/ANSI level and              writes v_declared before the bounds pass. Off by default.\n\nReturns\n\nA 2-tuple (net′, manifest) where:\n\nnet′ is the augmented network dict\nmanifest is a TransformationManifest recording every field written, the standards rule that motivated it, and benchmark-readiness findings before and after augmentation\n\nPasses (in order)\n\nVoltage snapping (optional) — snap each bus's derived nominal to a standard IEC 60038 / ANSI C84.1 level and write v_declared ([augment.voltage_snap] in config; off by default)\nVoltage bounds — v_min/v_max, vpn_min/vpn_max, vpp_min/vpp_max, vneg_max on buses (EN 50160, DSO planning practice)\nThermal limits — i_max on linecodes inferred from R₁₁ via IEC 60228\nGeneration — slack generator at source buses; q_min/q_max on existing generators with p_max (EN 50549 / IEEE 1547)\n\nEach pass is independently skippable via the recipe's apply_* flags. Existing values are never overwritten.\n\nExample\n\nnet    = parse_bmopf(\"my_feeder.json\")\nnet′, manifest = augment_case(net)\nrender_manifest(manifest)\nresult = solve_opf(net′)\n\n\n\n\n\n","title":"BMOPFTools.augment_case"},{"category":"type","location":"augmentation/#BMOPFTools.AugmentationRecipe","page":"Case augmentation","text":"AugmentationRecipe\n\nParameters controlling which augmentation passes run and what default values they inject.  Every field has a standards-grounded default; override only what you need.\n\nConstruct with keyword arguments:\n\nrecipe = AugmentationRecipe(vpn_lv_pu = (0.85, 1.15))\n\nor use default_recipe to get the unmodified defaults.\n\nDeclared supply voltage fallbacks\n\nVoltage bounds are expressed as percentages of a declared supply voltage, not the transformer rated voltage.  The authoritative source is the optional v_declared field on the bus (V).  When absent, the recipe fallbacks below are used; when those are nothing, the bus's v_nom from voltage-level analysis is used as a last resort.\n\nThe declared voltage is expressed per conductor (phase-to-ground ≈ phase-to-neutral), the same basis as v_nom.  Phase-to-neutral bounds use it directly; phase-to-phase (line-to-line) bounds derive their nominal as v_declared × √3.  Set the fallbacks to the per-conductor declared voltage for the deployment region, e.g. v_declared_lv = 230.0 for Europe/Australia (230 V L-N → 400 V L-L); for an 11 kV (L-L) MV system use v_declared_mv = 11000 / √3 ≈ 6350.0.\n\nFull precedence for the declared voltage, highest first:\n\nbus[\"v_declared\"] — explicit, set at import time\nthe optional voltage-snap pass ([augment.voltage_snap] in the TOML config) — snaps v_nom to a standard IEC/ANSI level and writes v_declared; never overrides an explicit v_declared\nthese recipe fallbacks (v_declared_lv/mv/hv)\nv_nom from voltage-level analysis\n\nSnapping (off by default) is the recommended way to pull imported 240/250 V transformers onto the standard 230 V without per-bus editing.\n\n\n\n\n\n","title":"BMOPFTools.AugmentationRecipe"},{"category":"function","location":"augmentation/#BMOPFTools.default_recipe","page":"Case augmentation","text":"default_recipe() -> AugmentationRecipe\n\nReturn an AugmentationRecipe with all defaults as specified in the package documentation.\n\n\n\n\n\n","title":"BMOPFTools.default_recipe"},{"category":"type","location":"augmentation/#BMOPFTools.TransformationManifest","page":"Case augmentation","text":"TransformationManifest\n\nComplete audit trail for one call to augment_case.\n\nFields\n\ncreated_at      — ISO-8601 timestamp of the augmentation run\nrecipe          — the recipe used (AugmentationRecipe, FixRecipe, or                     GeneratorRecipe); the change detail is also captured in entries\nentries         — ordered list of TransformEntry records\nfindings_before — benchmark_readiness_check findings on the                     input case (snapshot)\nfindings_after  — findings on the augmented output case\n\n\n\n\n\n","title":"BMOPFTools.TransformationManifest"},{"category":"type","location":"augmentation/#BMOPFTools.TransformEntry","page":"Case augmentation","text":"TransformEntry\n\nA single field change made by an augmentation pass.\n\nFields\n\ncomponent_type — :bus, :linecode, :generator, :inverter, or :transformer\ncomponent_id   — the dict key of the modified component\nfield          — the field name written (e.g. \"vpn_min\", \"i_max\")\nold_value      — previous value (nothing if the field was absent)\nnew_value      — value written\nrule           — standards citation (e.g. \"EN50160:2010§3.5\")\nconfidence     — :standard, :high, :medium, or :low\nnote           — human-readable explanation\n\n\n\n\n\n","title":"BMOPFTools.TransformEntry"},{"category":"function","location":"augmentation/#BMOPFTools.manifest_to_dict","page":"Case augmentation","text":"manifest_to_dict(m::TransformationManifest) -> Dict{String,Any}\n\nConvert a manifest to a plain dict suitable for JSON serialisation via write_bmopf or JSON3.write.\n\n\n\n\n\n","title":"BMOPFTools.manifest_to_dict"},{"category":"function","location":"augmentation/#BMOPFTools.render_manifest","page":"Case augmentation","text":"render_manifest(m::TransformationManifest; io=stdout)\n\nPrint a human-readable diff of all changes recorded in the manifest, grouped by component type.\n\n\n\n\n\n","title":"BMOPFTools.render_manifest"},{"category":"function","location":"augmentation/#BMOPFTools.add_generators","page":"Case augmentation","text":"add_generators(net; recipe=default_generator_recipe(), analysis=nothing)\n    -> (net′::Dict{String,Any}, manifest::TransformationManifest)\n\nPlace dispatchable DERs into net using an explainable, semantics-driven recipe. net is never mutated; net′ is an independent deep copy.\n\nWrites only bus, terminal_map, configuration, p_min, p_max, cost on each new generator — reactive bounds are intentionally left to augment_case. Every generator field written is recorded in the returned TransformationManifest.\n\nanalysis may be the output of analyze (or a dict carrying \"voltage_levels\" / \"connectivity\"); if nothing, the needed sub-analyses are run internally.\n\nExample\n\nnet1, _    = fix_case(net)\nnet2, dmf  = add_generators(net1; recipe=GeneratorRecipe(strategy=:load_following))\nnet3, _    = augment_case(net2)   # fills q_min/q_max on the new DERs\nresult     = solve_opf(net3)\n\n\n\n\n\n","title":"BMOPFTools.add_generators"},{"category":"type","location":"augmentation/#BMOPFTools.GeneratorRecipe","page":"Case augmentation","text":"GeneratorRecipe\n\nDeclarative configuration for add_generators. Every field is a deliberate, explainable knob — no randomness.\n\nPlacement strategy\n\nstrategy — :load_following (one DER per load bus), :hosting_capacity (DERs sized to a fraction of the feeding transformer rating), or :topology_targeted (DERs at feeder leaves / near the source).\ntopology_mode — :leaves or :near_source (only for :topology_targeted).\n\nFilters (composable over any strategy)\n\nvoltage_levels — restrict placement to these voltage-level families (:LV, :MV, :HV, :EHV); nothing = all levels.\nmin_local_load_va — skip buses whose aggregated local load is below this.\nskip_source_buses — never place at a voltage-source bus.\n\nSizing\n\nsize_basis — :fraction_of_local_load, :fraction_of_downstream_load, :fraction_of_transformer_rating, or :fixed_tiers.\nder_p_fraction — fraction applied for the :fraction_of_* bases.\nfixed_tier_w — voltage-level-family → fixed DER size (W) for :fixed_tiers.\np_min_fraction — p_min = p_min_fraction × p_max (0 = curtailable).\n\nCost (makes the OPF dispatch non-trivial)\n\ncost_basis — :cheaper_than_slack, :uniform, or :tiered_by_level.\nslack_cost, der_cost_factor — cost = der_cost_factor × slack_cost.\nder_cost_uniform — used when cost_basis = :uniform.\nder_cost_tiers — voltage-level-family → cost for :tiered_by_level.\n\nIdentity / safety\n\nconfiguration — generator configuration; nothing inherits from the load.\nid_prefix — new generator ids are $(id_prefix)$(bus).\noverwrite_existing — if false, buses that already host a generator are skipped (no duplicate / replacement).\napply_placement — master enable; false makes add_generators a no-op copy.\n\n\n\n\n\n","title":"BMOPFTools.GeneratorRecipe"},{"category":"function","location":"augmentation/#BMOPFTools.default_generator_recipe","page":"Case augmentation","text":"default_generator_recipe() -> GeneratorRecipe\n\nThe default DER placement recipe: load-following at LV buses, sized to 80 % of local load, priced at half the slack cost.\n\n\n\n\n\n","title":"BMOPFTools.default_generator_recipe"},{"category":"function","location":"augmentation/#BMOPFTools.add_inverters","page":"Case augmentation","text":"add_inverters(net; recipe=default_inverter_recipe(), analysis=nothing)\n    -> (net′::Dict{String,Any}, manifest::TransformationManifest)\n\nPlace inverter-interfaced DERs into net using an explainable, semantics-driven recipe. net is never mutated; net′ is an independent deep copy.\n\nWrites the inverter nameplate — bus, terminal_map, topology, prime_mover, s_max, p_avail, cost — on each new inverter. The active and reactive dispatch box (p_max/p_min/q_min/q_max) is intentionally left to augment_case's inverter pass, which derives it from s_max/p_avail. Every inverter field written is recorded in the returned TransformationManifest with confidence :synthetic.\n\nanalysis may be the output of analyze (or a dict carrying \"voltage_levels\" / \"connectivity\"); if nothing, the needed sub-analyses are run internally.\n\nThe placed inverters are dispatched by solve_opf once augment_case has filled their P/Q box. (The OPF engine fully supports inverters; only the from_pmd/to_pmd/from_dss I/O converters do not yet map inverter elements.)\n\nExample\n\nnet1, _    = fix_case(net)\nnet2, imf  = add_inverters(net1; recipe=InverterRecipe(strategy=:load_following))\nnet3, _    = augment_case(net2)   # fills p_max/p_min/q_min/q_max on the new inverters\n\n\n\n\n\n","title":"BMOPFTools.add_inverters"},{"category":"type","location":"augmentation/#BMOPFTools.InverterRecipe","page":"Case augmentation","text":"InverterRecipe\n\nDeclarative configuration for add_inverters. Mirrors GeneratorRecipe for the shared placement knobs, with inverter-specific fields for topology, prime mover and apparent-power sizing. Every field is a deliberate, explainable knob — no randomness.\n\nPlacement strategy\n\nstrategy — :load_following (one inverter per load bus), :hosting_capacity (sized to a fraction of the feeding transformer rating), or :topology_targeted (inverters at feeder leaves / near the source).\ntopology_mode — :leaves or :near_source (only for :topology_targeted).\n\nFilters (composable over any strategy)\n\nvoltage_levels — restrict placement to these voltage-level families (:LV, :MV, :HV, :EHV); nothing = all levels.\nmin_local_load_va — skip buses whose aggregated local load is below this.\nskip_source_buses — never place at a voltage-source bus.\n\nSizing (targets apparent power s_max)\n\nsize_basis — :fraction_of_local_load, :fraction_of_downstream_load, :fraction_of_transformer_rating, or :fixed_tiers.\ns_fraction — fraction applied for the :fraction_of_* bases (sets s_max).\nfixed_tier_va — voltage-level-family → fixed s_max (VA) for :fixed_tiers.\ns_to_p_ratio — p_avail = s_to_p_ratio × s_max per phase (1.0 = unity-rated PV; < 1.0 leaves headroom for reactive support at full irradiance).\n\nInverter model\n\nprime_mover — :PV (MVP). Drives p_min = 0 in the augment pass.\ninverter_topology — :infer (FOURLEG when the host load has a neutral terminal, else SINGLEPHASE), or a forced :FOUR_LEG / :THREE_LEG / :SINGLE_PHASE.\n\nCost (makes the OPF dispatch non-trivial)\n\ncost_basis — :cheaper_than_slack, :uniform, or :tiered_by_level.\nslack_cost, der_cost_factor — cost = der_cost_factor × slack_cost.\nder_cost_uniform — used when cost_basis = :uniform.\nder_cost_tiers — voltage-level-family → cost for :tiered_by_level.\n\nIdentity / safety\n\nid_prefix — new inverter ids are $(id_prefix)$(bus).\noverwrite_existing — if false, buses that already host an inverter are skipped (no duplicate / replacement).\napply_placement — master enable; false makes add_inverters a no-op copy.\n\n\n\n\n\n","title":"BMOPFTools.InverterRecipe"},{"category":"function","location":"augmentation/#BMOPFTools.default_inverter_recipe","page":"Case augmentation","text":"default_inverter_recipe() -> InverterRecipe\n\nThe default inverter placement recipe: load-following PV inverters at LV buses, s_max sized to 80 % of local load, unity-rated (p_avail = s_max), priced at half the slack cost.\n\n\n\n\n\n","title":"BMOPFTools.default_inverter_recipe"},{"category":"section","location":"bounds/known_traps/#Known-Traps","page":"Known Traps","text":"A gallery of small instances that exhibit each pathology from Bounds, Branches, and Feasibility. Each entry states what it demonstrates, a minimal construction, the expected behaviour per formulation, and what to look for. A validator can use these as targeted tests: \"give me the input that should expose my bug.\"\n\nnote: For maintainers\nThese constructions are minimal and self-contained; none are yet shipped as named regression cases in the benchmark library. The In BMOPFTools line on each trap points at the closest existing case, test, or tool to build from. When you add a named case, link it from the relevant trap so the gallery and the benchmark library cross-reference.","title":"Known Traps"},{"category":"section","location":"bounds/known_traps/#Trap-1-—-Unbounded-without-bounds","page":"Known Traps","text":"Shows: §1 — a non-coercive objective over a non-compact set has no optimum.\n\nConstruction: any feeder, remove voltage magnitude bounds and generator limits, set a linear objective (e.g. minimize a single weighted injection, or maximize sum V_i).\n\nExpected behaviour:\n\nFormulation Outcome\nLinear unbounded (DUAL_INFEASIBLE)\nSOC / SDP relaxation unbounded\nNonconvex AC unbounded or solver failure (NORM_LIMIT-class)\n\nLook for: after the bound-everything recipe (Diagnostics §1), voltage magnitudes pinned at the artificial bound. Adding underline voverline v alone makes it finite.\n\nIn BMOPFTools: start from an augmented feeder and strip its voltage/generator bounds — the inverse of the case augmentation step.","title":"Trap 1 — Unbounded without bounds"},{"category":"section","location":"bounds/known_traps/#Trap-2-—-Branch-multiplicity-(high-vs-low-voltage)","page":"Known Traps","text":"Shows: §2 — \"square\" is not \"unique.\"\n\nConstruction: the two-bus system of §2 — source E = 10angle 0, lossless reactance X, constant-power load P + jQ chosen so the radicand tfracE^44 - X^2P^2 - XE^2Q is small and positive (loaded but below the nose).\n\nExpected behaviour: two real AC solutions V_+ approx 1 p.u. and V_- ll 1 p.u.; an exact relaxation and a high-voltage-started nonconvex solve both return V_+; a flat-but-low or adversarially started Newton solve can land on V_-.\n\nLook for: different solutions from different starts ⇒ multiplicity. The low-voltage cluster is the tell (Diagnostics §4).\n\nIn BMOPFTools: OPF test T1 (Validating the OPF) solves the high-voltage root V = (V_s + √(V_s² − 4RP))/2 of this same quadratic; a low-R/X reactive variant loaded toward the nose exposes the second root.","title":"Trap 2 — Branch multiplicity (high vs low voltage)"},{"category":"section","location":"bounds/known_traps/#Trap-3-—-Relaxation-feasible-*past*-collapse","page":"Known Traps","text":"Shows: §3 and §4 — a relaxation enlarges the feasible set beyond the true AC boundary, so it can certify feasibility for a loading the network cannot serve.\n\nConstruction: the two-bus, one-generator characterization of Kocuk, Dey & Sun (2016). Pick a load just beyond the nose (radicand  0).\n\nExpected behaviour:\n\nFormulation Outcome\nNonconvex AC infeasible (no real solution past the nose)\nSOC / SDP relaxation may report feasible / \"optimal\" — non-physical\n\nLook for: relaxation OPTIMAL while the AC solve is LOCALLY_INFEASIBLE; large per-branch cone gap (Diagnostics §3). This is the headline reason a feasible relaxation is only a one-sided certificate.\n\nIn BMOPFTools: check the AC side with solve_feasibility_opf — a non-zero total_slack_magnitude_A confirms the loading is past collapse.","title":"Trap 3 — Relaxation feasible past collapse"},{"category":"section","location":"bounds/known_traps/#Trap-4-—-Loss-maximizing-objective-breaks-exactness","page":"Known Traps","text":"Shows: §5 and the loss-maximization list — an objective that rewards current makes the relaxed ell_ij slack.\n\nConstruction: a small radial feeder with DER; objective = maximize PV injection (or maximize export), full bounds, SOC-BFM.\n\nExpected behaviour: the SOC solution reports inflated currents; the cone gap ell_ij - S_ij^2v_i is strictly positive on loaded branches; primal_feasibility_report against the AC model is non-empty. Switching the objective to maximize injection net of losses restores exactness.\n\nLook for: positive cone gaps concentrated where upper voltage bounds bind (Diagnostics §3).\n\nIn BMOPFTools: adapt a radial DER feeder from the DER placement tutorial.","title":"Trap 4 — Loss-maximizing objective breaks exactness"},{"category":"section","location":"bounds/known_traps/#Trap-5-—-Generator-lower-bounds-break-exactness","page":"Known Traps","text":"Shows: the standard escape-hatch conditions in §5 — exactness proofs often assume no generator lower bounds or load over-satisfaction; reintroducing a lower bound can make the relaxation inexact, exact, or feasible-while-AC-infeasible (Kocuk, Dey & Sun, 2016).\n\nConstruction: the same two-bus, one-generator system with a binding underlineP^g  0; sweep the lower bound to move between the three outcomes.\n\nExpected behaviour: a single small instance reproduces all three approximation outcomes as the lower bound varies — a compact regression test for any relaxation implementation.\n\nLook for: the outcome flipping as a single parameter sweeps; tabulate cone gap vs underlineP^g.\n\nIn BMOPFTools: as Trap 3, sweeping the binding underlineP^g  0.","title":"Trap 5 — Generator lower bounds break exactness"},{"category":"section","location":"bounds/known_traps/#Suggested-test-matrix","page":"Known Traps","text":"For a formulation under validation, the minimal coverage is one instance per trap:\n\nTrap Property tested Pass criterion\n1 Well-posedness / bounds unbounded without bounds, finite with them\n2 Branch selection high-voltage solution recovered from a sane start\n3 One-sided certificate AC-infeasible past nose; relaxation gap detected\n4 Objective monotonicity exact under loss-min; inexact (detected) under loss-max\n5 Exactness escape hatches three outcomes reproduced across a bound sweep\n\n\n\nSee also: Bounds, Branches, and Feasibility · Decision matrix · Objectives that imply loss maximization · Diagnostics & validation · References","title":"Suggested test matrix"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Module","page":"API reference","text":"","title":"Module"},{"category":"section","location":"api/#Types","page":"API reference","text":"","title":"Types"},{"category":"section","location":"api/#Finding-accessors","page":"API reference","text":"","title":"Finding accessors"},{"category":"section","location":"api/#IO","page":"API reference","text":"","title":"IO"},{"category":"section","location":"api/#Admittance-export","page":"API reference","text":"","title":"Admittance export"},{"category":"section","location":"api/#Conversion","page":"API reference","text":"","title":"Conversion"},{"category":"section","location":"api/#Top-level-analysis-and-rendering","page":"API reference","text":"","title":"Top-level analysis and rendering"},{"category":"section","location":"api/#Solution-profiling","page":"API reference","text":"","title":"Solution profiling"},{"category":"section","location":"api/#Configuration","page":"API reference","text":"","title":"Configuration"},{"category":"section","location":"api/#Network-simplification","page":"API reference","text":"","title":"Network simplification"},{"category":"section","location":"api/#Analysis-passes","page":"API reference","text":"","title":"Analysis passes"},{"category":"section","location":"api/#Validation-passes","page":"API reference","text":"","title":"Validation passes"},{"category":"section","location":"api/#Case-preparation","page":"API reference","text":"See Case augmentation for the full reference and worked examples.  The main entry points are fix_case / FixRecipe (structural repairs) and augment_case / AugmentationRecipe / default_recipe (standards-grounded gap-filling), together with the shared TransformationManifest / TransformEntry / manifest_to_dict / render_manifest audit trail.","title":"Case preparation"},{"category":"module","location":"api/#BMOPFTools","page":"API reference","text":"BMOPFTools\n\nA Julia library for parsing, validating, analysing, and reporting on BMOPF-format distribution network datasets.\n\nThe network data model is a plain Dict{String,Any} that mirrors the BMOPF JSON schema exactly. No custom wrapper types are used for network data, so data flows naturally to and from JSON without conversion.\n\nPublic API\n\nnet    = parse_bmopf(path)          # load from BMOPF JSON file\nnet    = from_dss(\"Master.dss\")     # parse OpenDSS (via PowerIO.jl)\nreport = analyze(net)               # run all analyses\nrender(report, stdout)              # terminal output\nrender(report, \"report.md\")         # markdown file\n\nSee also: write_bmopf, to_pmd.\n\n\n\n\n\n","title":"BMOPFTools"},{"category":"type","location":"api/#BMOPFTools.Finding","page":"API reference","text":"Finding\n\nA single diagnostic finding produced by any analysis or validation pass.\n\nFields\n\nseverity       — ERROR, WARNING, or INFO\ncode           — stable dot-separated identifier, e.g. \"E.VOLT.LINE_CROSSING\".                    Use these for programmatic filtering; do not match on message.\nsection        — which report section produced this, e.g. :voltage_levels\ncomponent_type — :bus, :line, :load, :transformer, :network, etc.\ncomponent_id   — the dict key of the affected component, or nothing for                    network-level findings\nmessage        — human-readable description\ndetail         — optional machine-readable metadata (actual vs expected                    values, timestamps, etc.) for renderer and downstream use\n\n\n\n\n\n","title":"BMOPFTools.Finding"},{"category":"type","location":"api/#BMOPFTools.SummaryReport","page":"API reference","text":"SummaryReport\n\nAssembled output of analyze. Holds all section result dicts and the complete finding log. Passed to render for output.\n\nresults maps section name → analysis output dict. Renderers walk this structure; the keys are stable across versions.\n\n\n\n\n\n","title":"BMOPFTools.SummaryReport"},{"category":"type","location":"api/#BMOPFTools.SolutionReport","page":"API reference","text":"SolutionReport\n\nOutput of profile_solution. Holds the network name, result metadata, per-check summary dicts, and the complete finding log for the solution profile. Pass to render_solution for Markdown output.\n\n\n\n\n\n","title":"BMOPFTools.SolutionReport"},{"category":"type","location":"api/#BMOPFTools.Severity","page":"API reference","text":"Severity\n\nSeverity level for a Finding.\n\nERROR   — will directly compromise OPF correctness or prevent execution\nWARNING — degrades result quality or indicates suspicious data\nINFO    — informational; worth knowing but not actionable\n\n\n\n\n\n","title":"BMOPFTools.Severity"},{"category":"constant","location":"api/#BMOPFTools.ERROR","page":"API reference","text":"Finding severity: will compromise OPF correctness or prevent execution.\n\n\n\n\n\n","title":"BMOPFTools.ERROR"},{"category":"constant","location":"api/#BMOPFTools.WARNING","page":"API reference","text":"Finding severity: degrades result quality or indicates suspicious data.\n\n\n\n\n\n","title":"BMOPFTools.WARNING"},{"category":"constant","location":"api/#BMOPFTools.INFO","page":"API reference","text":"Finding severity: informational — worth knowing, not necessarily actionable.\n\n\n\n\n\n","title":"BMOPFTools.INFO"},{"category":"function","location":"api/#BMOPFTools.errors","page":"API reference","text":"errors(findings::Vector{Finding}) -> Vector{Finding}\nerrors(report::SummaryReport) -> Vector{Finding}\n\nThe subset of findings with ERROR severity. See also warnings, infos.\n\n\n\n\n\n","title":"BMOPFTools.errors"},{"category":"function","location":"api/#BMOPFTools.warnings","page":"API reference","text":"warnings(findings::Vector{Finding}) -> Vector{Finding}\nwarnings(report::SummaryReport) -> Vector{Finding}\n\nThe subset of findings with WARNING severity. See also errors, infos.\n\n\n\n\n\n","title":"BMOPFTools.warnings"},{"category":"function","location":"api/#BMOPFTools.infos","page":"API reference","text":"infos(findings::Vector{Finding}) -> Vector{Finding}\ninfos(report::SummaryReport) -> Vector{Finding}\n\nThe subset of findings with INFO severity. See also errors, warnings.\n\n\n\n\n\n","title":"BMOPFTools.infos"},{"category":"function","location":"api/#BMOPFTools.parse_bmopf","page":"API reference","text":"parse_bmopf(path::AbstractString; terminal_aliases) -> Dict{String,Any}\nparse_bmopf(io::IO; terminal_aliases) -> Dict{String,Any}\nparse_bmopf(json::AbstractString; from_string=true, terminal_aliases) -> Dict{String,Any}\n\nParse a BMOPF JSON file (or IO stream, or raw JSON string) into a plain Dict{String,Any} that mirrors the schema structure exactly.\n\nThe returned dict is mutable — analysis functions treat it as read-only but callers may modify it freely.\n\nEnums from PowerModelsDistribution (e.g. WYE, DELTA) are stored as plain strings in BMOPF JSON and remain strings after parsing.  The to_pmd conversion layer handles the string → Enum translation.\n\nTerminal normalization\n\nThe data model requires terminal identifiers to be strings. Non-string entries (e.g. integer terminals [1,2,3,4]) are coerced at ingest:\n\nif every numeric token is covered by terminal_aliases (default 1→\"1\", 2→\"2\", 3→\"3\", 4→\"n\"), the alias map is applied;\notherwise — the profiling guard — every token becomes its verbatim decimal string and the neutral-identification heuristic decides semantics downstream.\n\nCoercion is recorded under _meta[\"terminal_coercions\"] and surfaced as a W.SPEC.TERMINAL_TYPES conformance finding. Pass a custom terminal_aliases::Dict to override the convention.\n\nRaises\n\nArgumentError if the file cannot be found or is not valid JSON.\nDoes not validate against the BMOPF JSON Schema here; call schema_check and spec_conformance_check for that.\n\n\n\n\n\n","title":"BMOPFTools.parse_bmopf"},{"category":"function","location":"api/#BMOPFTools.write_bmopf","page":"API reference","text":"write_bmopf(net::Dict{String,Any}, dest; meta=nothing)\n\nSerialise a BMOPF network dict to JSON.\n\ndest::IO             — writes to the IO stream\ndest::AbstractString — writes to a file at that path\n\nA meta block is always written. Fields are assembled in this priority order (highest wins): the meta keyword argument → net[\"meta\"] → auto-generated defaults ($schema, generator, created). Caller-supplied values are never overwritten by auto-generation.\n\nKeyword argument\n\nmeta: a Dict of fields to include or override in the written meta block. All fields are optional; common ones are title, description, license, authors, sources, and version. See docs/src/conventions.md for the full field reference.\n\nExample\n\nwrite_bmopf(net, \"output.json\";\n    meta = Dict(\n        \"title\"   => \"LV network 1, Feeder 1\",\n        \"license\" => \"https://creativecommons.org/licenses/by/4.0/\",\n        \"authors\" => [Dict(\"name\" => \"Frederik Geth\", \"orcid\" => \"0000-0001-9534-2265\")],\n        \"sources\" => [Dict(\"name\" => \"ENWL dataset\", \"format\" => \"OpenDSS\",\n                           \"url\"  => \"https://www.enwl.co.uk/…\")]\n    ))\n\n\n\n\n\n","title":"BMOPFTools.write_bmopf"},{"category":"function","location":"api/#BMOPFTools.migrate","page":"API reference","text":"migrate(net::Dict{String,Any}) -> Dict{String,Any}\n\nForward-migrate a BMOPF network dict to the current spec version.\n\nIf the dict already targets the current spec (or has no recognisable $schema URI) it is returned unchanged. Otherwise each intermediate migration step is applied in sequence and a W.MIGRATE.UPGRADED finding is appended to net[\"_meta\"][\"migration_notes\"] so the transformation is auditable.\n\nCalled automatically by parse_bmopf; can also be called directly on an already-parsed dict.\n\n\n\n\n\n","title":"BMOPFTools.migrate"},{"category":"function","location":"api/#BMOPFTools.is_timeseries","page":"API reference","text":"is_timeseries(net::Dict{String,Any}) -> Bool\n\nReturn true if the network dict contains a root-level \"time_series\" collection with at least one entry, and at least one component references it via a \"time_series\" sub-dict.\n\nThis follows PMD's convention: a network with a \"time_series\" key but no component references is treated as a snapshot network.\n\n\n\n\n\n","title":"BMOPFTools.is_timeseries"},{"category":"function","location":"api/#BMOPFTools.get_snapshot","page":"API reference","text":"get_snapshot(net::Dict{String,Any}, t_index::Int) -> Dict{String,Any}\n\nMaterialise a snapshot of a time-series network at time step t_index (1-based).\n\nReturns a deep copy of net with all \"time_series\" parameter references resolved to concrete scalar/vector values and all component-level \"time_series\" sub-dicts removed. The root-level \"time_series\" key is also removed from the result.\n\nFor a network without time series, returns a deep copy unchanged.\n\nPMD convention: time series values are scaling factors applied multiplicatively to the static parameter value:\n\nresolved_value = static_value * scale_factor[t_index]\n\nRaises\n\nBoundsError if t_index is out of range for any referenced series.\n\n\n\n\n\n","title":"BMOPFTools.get_snapshot"},{"category":"function","location":"api/#BMOPFTools.sideload_coordinates!","page":"API reference","text":"sideload_coordinates!(net, csv_path) -> (n_matched, n_skipped)\n\nRead a OpenDSS-style bus coordinate CSV (bus_id,x,y, no header) and add \"longitude\" and \"latitude\" fields to matching bus objects in net.\n\nReturns the number of buses matched and the number of CSV rows skipped because the bus ID was not present in net (open-switch stub buses and synthetic slack buses are the usual cause).\n\nThe CSV x column is treated as longitude and y as latitude — the convention used by OpenDSS Buscoords files.  No coordinate-system transformation is performed; values are stored as-is.\n\n\n\n\n\n","title":"BMOPFTools.sideload_coordinates!"},{"category":"function","location":"api/#BMOPFTools.transformer_yprim","page":"API reference","text":"transformer_yprim(xfmr, subtype) -> (nodes, Y)\n\nReturn the nodal admittance block for a single transformer data dict.\n\nnodes  — Vector{Tuple{String,String}} of (bus_id, terminal_name) pairs,            in the same order as the rows/columns of Y.\nY      — Matrix{ComplexF64}, SI siemens. Symmetric: Y ≈ transpose(Y).\n\nsubtype must be one of \"single_phase\", \"center_tap\", \"wye_delta\", \"delta_wye\".\n\nThe regulator subtypes \"single_phase_autotransformer\" and \"open_delta_regulator\" use the fixed-tap effective ratio n_eff (BMOPFTools._autotransformer_ratio) in place of the nameplate turns ratio.\n\nRaises an ArgumentError for unknown subtypes. Returns ([], zeros(0,0)) when the transformer is degenerate (zero v_ref_to, missing terminal maps).\n\n\n\n\n\n","title":"BMOPFTools.transformer_yprim"},{"category":"function","location":"api/#BMOPFTools.export_yprim","page":"API reference","text":"export_yprim(net) -> Dict\n\nBuild the Yprim block for every transformer in the network and return a nested Dict:\n\nDict(\n  subtype => Dict(\n    id => Dict(\"nodes\" => [...], \"Y_real\" => [...], \"Y_imag\" => [...])\n  )\n)\n\nEach \"nodes\" entry is a Vector of [bus_id, terminal_name] pairs. \"Y_real\" and \"Y_imag\" are row-major dense matrices (Vector of Vector).\n\n\n\n\n\n","title":"BMOPFTools.export_yprim"},{"category":"function","location":"api/#BMOPFTools.write_yprim","page":"API reference","text":"write_yprim(net, path)\n\nWrite the Yprim export for all transformers in net to a JSON file at path.\n\n\n\n\n\n","title":"BMOPFTools.write_yprim"},{"category":"function","location":"api/#BMOPFTools.to_pmd","page":"API reference","text":"to_pmd(net::Dict{String,Any}) -> Dict{String,Any}\n\nConvert a BMOPF network dict to a PowerModelsDistribution ENGINEERING model dictionary, suitable for passing directly to PMD's solve_mc_* functions or transform_data_model.\n\nThe output includes the \"data_model\" => ENGINEERING marker that PMD expects.\n\nKey reverse mappings\n\nBMOPF terminal_names string arrays → PMD terminals int arrays\nBMOPF terminal_map_* string arrays → PMD f_connections/t_connections int arrays\nBMOPF voltage values in V → PMD in p.u. (requires settings voltage base)\nBMOPF power values in W/var → PMD scaled by power_scale_factor\nBMOPF v_angle in radians → PMD va in degrees\nBMOPF open_switch bool → PMD state (0=OPEN, 1=CLOSED)\nBMOPF pattern keys (Rseries11, G1_1, ...) → PMD matrices\n\nNotes\n\nA minimal settings dict is always included in the output.\nIf the BMOPF net contains _pmd sub-dicts (carried through from a PMD ENGINEERING source), those fields are merged back into the PMD component.\n\n\n\n\n\n","title":"BMOPFTools.to_pmd"},{"category":"function","location":"api/#BMOPFTools.from_dss","page":"API reference","text":"from_dss(path::AbstractString; name=nothing) -> Dict{String,Any}\n\nParse an OpenDSS Master file directly to a BMOPF network dict using PowerIO.jl.\n\nThis is the recommended path for reading OpenDSS networks: the Rust parser (bound in-process by PowerIO.jl) materialises every OpenDSS class default explicitly, validates fidelity against the OpenDSS solver, and produces schema-valid BMOPF JSON without going through PowerModelsDistribution.\n\nOpenDSS identifiers are case-insensitive but case-preserving, whereas BMOPF keys are matched exactly. To reconcile the two, every identifier and every reference to one (bus names, linecodes, component ids) is case-folded to lower case on ingest, so references resolve regardless of the casing each OpenDSS statement happened to use.\n\nOpenDSS numeric terminal names (\"1\", \"2\", \"3\", \"4\") are remapped to the task-force convention (\"a\", \"b\", \"c\", \"n\") and neutral_terminal is set to \"n\" on every affected bus.\n\nArguments\n\npath: path to the OpenDSS Master.dss file (or any .dss entry point)\nname: optional network name string set on net[\"name\"] after parsing. Defaults to the relative path of the Master file from the working directory.\n\nConversion warnings\n\nPowerIO reports every piece of information that cannot be represented in BMOPF JSON (e.g. shunt admittance, load shape time series, RegControl OLTC taps). These are surfaced on _meta[\"powerio_warnings\"] in the returned dict, so callers can inspect them without losing the converted data.\n\nErrors\n\nArgumentError if the DSS file does not exist.\nErrorException if PowerIO produces no output (parse failure or schema error).\n\nExample\n\nnet = from_dss(\"test/data/ENWL/network_1/Feeder_1/Master.dss\")\nreport = analyze(net)\nrender(report, stdout)\n\n\n\n\n\n","title":"BMOPFTools.from_dss"},{"category":"function","location":"api/#BMOPFTools.analyze","page":"API reference","text":"analyze(net::Dict{String,Any}; t_index::Int=1) -> SummaryReport\n\nRun all analysis and validation passes on a BMOPF network dict and return a SummaryReport.\n\nFor snapshot networks, t_index is ignored. For networks with a \"time_series\" key, the snapshot at t_index is materialised first.\n\n\n\n\n\nanalyze(path::AbstractString; kwargs...) -> SummaryReport\n\nParse a BMOPF JSON file and run analyze.\n\n\n\n\n\n","title":"BMOPFTools.analyze"},{"category":"function","location":"api/#BMOPFTools.render","page":"API reference","text":"render(report::SummaryReport, dest; kwargs...)\n\nRender a SummaryReport to dest.\n\ndest::IO            — writes terminal-formatted text (ANSI colour if tty)\ndest::AbstractString — writes to file; format inferred from extension (.md → Markdown, anything else → plain text)\n\nKeyword arguments\n\ncolor::Bool — force-enable/disable ANSI colour for IO dest (default: auto)\nverbose::Bool — include INFO-level findings (default: true)\n\n\n\n\n\n","title":"BMOPFTools.render"},{"category":"function","location":"api/#BMOPFTools.render_terminal","page":"API reference","text":"render_terminal(report, io; color, verbose)\n\nWrite a human-readable text report to io.\n\n\n\n\n\n","title":"BMOPFTools.render_terminal"},{"category":"function","location":"api/#BMOPFTools.render_markdown","page":"API reference","text":"render_markdown(report, io; verbose)\n\nWrite a Markdown-formatted report to io.\n\n\n\n\n\n","title":"BMOPFTools.render_markdown"},{"category":"function","location":"api/#BMOPFTools.render_ascii_tree","page":"API reference","text":"render_ascii_tree(net, io; max_buses, max_depth, fold_chains, legend_limit,\n                           split_by_level)\n\nWrite an ASCII tree of the network graph to io.\n\nNode labels – the source bus is tagged SRC; loads appear as L1,L2,… and generators as G1,G2,… (numbered per section, mapped in the legend printed after each section). Transformer and switch edges are annotated.\n\nLarge-network handling – when a section's bus count exceeds max_buses:\n\nfold_chains=true (default) collapses unbranched corridors with no attached loads or generators into ⋮ (N buses) stubs.\nSubtrees deeper than max_depth are replaced by a [+N buses, M loads] line.\n\nMulti-voltage split – when split_by_level=true (default) and the spanning tree crosses ≥ 2 voltage level boundaries, the file is split into a root section (MV backbone) and one LV section per distribution transformer.\n\n\n\n\n\n","title":"BMOPFTools.render_ascii_tree"},{"category":"function","location":"api/#BMOPFTools.profile_solution","page":"API reference","text":"profile_solution(net, result; t_index::Int=1) -> SolutionReport\n\nProfile an OPF result dict against the BMOPF network that produced it. Checks bound satisfaction (voltage, thermal, generator dispatch), constraint residuals, power balance, and produces informational summaries (loss fraction, neutral shift).\n\nnet may be a snapshot or time-series network dict; t_index selects the snapshot when time-series data is present. result is the dict returned by solve_opf or any compatible solver.\n\nReturns a SolutionReport which can be rendered to Markdown with render_solution.\n\n\n\n\n\n","title":"BMOPFTools.profile_solution"},{"category":"function","location":"api/#BMOPFTools.render_solution","page":"API reference","text":"render_solution(report::SolutionReport, dest; verbose::Bool=true)\n\nRender a SolutionReport to dest.\n\ndest::IO             — writes Markdown text\ndest::AbstractString — writes to file (.md extension recommended)\n\n\n\n\n\n","title":"BMOPFTools.render_solution"},{"category":"function","location":"api/#BMOPFTools.solution_check","page":"API reference","text":"solution_check(net, result, findings) -> Dict{String,Any}\n\nProfile an OPF result dict against the network that produced it. Appends Finding objects to findings and returns a summary dict.\n\nnet must be a snapshot (non-time-series) BMOPF network dict. result is the dict returned by solve_opf (or any compatible solver).\n\n\n\n\n\n","title":"BMOPFTools.solution_check"},{"category":"function","location":"api/#BMOPFTools.voltage_zone_summary","page":"API reference","text":"voltage_zone_summary(net, result) -> Dict{String,Any}\n\nAggregate the solved bus voltages into a per-galvanic-zone band summary for at-a-glance inspection. Galvanic zones (the connected components left after cutting transformer windings) are the natural grouping because every bus in a zone shares one voltage base, so the per-unit magnitudes are directly comparable; volts across a transformer boundary are not.\n\nEach zone is reduced over its phase terminals (the neutral is excluded from the magnitude band but tracked separately as a neutral shift):\n\nv_base        — phase-to-neutral voltage base (V). Taken from the zone's voltage source if it contains one, else the midpoint of the buses' v_min/ v_max, else the median solved phase magnitude.\nvm_min_pu / vm_max_pu — min/max phase magnitude in the zone, per unit.\nvm_min_bus / vm_max_bus — the buses carrying those extremes.\nmax_imbalance_pct — worst per-bus phase-magnitude spread (max−min)/v_base.\nmax_imbalance_bus — bus carrying that worst imbalance.\nmax_neutral_shift_v / max_neutral_shift_bus — worst neutral terminal voltage in the zone.\nstatus — \"ok\", \"active\" (within 1 % of a bound), or \"violation\", derived from the buses' own v_min/v_max using the same threshold as the bound checks.\nbus_rows — per-bus drill-down records (bus, vm_min_v/vm_max_v, vm_min_pu/vm_max_pu, imbalance_pct, neutral_shift_v, deviation_pu, status), sorted worst-deviation-from-1.0-pu first. Rendered only in verbose output.\n\nReturns Dict(\"zones\" => Vector{Dict}, \"n_zones\" => Int), zones sorted by label. Returns empty zones when there is no bus result.\n\n\n\n\n\n","title":"BMOPFTools.voltage_zone_summary"},{"category":"function","location":"api/#BMOPFTools.load_config","page":"API reference","text":"load_config(path=nothing) -> Dict{String,Any}\n\nLoad a BMOPFTools threshold configuration.\n\nWith no argument, returns a fresh copy of the shipped defaults. With a path to a user TOML file, the file is parsed and deep-merged over the defaults, so the file need only specify the keys it wants to change.\n\nThe returned dict is sectioned exactly like config/default.toml ([\"domain_rules\"], [\"thermal\"], [\"provenance\"][\"grounding\"], …) and is suitable to pass to any entry point's config= keyword:\n\ncfg = load_config(\"my_lv_feeder.toml\")\nreport = analyze(net; config=cfg)\n\n\n\n\n\n","title":"BMOPFTools.load_config"},{"category":"function","location":"api/#BMOPFTools.simplify_network","page":"API reference","text":"simplify_network(net;\n                 open_switches   = true,\n                 closed_switches = true,\n                 dangling_lines  = true,\n                 series_lines    = true) -> net′\n\nApply selected topology simplifications in order:\n\ncollapse_closed_switches — merge bus pairs joined by zero-impedance closed switches\nremove_open_switches     — delete open switch elements\nremove_dangling_lines    — remove stub lines and their leaf buses\nmerge_series_lines       — fuse consecutive same-linecode lines at pass-through buses\n\nEach operation can be disabled individually via keyword arguments. Returns a deep copy of net with all selected operations applied; outcomes accumulate in net′[\"_simplification_log\"].\n\n\n\n\n\n","title":"BMOPFTools.simplify_network"},{"category":"function","location":"api/#BMOPFTools.merge_series_lines","page":"API reference","text":"merge_series_lines(net) -> net′\n\nReturn a deep-copied network with consecutive same-linecode lines fused at pass-through buses (buses with exactly two line connections and no other elements). Iterates to convergence so chains of three or more lines are fully collapsed in one call.\n\nA pass-through bus is blocked — and a log entry emitted — when:\n\nthe intermediate bus carries any non-line element (load, shunt, etc.): code NON_LINE_ON_BUS (info)\na switch is the blocking element: code SWITCH_IN_CHAIN (warning; likely user error — a switch placeholder left in the middle of a cable run)\nadjacent linecodes differ: code LINECODE_MISMATCH (info)\nterminal maps at the shared bus are incompatible: code TERMINAL_MISMATCH (warning)\n\nSuccessful merges record _merged_from on the surviving line and emit LINES_MERGED (info).\n\nAll outcomes are appended to net′[\"_simplification_log\"].\n\n\n\n\n\n","title":"BMOPFTools.merge_series_lines"},{"category":"function","location":"api/#BMOPFTools.remove_dangling_lines","page":"API reference","text":"remove_dangling_lines(net) -> net′\n\nReturn a deep-copied network with all stub lines removed. A stub is a line whose far-end bus has exactly one line connection and no active elements (loads, generators, shunts, voltage sources, transformers, switches). The far-end bus is removed along with the line. Iterates to convergence so dangling chains are fully pruned.\n\nSuccessful removals emit LINE_REMOVED (info). All outcomes appended to net′[\"_simplification_log\"].\n\n\n\n\n\n","title":"BMOPFTools.remove_dangling_lines"},{"category":"function","location":"api/#BMOPFTools.remove_open_switches","page":"API reference","text":"remove_open_switches(net) -> net′\n\nReturn a deep-copied network with all open switch elements (open_switch=true) deleted. The buses they connected are retained. Emits SWITCH_REMOVED (info) for each deleted switch and ISOLATED_BUS (warning) for any bus that has no remaining connections after the removal.\n\nAll outcomes appended to net′[\"_simplification_log\"].\n\n\n\n\n\n","title":"BMOPFTools.remove_open_switches"},{"category":"function","location":"api/#BMOPFTools.collapse_closed_switches","page":"API reference","text":"collapse_closed_switches(net) -> net′\n\nReturn a deep-copied network with closed switches (open_switch=false) removed by merging the two buses they connect. The bus_from bus always survives; the bus_to bus is absorbed and every element that referenced it is redirected. Terminal names and grounding sets are unioned; voltage bounds are tightened to their intersection. Iterates to convergence.\n\nCollapse is blocked — and a warning logged — when:\n\nboth buses carry a voltage source: code MERGE_CONFLICT_SOURCE\nthe switch's terminal-map arities differ: code MERGE_CONFLICT_TERMINALS\nthe switch is a self-loop: code MERGE_CONFLICT_TERMINALS\n\nSuccessful collapses emit SWITCH_COLLAPSED (info). All outcomes appended to net′[\"_simplification_log\"].\n\n\n\n\n\n","title":"BMOPFTools.collapse_closed_switches"},{"category":"function","location":"api/#BMOPFTools.inventory_analysis","page":"API reference","text":"inventory_analysis(net, findings) -> Dict{String,Any}\n\nCount components and compute summary statistics for each component type. Returns a dict keyed by component type name; each value is a sub-dict with at least a \"total\" count and type-specific breakdowns.\n\n\n\n\n\n","title":"BMOPFTools.inventory_analysis"},{"category":"function","location":"api/#BMOPFTools.voltage_level_analysis","page":"API reference","text":"voltage_level_analysis(net, findings) -> Dict{String,Any}\n\nPropagate nominal voltage through the network graph via BFS from each voltage source, assigning a voltage level to every bus. Transformers are level-change edges; all other edges (lines, switches) must connect buses at the same level.\n\nReturns identified voltage levels, bus assignments, and any consistency violations.\n\n\n\n\n\n","title":"BMOPFTools.voltage_level_analysis"},{"category":"function","location":"api/#BMOPFTools.connectivity_analysis","page":"API reference","text":"connectivity_analysis(net, findings) -> Dict{String,Any}\n\nBuild an undirected graph of the network and compute:\n\nNumber of connected components\nRadial vs meshed topology (cycle detection)\nDegree statistics\nTree depth and longest path from voltage source\nOpen-switch isolated sections\n\n\n\n\n\n","title":"BMOPFTools.connectivity_analysis"},{"category":"function","location":"api/#BMOPFTools.diversity_analysis","page":"API reference","text":"diversity_analysis(net, findings) -> Dict{String,Any}\n\nAssess parameter diversity across component categories. Flags datasets with high symmetry (low coefficient of variation, duplicate tuples) which may indicate copy-paste or templated parameterisation.\n\n\n\n\n\n","title":"BMOPFTools.diversity_analysis"},{"category":"function","location":"api/#BMOPFTools.operational_analysis","page":"API reference","text":"operational_analysis(net, findings) -> Dict{String,Any}\n\nCompute operational loading statistics:\n\nTotal load and generation\nLoad by voltage level and configuration\nTransformer utilisation at nominal setpoints\nLine thermal headroom\nGeneration/load ratio\n\n\n\n\n\n","title":"BMOPFTools.operational_analysis"},{"category":"function","location":"api/#BMOPFTools.load_model_analysis","page":"API reference","text":"load_model_analysis(net, findings) -> Dict{String,Any}\n\nSummarise load voltage-dependence and emit OPF-formulation flags:\n\nI.LOAD.EXP_ZIP_EQUIVALENT — exponential loads with only integer exponents {0,1,2}; these can be represented as ZIP, keeping the OPF quadratic.\nW.LOAD.NL_NO_VMIN — voltage-dependent loads on buses without any lower voltage magnitude bound; the OPF squared-voltage variable then relies on a default floor rather than an engineering bound.\n\n\n\n\n\n","title":"BMOPFTools.load_model_analysis"},{"category":"function","location":"api/#BMOPFTools.provenance_analysis","page":"API reference","text":"provenance_analysis(net, findings) -> Dict{String,Any}\n\nDetect signatures of how the dataset was produced, and make implicit modeling assumptions explicit:\n\nLinecode impedance classification — per linecode, classify the phase block of the series impedance matrix:\ndecoupled        — diagonal matrix: positive-sequence-only data; the                      three phases are mathematically independent.\nexactly_balanced — equal self and equal mutual entries: constructed                      from sequence parameters (r1,x1,r0,x0) or under a                      transposition assumption. The implied Z1/Z0 are                      recovered and reported.\nnear_balanced    — balanced within 1%: possibly physical (twisted /                      bundled symmetric cable construction).\ndistinct         — consistent with first-principles geometry (Carson).\nAlso checks reciprocity (Z symmetric) and passivity (R block PSD).\nWires per voltage level — 3-wire vs 4-wire classification. A 3-wire LV level is flagged as likely Kron-reduced (LV is physically 4-wire); 3-wire MV is normal. A 4-wire network whose neutrals are all perfectly grounded is exactly Kron-reducible (redundant variables).\nNeutral grounding — builds the neutral-conductor continuity graph and verifies every neutral section reaches a grounding (perfect grounding, grounding shunt, or a source that references the neutral). Floating neutral sections leave the zero-sequence path undefined.\n\nThe summary string under \"convention\" states the inferred modeling convention explicitly; renderers print it in the report.\n\n\n\n\n\n","title":"BMOPFTools.provenance_analysis"},{"category":"function","location":"api/#BMOPFTools.infeasibility_preflight","page":"API reference","text":"infeasibility_preflight(net, findings) -> Dict{String,Any}\n\nPre-flight checks that predict infeasibility risk before running the OPF. Does not run power flow — all checks are static data inspection.\n\nCovers:\n\nGeneration adequacy (local capacity vs load)\nVoltage bound tightness\nConstraint conflict detection (vmin > vmax, pmin > pmax)\nTopological single-point-of-failure risk\n\n\n\n\n\n","title":"BMOPFTools.infeasibility_preflight"},{"category":"function","location":"api/#BMOPFTools.schema_check","page":"API reference","text":"schema_check(net, findings) -> Dict{String,Any}\n\nTwo-layer schema validation:\n\nLayer 1 — JSONSchema.jl validation against the bundled JSON Schema for the spec version declared in meta.$schema. Catches type errors, missing required fields, enum violations (configuration must be \"WYE\", \"DELTA\", or \"SINGLE_PHASE\"), and nonnegative_number range constraints. Errors are reported as ERROR findings; additional (unknown) properties as INFO.\n\nLayer 2 — hand-rolled unknown-field catalogue. Always runs; produces the unknown_fields_by_type result dict used by the dataset-assessment report section regardless of whether layer 1 ran.\n\nLayer 1 is skipped (with an INFO finding) when the spec version in meta.$schema is not recognised — the hand-rolled catalogue still runs as a fallback.\n\n\n\n\n\n","title":"BMOPFTools.schema_check"},{"category":"function","location":"api/#BMOPFTools.completeness_check","page":"API reference","text":"completeness_check(net, findings) -> Dict{String,Any}\n\nTwo-layer completeness check:\n\nHard: are all schema-required fields present?\nSoft: which optional fields are absent across the dataset?\n\n\n\n\n\n","title":"BMOPFTools.completeness_check"},{"category":"function","location":"api/#BMOPFTools.domain_rules_check","page":"API reference","text":"domain_rules_check(net, findings) -> Dict{String,Any}\n\nCheck numerical plausibility of field values. All thresholds are configurable via the thresholds keyword (see _DEFAULT_THRESHOLDS).\n\n\n\n\n\n","title":"BMOPFTools.domain_rules_check"},{"category":"function","location":"api/#BMOPFTools.redundancy_check","page":"API reference","text":"redundancy_check(net, findings) -> Dict{String,Any}\n\nIdentify redundant or trivial elements that do not contribute to the OPF problem and may indicate data quality issues.\n\n\n\n\n\n","title":"BMOPFTools.redundancy_check"},{"category":"function","location":"api/#BMOPFTools.integrity_check","page":"API reference","text":"integrity_check(net, findings) -> Dict{String,Any}\n\nStructural integrity checks:\n\nReferential integrity: every bus/bus_from/bus_to exists; every line's linecode exists; every terminal-map entry is a terminal of the referenced bus (this also enforces that nodal elements never connect directly to ground).\nDimension consistency: line terminal-map arity vs linecode matrix size; i_max vector length vs conductor count; load setpoint length vs configuration; source v_magnitude/v_angle lengths vs terminal map.\nPadded matrices: all-zero row/column pairs in linecode impedances.\nGalvanic islands: transformer windings are galvanic separations; each island needs a voltage reference (source, perfect grounding, or a grounding shunt) or its voltages are defined only up to a shift.\nWye without neutral: wye-configured loads/generators at buses with no identifiable neutral imply an undeclared ground return.\nLow-impedance lines: lines far below the network's typical series impedance — model them as (lossless) switches instead.\nGenerator cost symmetry: identical cost vectors create dispatch degeneracy (any split between them is optimal).\n\n\n\n\n\n","title":"BMOPFTools.integrity_check"},{"category":"function","location":"api/#BMOPFTools.spec_conformance_check","page":"API reference","text":"spec_conformance_check(net, findings) -> Dict{String,Any}\n\nCheck conformance with the TF data model beyond what the JSON Schema can express:\n\nexactly one voltage source (spec Eq. 17);\nload/generator configuration strings and configuration ↔ terminal-map arity consistency (SINGLE_PHASE = 2, WYE = 4, DELTA = 3 terminals);\ntransformer terminal-map arities per subtype;\nlinecode impedance matrices stored with all n² entries (spec §4.1.1 row-first format) rather than upper-triangular shorthand.\n\n\n\n\n\n","title":"BMOPFTools.spec_conformance_check"},{"category":"function","location":"api/#BMOPFTools.benchmark_readiness_check","page":"API reference","text":"benchmark_readiness_check(net, findings) -> Dict{String,Any}\n\nAssess whether the case defines a non-trivial, well-posed OPF benchmark per the TF spec (generation-cost objective, Eq. 135), and list the augmentation steps needed to make it one. Also flags degeneracy risks that could cause primal non-uniqueness or flat objective directions.\n\n\n\n\n\n","title":"BMOPFTools.benchmark_readiness_check"},{"category":"section","location":"conventions/#Data-model-conventions","page":"Data model conventions","text":"This page documents the BMOPF data-model conventions as implemented by BMOPFTools, following the Task Force specification (ref. 1) with the implementation choices called out explicitly.","title":"Data model conventions"},{"category":"section","location":"conventions/#Structure-and-identifiers","page":"Data model conventions","text":"The network is a nested Dict{String,Any}: component types at the top level (bus, line, linecode, voltage_source, load, generator, shunt, switch, transformer), each a dict of id => component. IDs are strings and carry no ordering semantics. Keys beginning with _ (_meta, _pmd, _slack, …) are tolerated extension/bookkeeping fields and are never reported as schema violations.","title":"Structure and identifiers"},{"category":"section","location":"conventions/#Units","page":"Data model conventions","text":"All physical quantities are SI (spec Table 8):\n\nQuantity Unit\nvoltage V\ncurrent A\nlength m\nactive / reactive / apparent power W / var / VA\nresistance / reactance Ω (lines: Ω/m)\nconductance / susceptance S (lines: S/m)\nangle rad\ngeneration cost $/kWh\n\nNote that from_dss (via PowerIO) emits SI values directly — line lengths in metres, linecodes per-metre, voltages in volts and powers in watts/vars — so no further scaling is applied on ingest.","title":"Units"},{"category":"section","location":"conventions/#Terminal-names","page":"Data model conventions","text":"Terminal identifiers are strings. The library writes the OpenDSS-flavoured convention \"1\", \"2\", \"3\", \"n\" and understands these conventions on read:\n\nConvention Phases Neutral\nOpenDSS numeric \"1\" \"2\" \"3\" \"4\" (positional, see below)\nBMOPFTools canonical \"1\" \"2\" \"3\" \"n\"\nLetter \"a\" \"b\" \"c\" (any case) \"n\"/\"N\"\nIEC 60445 \"L1\" \"L2\" \"L3\" \"N\"\n\nNeutral identification is heuristic (the spec carries no explicit marker): a terminal named n/N is the neutral; failing that, terminal \"4\" of a bus whose terminal set is exactly {\"1\",\"2\",\"3\",\"4\"}. Anything else is treated as all-phase.\n\nIngest normalisation: JSON files with non-string terminal entries (e.g. [1,2,3,4]) are coerced by parse_bmopf. If every numeric token is covered by the alias table (default 1→\"1\", 2→\"2\", 3→\"3\", 4→\"n\", overridable via terminal_aliases), the aliases apply; otherwise — the profiling guard, e.g. a 5th conductor present — everything becomes its verbatim decimal string. Coercion is recorded in _meta[\"terminal_coercions\"] and flagged as W.SPEC.TERMINAL_TYPES.","title":"Terminal names"},{"category":"section","location":"conventions/#Buses,-bounds-and-grounding","page":"Data model conventions","text":"A bus carries terminal_names, optionally perfectly_grounded_terminals, and optional voltage-magnitude bounds in four flavours: phase-to-ground (v_min/v_max), phase-to-neutral (vpn_*), phase-to-phase (vpp_*) and sequence (vpos_*). v_min/v_max and vpn_min/vpn_max are per-phase arrays (one element per phase terminal, in terminal_names order, neutral excluded); vpp_min/vpp_max are per-pair arrays (one element per unordered phase pair, in (i<j) order). A scalar v_min/v_max is rejected at ingest (parse_bmopf) — wrap it in an array of the phase count. The neutral conductor has its own separate, optional, maximum-only bound vn_max (scalar, neutral-to-ground), valid only when the bus has a neutral. Absent bounds mean unconstrained (spec §4.1.5).\n\nGrounding semantics (spec Table 10):\n\nperfect grounding is a bus property (perfectly_grounded_terminals);\ngrounding through an impedance is a shunt whose terminal map references the neutral (e.g. Y = [G_1_1 + jB_1_1], N = [\"n\"]);\nloads, generators, transformers and switches never connect directly to ground — only to bus terminals;\nvoltage sources are defined voltage-to-ground and pin every terminal in their map (a source whose map includes the neutral references it to ground).","title":"Buses, bounds and grounding"},{"category":"section","location":"conventions/#Lines,-linecodes-and-matrices","page":"Data model conventions","text":"Lines carry only topology (bus_from/bus_to, terminal maps, length, linecode); all impedance lives in the linecode as per-length matrices in flattened row-first pattern keys: R_series_1_2 ⇒ rs[1,2] (Ω/m), likewise X_series_*, and optional G_from_*/G_to_*/B_from_*/B_to_* (S/m) for the two shunt half-sections of the Π model. Optional ratings: i_max (A, per conductor) and s_max (VA).\n\nThe spec defines full row-first storage; BMOPFTools also reads upper-triangular shorthand (mirroring the missing transpose entries) and reports it as I.SPEC.MATRIX_TRIANGULAR. It always writes all n² entries.","title":"Lines, linecodes and matrices"},{"category":"section","location":"conventions/#Loads-and-generators","page":"Data model conventions","text":"configuration ∈ SINGLE_PHASE (2 terminals, between any two nodes), WYE (4 terminals, midpoint return) or DELTA (3 terminals). Setpoint vectors p_nom/q_nom have length 1 (single-phase) or 3. Note the spec FAQ: a \"wye\" in the BMOPF sense always has the neutral return — a 2-terminal load is SINGLE_PHASE, not a degenerate wye.\n\nGenerators additionally carry cost and optional p_min/p_max/q_min/q_max. cost is a per-phase vector of linear coefficients ($/W), one element per phase term; the objective contribution of phase k is cost[k]·P_k. (There is no polynomial/quadratic cost form.) A generator without bounds is an unbounded (slack-style) unit.\n\nVoltage source as slack. The voltage_source fixes its terminal voltages and acts as the network's current slack — it injects the current that closes KCL at the source bus, so no auxiliary slack generator is needed. Beyond v_magnitude/v_angle, it accepts an optional configuration, per-phase flow bounds p_min/p_max/q_min/q_max, and a per-phase cost. The augmentation pass sets cost on the source by default. See Voltage source as current slack. Placing an unbounded generator at the source bus duplicates the slack and is flagged by the pre-flight check (W.PRE.SOURCE_BUS_GENERATOR).","title":"Loads and generators"},{"category":"section","location":"conventions/#Transformer-subtypes","page":"Data model conventions","text":"Six subtypes, each its own sub-dict under transformer.  All impedance fields are in SI units (Ω or S); v_ref_* in V; s_rating in VA.\n\nSubtype map arity (from, to) OPF model impedance fields\nsingle_phase (2, 2) Γ-equivalent, series Z referred to HV r/x_series_from (HV, Ω), r/x_series_to (LV, Ω), g/b_no_load (S)\ncenter_tap (2, 3) T-model, per-leg secondary Z same field names — see note below\nwye_delta (4, 3) per-winding T behind ideal Yd transform r/x_series_from (wye), r/x_series_to (delta), g/b_no_load (S)\ndelta_wye (3, 4) per-winding T behind ideal Dy transform r/x_series_from (delta), r/x_series_to (wye), g/b_no_load (S)\nsingle_phase_autotransformer (2, 2) step voltage regulator: YY core at fixed-tap effective ratio n_eff, shared neutral r/x_series_from, r/x_series_to, g/b_no_load; ratio from tap_ratio + regulator_type\nopen_delta_regulator (4, 4) monolithic open-delta: two line-to-line regulating windings + galvanic straight-through per-regulator r/x_series_*; connection, tap_ratio (len 2), regulator_type\n\nsingle_phase: the series impedance R = R_1 + N^2 R_2, X = X_1 + N^2 X_2 is lumped onto the HV side (Γ convention).  r_series_from/x_series_from are the HV winding values (Ω on the HV voltage base); r_series_to/x_series_to are the LV winding values (Ω on the LV voltage base).  The no-load shunt g_no_load/b_no_load is placed at the HV terminals, phase-to-ground.\n\ncenter_tap: terminal_map_from = [\"1\",\"n\"] (HV phase + neutral), terminal_map_to = [\"1\",\"n\",\"2\"] (leg-1, center-tap neutral, leg-2). v_ref_to is the per-leg voltage (e.g. 120 V, not 240 V). The OPF uses a T-model with independent per-leg secondary impedance branches — r_series_to/x_series_to apply separately to each leg, so unbalanced loading produces different voltages on the two legs.\n\nwarning: Leakage from OpenDSS XHL/XLT/XHT\nFor center_tap, x_series_from/x_series_to are the star-network leakage values, not XHL/2.  Given OpenDSS pair-wise values in %:x_series_from = (XHL + XHT − XLT) / 2  ×  Vhv² / (100 · s_rating)\nx_series_to   = (XHL + XLT − XHT) / 2  ×  Vlv² / (100 · s_rating)Using the 2-winding shortcut XHL/2 on both sides forces both leg voltages to be identical under unbalanced loading, which is wrong.\n\nwye_delta/delta_wye: a per-winding T-model behind the ideal Yd/Dy transform, matching the OpenDSS / PMD reference loss network.  Each winding carries its own series impedance (r/x_series_from, r/x_series_to) and a g/b_no_load core-loss shunt sits at the from-side (HV) phase terminals. v_ref_* are phase-to-neutral equivalents (the √3 factor is absorbed into the effective turns ratio n_eff).  The older single r_series/x_series (wye-side lumped, delta ideal) is accepted as legacy shorthand and migrated onto r_series_from/x_series_from with the secondary branch zero — see the conversion guide and feedback item 21.\n\nsingle_phase_autotransformer: a single-phase step voltage regulator modelled as an autotransformer (series + common winding sharing a node), so the from and to sides are galvanically tied — not isolated.  The ratio is the fixed tap_ratio a (regulated/source); regulator_type selects the ANSI connection, giving the effective from→to ratio n_texteff=1a (Type B, default) or n_texteff=a (Type A).  The OPF voltage/current constraints are the single_phase YY form with N=n_texteff, plus a shared-neutral KCL (I_n + I_textseries + I_textto=0) that closes the common-winding return. v_ref_* are not used (the ratio is tap_ratio); per-unit propagates the same base across the galvanic tie (no voltage-level change).\n\nopen_delta_regulator: a monolithic three-phase open-delta regulator — two single-phase autotransformer windings connected line-to-line across the phase pairs implied by connection (ABBC/BCAC/CABA, GridLAB-D convention), with per-regulator taps tap_ratio = [a1, a2].  The phase common to both regulators is a galvanic straight-through (V_shared,from = V_shared,to), the physically-correct \"common neutral\" model of Yan et al. (2018); the two regulated line-to-line voltages are boosted by their taps while the shared phase passes through unchanged.  See the OPF reference and the derivation note docs/transformer_admittance_derivation.md.\n\nThere is no wye-wye three-phase type: three-phase wye-wye units must be decomposed into three single_phase transformers.  The converter currently parks them in single_phase with 3-phase terminal maps and the conformance check flags the arity (W.SPEC.XFMR_TMAP_ARITY) — see the conversion guide.","title":"Transformer subtypes"},{"category":"section","location":"conventions/#Switches","page":"Data model conventions","text":"","title":"Switches"},{"category":"section","location":"conventions/#Scope:-what-the-switch-object-represents","page":"Data model conventions","text":"The switch is the data model's element for any section of negligible series impedance — a per-conductor short between two buses that either carries current with no voltage drop (closed) or carries none (open). It is an idealised, lossless two-bus element: there is no linecode, no length, and no R/X (contrast Lines). The closed switch imposes v_b^textfr = v_b^textto on every conductor in its terminal map and the open switch fixes its currents to zero (see the OPF switch model).\n\nA single object therefore covers a family of physical devices that, for steady-state OPF, all reduce to the same idealisation. The intended scope:\n\nPhysical device Why it maps to switch\nDisconnector / isolator Manually-operated open/close point; impedance negligible when closed.\nLoad-break switch / sectionaliser Field switching point for reconfiguration; lossless when closed.\nCircuit breaker The conducting path is lossless; protection/trip logic is out of scope for steady-state OPF.\nRecloser As above — a breaker with reclosing control; only its open/closed conduction state matters here.\nTie switch Normally-open reconfiguration link between feeders.\nBusbar section / jumper / bus-tie A near-zero-impedance metallic connection between nodes; an explicit object instead of merging the buses.\nNear-zero-impedance line A line whose Z is below the conditioning threshold — the augmentation pass converts it to a closed switch (W.DOM.LINE_LOW_IMPEDANCE, ref. 2).\n\nWhat it is not: a switch never carries impedance, losses, shunt admittance, tap or phase-shift (use a line, shunt, or transformer); it never connects to ground (only to bus terminals, like lines); and its open_switch flag is a data state, not a control law — there is no protection, fault, or time-sequence model. Reliability/protection studies that need trip curves and reclose sequences are out of scope for the steady-state benchmark.","title":"Scope: what the switch object represents"},{"category":"section","location":"conventions/#Why-a-dedicated-element-(rather-than-zero-impedance-lines-or-bus-merging)","page":"Data model conventions","text":"Inserting a zero- or near-zero-impedance branch into an admittance (bus-injection) formulation produces an infinite entry in Y and an ill-conditioned or rank-deficient system; the classic remedies are either bus merging (contract the branch and fuse its endpoints into a \"super bus\") or special zero-impedance branch handling ref. 2. The branch-flow / current–voltage formulation used here sidesteps this: a closed switch is expressed as an exact voltage-equality constraint with explicit current variables, so it is represented without an admittance and without losing the two distinct buses (and their bounds, names and attached devices). Keeping the section as a first-class object — rather than silently merging buses — preserves topology, lets the open/closed state be toggled for reconfiguration studies, and keeps current results addressable per switch (see switch currents).","title":"Why a dedicated element (rather than zero-impedance lines or bus merging)"},{"category":"section","location":"conventions/#Terminology-in-other-tools-and-standards","page":"Data model conventions","text":"The same \"lossless conducting section\" concept appears across the field under a range of names:\n\nOpenDSS has no separate switch element: a Line with Switch=yes becomes a 1 mΩ line that protection control elements (Fuse, Relay, Recloser) open and close on a terminal. So in OpenDSS the conduction path and the control are deliberately split — the line is the switchable section, the control element is the logic. The BMOPF switch corresponds to the switchable line, not the controller.\nPowerModels / PowerModelsDistribution define an explicit switch component with a discrete state ∈ {OPEN, CLOSED}; closed enforces equal voltages across the two buses, open blocks flow. A switch with no rs/xs/ linecode is ideal (lossless); lossy parameters trigger a decomposition into a virtual branch + bus + ideal switch. BMOPF's open_switch boolean maps directly onto PMD state (0=OPEN, 1=CLOSED) — see to_pmd — and the BMOPF switch is always the ideal, lossless case.\nCIM (IEC 61970-301) models this as a Switch class with open / normalOpen / ratedCurrent attributes, specialised into ProtectedSwitch (→ Breaker, Recloser, LoadBreakSwitch), Disconnector, GroundDisconnector and Sectionaliser, with Jumper and BusbarSection as related connectivity elements. BMOPF deliberately collapses this whole taxonomy into one object: the device subtype (breaker vs. disconnector vs. busbar) is not represented because it does not change the steady-state equations.\nGeneral power-flow literature calls these zero-impedance branches (ZIB) or zero-impedance lines, and discusses the super-bus / branch-contraction treatment they require in admittance formulations ref. 2.\n\nThe takeaway: most tools either (a) reuse a line with a flag (OpenDSS) or (b) provide a dedicated ideal switch with an open/closed state (PMD, CIM). BMOPF follows (b), with a single lossless switch object whose open_switch boolean is the open/closed state, and intentionally abstracts away the device-class distinctions (breaker / recloser / disconnector / busbar / tie) that CIM enumerates, since they are immaterial to a steady-state OPF.","title":"Terminology in other tools and standards"},{"category":"section","location":"conventions/#Metadata-blocks","page":"Data model conventions","text":"The network dict carries two distinct metadata containers, with different scopes and serialisation behaviour.","title":"Metadata blocks"},{"category":"section","location":"conventions/#meta-—-spec-level,-written-to-JSON","page":"Data model conventions","text":"net[\"meta\"] is a flat Dict{String,Any} included verbatim in the JSON output by write_bmopf.  All fields are optional.  Unknown fields are allowed (an I.SCHEMA.UNKNOWN_FIELDS info finding is raised, not an error) so callers can add project-specific keys freely.\n\nField Type Description\n$schema String (URI) Schema URI for version detection and forward migration. Auto-filled by write_bmopf.\ntitle String Human-readable name for this dataset / case.\ndescription String Free-text description.\nversion String Dataset version (any string; semver recommended).\ncreated String ISO 8601 datetime when the file was first created (e.g. \"2024-06-19T14:32:00Z\"). Auto-filled on first write.\nmodified String ISO 8601 datetime of most recent edit. Not auto-filled; set explicitly when updating a file.\nlicense String SPDX identifier (e.g. \"CC-BY-4.0\") or full URI.\nauthors Array of objects List of contributors; each object may have name, email, orcid.\nsources Array of objects Origin datasets; each object may have name, url, format, doi, version.\ngenerator Object Tool provenance: {\"tool\": \"BMOPFTools.jl\", \"version\": \"x.y.z\"}. Auto-filled by write_bmopf.\n\nAuto-generation on write. write_bmopf always emits a meta block.  It merges fields in priority order: the meta keyword argument → net[\"meta\"] → auto-generated defaults.  Auto-generation fills three fields if they are absent: $schema, generator, and created. Caller-supplied values are never overwritten.\n\nOn parse. parse_bmopf and from_dss carry net[\"meta\"] through unchanged.  migrate reads meta.$schema to detect the spec version and apply forward migrations. The schema checker validates known fields and flags format violations as warnings (W.SCHEMA.META_*).\n\nExample (passed to write_bmopf via the meta kwarg):\n\nwrite_bmopf(net, \"lv_feeder1.json\";\n    meta = Dict(\n        \"title\"       => \"LV network 1, Feeder 1\",\n        \"description\" => \"ENWL LV test feeder, unbalanced residential load\",\n        \"license\"     => \"https://creativecommons.org/licenses/by/4.0/\",\n        \"authors\"     => [Dict(\"name\" => \"Frederik Geth\",\n                               \"orcid\" => \"0000-0001-9534-2265\")],\n        \"sources\"     => [Dict(\"name\" => \"ENWL dataset\",\n                               \"format\" => \"OpenDSS\",\n                               \"url\"    => \"https://www.enwl.co.uk/\")],\n    ))","title":"meta — spec-level, written to JSON"},{"category":"section","location":"conventions/#_meta-—-tool-private,-never-serialised","page":"Data model conventions","text":"net[\"_meta\"] is a Dict{String,Any} used internally by BMOPFTools for traceability.  It is not written to JSON by write_bmopf and is never reported as a schema violation.  Its contents are informational; downstream code should treat them as advisory.\n\nKey Set by Contents\nparsed_at parse_bmopf Timestamp when the JSON was parsed.\nterminal_coercions parse_bmopf `{\"n\": <count>, \"mode\": \"<alias\npowerio_source from_dss Absolute path of the .dss file that was converted.\npowerio_warnings from_dss Array of warning strings emitted by the DSS→JSON converter.\nmigration_notes migrate Array of W.MIGRATE.UPGRADED finding dicts appended when a forward migration is applied.","title":"_meta — tool-private, never serialised"},{"category":"section","location":"conventions/#Time-series-(extension)","page":"Data model conventions","text":"time_series at the root plus component-level time_series reference dicts follow the PMD convention (values are multiplicative scale factors on the static value). This is a BMOPFTools extension beyond the static-only TF spec; get_snapshot materialises a snapshot at a given index and analyze does this automatically.","title":"Time series (extension)"},{"category":"section","location":"bounds/decision_matrix/#Decision-Matrix","page":"Decision Matrix","text":"A lookup table for the question \"given my objective, my bounds, and my formulation — is the problem well posed, which solution will I get, and can I trust it?\" Read the prose reasoning behind every entry in Bounds, Branches, and Feasibility.\n\nnote: How to use this\nDataset authors: find your intended objective and confirm the instance lands in a green row before shipping.\nValidators: this is the map of where your formulation should agree with the benchmark and where it is expected to diverge. Divergence inside a \"✗ not physical\" cell is not a bug in your code.","title":"Decision Matrix"},{"category":"section","location":"bounds/decision_matrix/#Objective-bounds-formulation","page":"Decision Matrix","text":"Legend: ✓ = well behaved / exact / physical; ⚠ = conditional, verify; ✗ = ill posed / inexact / non-physical.\n\nObjective family Bounded? (no op. limits) Bounded? (full op. limits) Branch selected Relaxation exactness (SOC/SDP) Recommended model\nMin generation cost (non-neg. coeffs) ⚠ coercive only if strictly convex ✓ High-voltage ✓ under standard conditions Relaxation, verify ex post\nMin losses / min slack power ✓ (coercive) ✓ High-voltage ✓ Relaxation\nFeasibility (constant objective) ✗ unbounded directions remain ✓ Undetermined among feasible pts ⚠ one-sided certificate only Nonconvex or verified relaxation\nMin import from grid ✓ ✓ High-voltage ✓ Relaxation\nMax Σ voltage magnitude ⚠ unbounded above without v̄ ✓ High-voltage ⚠ fails when upper v bounds bind Relaxation, watch upper bounds\nMax PV / DER injection ✗ ⚠ bounded but boundary-seeking Drives to upper v bound ✗ typically inexact Nonconvex; see list\nMax load served / loadability ✗ ⚠ drives to the nose Toward low-voltage / nose ✗ inexact near collapse Nonconvex + continuation\nMax transfer / ATC / interface flow ✗ ⚠ boundary-seeking Maximizes current ✗ Nonconvex\nMax export / feed-in ✗ ⚠ Reverse-flow current max ✗ Nonconvex\nMax storage throughput / arbitrage volume ✗ ⚠ Cycling-current max ✗ (plus SoC-constraint inexactness) Nonconvex\nMax revenue / profit ✗ ⚠ More generation & flow ✗ Nonconvex\nMin cost with a negative coefficient ✗ ⚠ Inverted → toward low-voltage ✗ data-induced Audit signs; nonconvex if real\n\nwarning: The columns are not independent\n\"Bounded,\" \"branch,\" and \"exact\" are three readings of one underlying property (see §5). A row that is ✗ on exactness is almost always boundary-seeking and low-voltage-leaning too. The green family at the top — objectives non-decreasing in generation/losses — is green in every column for the same reason.","title":"Objective × bounds × formulation"},{"category":"section","location":"bounds/decision_matrix/#Formulation-cross-cut","page":"Decision Matrix","text":"The table's exactness column assumes a single-phase, radial-or-mild-mesh setting. Adjust as follows:\n\nLinear approximations (LinDistFlow, network/DC): always bounded and convex given bounds; never \"inexact\" in the relaxation sense, but they cannot represent collapse or branch multiplicity at all. Use for screening, not for any question on this page.\nConvex relaxations (SOC-BFM/BIM, SDP): the exactness column applies. On meshed networks the radial guarantees weaken; on multiphase / unbalanced networks treat all ✓ as ⚠ and verify (Bernstein et al., 2018).\nNonconvex AC: no exactness question, but every ✗/⚠ row can still converge to a low-voltage or locally optimal solution depending on the start point. Use a high-voltage (flat or warm) start and check the diagnostics.\n\n\n\nSee also: Bounds, Branches, and Feasibility · Objectives that imply loss maximization · Diagnostics & validation · References","title":"Formulation cross-cut"},{"category":"section","location":"bounds/loss_maximization/#Objectives-That-Imply-Loss-Maximization","page":"Objectives That Imply Loss Maximization","text":"Several objectives that read as maximizing something desirable are, mechanically, maximizing line losses or branch current. On a convex relaxation that breaks exactness; on the nonconvex model it drags the solution toward the low-voltage branch. This page is the catalogue, with safe reformulations.","title":"Objectives That Imply Loss Maximization"},{"category":"section","location":"bounds/loss_maximization/#The-one-test","page":"Objectives That Imply Loss Maximization","text":"A relaxation stays exact, and the nonconvex problem stays on the high-voltage branch, when the objective is non-decreasing in every nodal generation — equivalently, in line losses (Low, 2014; Gan et al., 2015; Yuan & Paolone, 2020). So, for any objective not on the safe list:\n\ntip: Self-diagnosis\nAt fixed loads and topology, does the optimizer prefer larger branch current anywhere — or does its optimum sit where an upper voltage bound binds? If either is yes, expect relaxation inexactness and drift toward the low-voltage solution.","title":"The one test"},{"category":"section","location":"bounds/loss_maximization/#The-catalogue","page":"Objectives That Imply Loss Maximization","text":"Objective as written Why it rewards loss / current Safer reformulation\nMax PV/DER injection, max renewable dispatch, naive hosting capacity Surplus injection becomes reverse flow → I^2 r, and pushes voltages up until the upper bound binds — the regime where SOC/SDP exactness is known to fail Maximize injection net of losses; or fix injection and solve feasibility; or penalize curtailment instead of rewarding injection — then verify exactness\nMax total load served / loadability / demand delivered Drives straight to the nose; the loadability limit is the collapse point Parametrize load, minimize cost/loss at each level; for true loadability use the nonconvex model + continuation\nMax power/energy transfer, ATC, interface flow Directly maximizes branch current → I^2 r Bound transfer as a constraint; study maximum transfer only in the nonconvex model\nMax export to grid / feed-in / \"self-sufficiency\" via export Reverse-flow current maximization Recast as minimize net cost / minimize net import\nMax storage throughput / arbitrage volume / EV energy delivered Rewards cycling current; the linear charge/discharge constraints add their own documented inexactness Minimize cost including losses; bound throughput as a constraint\nMax reactive support / VAR injection Reactive current also dissipates I^2 r Minimize losses subject to voltage constraints; reward constraint satisfaction, not raw Q\nMin cost with a negative cost coefficient (subsidy, feed-in tariff as negative cost) The form is the safe one, but a negative coefficient inverts monotonicity, so min-cost silently becomes max-generation → loss-max. The trap is in the data, not the objective Audit the sign of every cost term; if negative prices are real, switch to the nonconvex model and do not trust the relaxation\nMax revenue / profit (price × output) Rewards more generation and flow As above — minimize net cost instead\n\nwarning: The asymmetry to remember\nMinimize import is loss-aligned and safe. Maximize export is not — even though both sound like \"less reliance on the grid.\" Direction of the optimization, not the English description, is what determines the monotonicity.\n\nnote: This is a tested behaviour, not just a caution\nThe negative-coefficient row is exercised directly in the OPF suite: test T4 (Validating the OPF) gives a generator a negative cost coefficient and confirms the optimum drives each phase to p_max with objective = −3·P_max — i.e. min-cost has silently become max-generation. If your own formulation reproduces that number, it is reproducing the trap, not a bug.","title":"The catalogue"},{"category":"section","location":"bounds/loss_maximization/#Two-that-look-risky-but-are-mostly-fine","page":"Objectives That Imply Loss Maximization","text":"Maximize sum_i V_i. Counterintuitively this is usually loss-reducing and correctly selects the high-voltage branch. The residual risk is different: it can drive into binding upper voltage bounds, which is a separate exactness-failure mode (condition C2 in Gan et al., 2015). Safe for branch selection; watch the ceiling for relaxation tightness.\nMinimize generation / min slack power / min cost (non-negative coeffs). The canonical safe family. Because slack power = losses + net load, minimizing it is loss minimization — which is exactly why a feasibility problem is so often turned into min-slack to make it well posed and branch-correct (§5).","title":"Two that look risky but are mostly fine"},{"category":"section","location":"bounds/loss_maximization/#When-loss-maximizing-is-the-actual-research-question","page":"Objectives That Imply Loss Maximization","text":"Maximum loadability, voltage-stability margin, and worst-case transfer are legitimate questions — they are deliberately boundary-seeking. The point is not to avoid them but to model them correctly:\n\nnote: Note\nSolve these in the nonconvex AC model (optionally with a continuation / margin formulation) — for BMOPFTools that is solve_opf — not in a relaxation. No relaxation is a valid feasibility certificate in this regime, including any you reach via to_pmd: a relaxation will happily report an \"optimal\" loadability past the true collapse point. See Known traps for runnable instances that exhibit this.\n\n\n\nSee also: Bounds, Branches, and Feasibility · Decision matrix · Diagnostics & validation · References","title":"When loss-maximizing is the actual research question"},{"category":"section","location":"conversion/#Conversion-guide","page":"Conversion guide","text":"to_pmd exports a BMOPF data model to a PowerModelsDistribution ENGINEERING dict. This page documents every deliberate decision in that converter — the things you would otherwise have to discover by diffing data. The same field mapping (read in reverse) describes how a PMD ENGINEERING dict maps onto BMOPF, which is useful when comparing against PMD-based tooling.\n\nNote. OpenDSS networks are now ingested directly by from_dss (via PowerIO.jl), which emits BMOPF JSON without going through PMD. The earlier from_pmd parser has been removed.","title":"Conversion guide"},{"category":"section","location":"conversion/#Scaling-and-basic-field-mapping","page":"Conversion guide","text":"Voltages: PMD vm (per-unit on voltage_scale_factor) → volts; va degrees → radians.\nPowers: PMD values × power_scale_factor → W/var/VA.\nTerminals: PMD integers 1,2,3 → \"1\",\"2\",\"3\", 4 → \"n\".\nPMD enum values (WYE, DELTA, …) → strings.\nUnrecognised PMD fields are preserved per component under a _pmd sub-dict — nothing is silently dropped; to_pmd merges them back.\nLine lengths and per-length linecode values arrive from PMD already in metres / Ω-per-metre (PMD normalises DSS units internally).","title":"Scaling and basic field mapping"},{"category":"section","location":"conversion/#Identifier-case-folding","page":"Conversion guide","text":"OpenDSS identifiers are case-insensitive but case-preserving: SourceBus, sourcebus and SOURCEBUS denote the same bus, and different statements in one model may spell it differently. BMOPF keys are matched exactly, so from_dss case-folds every identifier and every reference to lower case on ingest (bus names, linecodes, component ids; bus / bus_from / bus_to / linecode references). This is lossless — OpenDSS uniqueness up to case guarantees folding only reunites references to the same object. If two ids in a collection ever fold to the same value (which valid OpenDSS cannot produce) from_dss raises rather than silently dropping one.","title":"Identifier case-folding"},{"category":"section","location":"conversion/#The-earth-terminal-(OpenDSS-.0)","page":"Conversion guide","text":"BMOPF has no earth terminal — ground is implicit. OpenDSS, by contrast, uses node .0 for earth, which surfaces in the BMOPF JSON as terminal 5.\n\nPowerIO renders the OpenDSS earth node as terminal \"5\" (e.g. a transformer wye winding whose star point is earthed arrives with terminal_map_to = [\"1\",\"2\",\"3\",\"5\"]). On ingest, from_dss routes \"5\" to the bus neutral \"n\" as part of the 1,2,3,4 → a,b,c,n remap, so every bus ends up on the a/b/c/n convention and its neutral is detected. The earthed star point is then grounded through the bus neutral rather than solidly — a slightly lossy choice (matching the earlier from_pmd behaviour) that is recorded under net[\"_meta\"][\"earth_terminal_routing\"] so it stays inspectable.","title":"The earth terminal (OpenDSS .0)"},{"category":"section","location":"conversion/#Pricing-the-slack-source","page":"Conversion guide","text":"The OpenDSS circuit object is simultaneously a voltage reference and an implicit unbounded power injection. The BMOPF voltage_source captures both: it is the network's current slack (see Voltage source as current slack). What's missing for a well-posed cost objective on a raw utility dataset (no generators at all) is a price on that imported power.\n\nfrom_dss imports the source without a price (from_dss does not add a cost). The augmentation pass supplies one: it attaches a per-phase cost to the source itself (default 1.0 $/kWh, kwarg slack_cost), so minimum-cost dispatch equals loss minimisation. No flow bounds are added, so the source stays an unbounded slack, and no auxiliary generator is created — the cost lives on the voltage_source.","title":"Pricing the slack source"},{"category":"section","location":"conversion/#Load-configuration","page":"Conversion guide","text":"The spec distinguishes SINGLE_PHASE (any two nodes) from WYE (4-terminal midpoint return). 2-terminal loads are SINGLE_PHASE; to_pmd maps SINGLE_PHASE back to a PMD 2-terminal \"wye\" load.","title":"Load configuration"},{"category":"section","location":"conversion/#Transformer-impedance-bases","page":"Conversion guide","text":"PMD and OpenDSS give per-winding resistance rw (%) and pair-wise leakage xsc/xhl/xlt/xht (%), both on the winding's own kVA/kV² base. BMOPF stores ohms on each winding's own voltage base.\n\nsingle_phase (2-winding, Γ-model):\n\nZ_base,from = v_ref_from² / s_rating\nZ_base,to   = v_ref_to²   / s_rating\nr_series_from = rw₁ · Z_base,from\nr_series_to   = rw₂ · Z_base,to\nx_series_from = (xhl / 2) · Z_base,from    # half of pair leakage on each side\nx_series_to   = (xhl / 2) · Z_base,to\n\ncenter_tap (3-winding, T-model — star-network leakage conversion required):\n\nOpenDSS specifies three pair-wise leakage values XHL, XLT, XHT (%). These are not split evenly — they must be converted via the star (Steinmetz) network formula before storing:\n\nx_series_from = (XHL + XHT − XLT) / 2  ×  Z_base,from / 100\nx_series_to   = (XHL + XLT − XHT) / 2  ×  Z_base,to   / 100\n\nFor the common symmetric case XHT = XHL (both legs same leakage to HV), this simplifies to x_series_from = (XHL − XLT/2) × Z_base,from / 100 and x_series_to = (XLT/2) × Z_base,to / 100.\n\nwarning: Warning\nUsing XHL/2 for both sides (copying the 2-winding formula) produces identical leg voltages regardless of load imbalance.  The error is ~0.4–0.5 V per leg under a 3 kW imbalance on a 120 V feeder.\n\nResistance maps directly per winding:\n\nr_series_from = rw₁ · Z_base,from      # wdg1 (HV)\nr_series_to   = rw₂ · Z_base,to        # wdg2 = wdg3 for a symmetric unit\n\nNote: v_ref_to is the per-leg voltage (e.g. 120 V), not the full secondary span (240 V).\n\nNo-load branch (applies to both single_phase and center_tap):\n\nOpenDSS %noloadloss and %imag (or cmag) convert to SI admittances at the HV terminals:\n\nY_base = s_rating / v_ref_from²\nG = (%noloadloss / 100) · Y_base                      → g_no_load  (S)\nY_mag = cmag · Y_base          # cmag = %imag/100 · s_rating/v_ref_from\nB = sqrt(Y_mag² − G²)                                 → b_no_load  (S)\n\nBoth fields are omitted when zero.\n\nwye_delta / delta_wye (2-winding, per-winding T-model):\n\nThese now use the same per-winding field set as single_phase — separate r/x_series_from (wye/primary winding) and r/x_series_to (delta/secondary winding), plus a g/b_no_load core-loss branch — matching the OpenDSS / PMD eng2math reference loss network. The series impedance enters the OPF as a voltage drop on the winding currents behind the ideal Yd/Dy transform; the delta side is no longer assumed ideal.\n\nZ_base,from = v_ref_from² / s_rating\nZ_base,to   = v_ref_to²   / s_rating\nr_series_from = rw₁ · Z_base,from\nr_series_to   = rw₂ · Z_base,to\nx_series_from = xsc₁ · Z_base,from     # PMD lumps all leakage on winding 1\nx_series_to   = 0                      # 2-winding star: LV branch is zero\n\nThe no-load branch maps as for the two-winding types:\n\nY_base = s_rating / v_ref_from²\ng_no_load =  (noloadloss) · Y_base       # noloadloss = %noloadloss / 100\nb_no_load = -(cmag)       · Y_base       # cmag       = %imag       / 100\n\nnote: Leakage placement\nFor a 2-winding unit PMD's star conversion (_sc2br_impedance) puts the entire xhl leakage on the winding-1 (HV) branch, with zero on the LV branch — not an even split. BMOPFTools follows that convention for wye_delta/delta_wye.\n\nLumped single-impedance form. A wye_delta/delta_wye transformer may carry a single lumped r_series/x_series (wye-side, delta ideal) instead of the per-winding fields — this is what from_dss (PowerIO) emits today. The OPF and Ybus builders read only the per-winding r/x_series_from/_to fields, so the lumped form is normalised at parse time by migrate / _migrate_transformer_series_fields!: it is moved onto r_series_from/x_series_from (with the secondary branch zero) and a W.MIGRATE.XFMR_SERIES_FIELDS note is recorded. to_pmd writes the per-winding fields back to PMD rw/xsc.","title":"Transformer impedance bases"},{"category":"section","location":"conversion/#Known-limitations","page":"Conversion guide","text":"Lumped transformer impedance (from from_dss). PowerIO emits wye_delta/delta_wye units with a single lumped r_series/x_series rather than a per-winding T-model. BMOPFTools normalises this onto r_series_from/x_series_from on ingest (see above), so the OPF/Ybus see a nonzero leakage impedance — but the leakage is all on the from-winding (the to-winding branch is zero). A faithful per-winding split would require per-winding emission upstream (tracked as a PowerIO.jl issue).\nEarth terminal \"5\" → bus neutral (from from_dss). PowerIO keeps the OpenDSS earth node as terminal \"5\". BMOPFTools routes it to the bus neutral on ingest (see above), which grounds an earthed star point through the bus neutral rather than solidly. Native earth resolution upstream is tracked as a PowerIO.jl issue.\nWye-wye three-phase transformers have no spec type. They are parked in single_phase with 3-phase terminal maps and flagged (W.SPEC.XFMR_TMAP_ARITY); the faithful decomposition into three single-phase units is future work.\nRegControl / tap controllers do not convert; see the regulator-pattern detection in the methodology notes.\nRegulator subtypes (single_phase_autotransformer, open_delta_regulator) are OPF-native data-model objects but are not produced by from_dss — it does not recognise OpenDSS AutoTrans/RegControl or open-delta banks as these objects, and to_pmd does not emit them. They are authored directly in BMOPF JSON. See conventions and the OPF reference.","title":"Known limitations"},{"category":"section","location":"bounds/diagnostics/#Diagnostics-and-Validation","page":"Diagnostics & Validation","text":"How to detect the failure modes from Bounds, Branches, and Feasibility, rather than merely avoid them. This page is aimed at the validation audience: you are checking your own linear, convex, or nonconvex formulation against the benchmark and need to know what to measure and what a discrepancy means.\n\nThe throughline: two well-known generic optimization tools become your two domain-specific diagnostics. Bound everything and see what binds is the unboundedness test of §1; evaluating a relaxed solution against the AC equations is the exactness test of §5.\n\ntip: BMOPFTools-native shortcuts\nThe JuMP recipes below apply to your model. Two of these diagnostics already exist inside this package, and you should reach for them first:AC-feasibility of a candidate point (Symptom 3) is exactly what solve_feasibility_opf measures: it adds an elastic slack current at every non-source terminal and minimises its norm, so a returned total_slack_magnitude_A ≈ 0 certifies the point satisfies every KCL/KVL and component-model equation. This is the native, four-wire analogue of primal_feasibility_report against the AC model.\nIll-posedness before you solve — flat objectives, free injections, and cost degeneracy — is caught structurally by the benchmark-readiness flags (W.BENCH.GEN_ZERO_COST, W.BENCH.GEN_DEGENERATE_COST, W.BENCH.GEN_NO_DOF; see Methodology § Benchmark readiness). A DUAL_INFEASIBLE from a zero-cost generator is the runtime echo of W.BENCH.GEN_ZERO_COST.","title":"Diagnostics & Validation"},{"category":"section","location":"bounds/diagnostics/#Symptom-1-—-INFEASIBLE_OR_UNBOUNDED-/-DUAL_INFEASIBLE","page":"Diagnostics & Validation","text":"First, disambiguate. Unboundedness can only come from the objective, so drop it and re-solve (YALMIP; JuMP):\n\n@objective(model, Min, 0)   # feasibility test\noptimize!(model)\n# feasible now  ⇒ original was UNBOUNDED\n# infeasible now ⇒ original was (also) INFEASIBLE\n\nThen localize the unboundedness. Add large finite bounds to every free or one-sided variable, re-solve, and inspect which variables sit at the artificial bound — those are your unbounded directions:\n\nfor v in all_variables(model)\n    has_lower_bound(v) || set_lower_bound(v, -1e6)\n    has_upper_bound(v) || set_upper_bound(v,  1e6)\nend\noptimize!(model)\nfor v in all_variables(model)\n    if isapprox(value(v), 1e6; atol=1) || isapprox(value(v), -1e6; atol=1)\n        @info \"at artificial bound\" v value(v)\n    end\nend\n\nIn a power-flow model the variables pinned at the artificial bound are almost always voltage magnitudes (missing underline v / overline v) or an unbounded generator — i.e. exactly the missing operational limits from §1. Erwin Kalvelagen's variant — bind the objective to a large-bounded auxiliary variable via an equality, then read its value — is the same idea (the canonical infeasible/unbounded writeup).","title":"Symptom 1 — INFEASIBLE_OR_UNBOUNDED / DUAL_INFEASIBLE"},{"category":"section","location":"bounds/diagnostics/#Symptom-2-—-model-is-infeasible-and-you-do-not-know-why","page":"Diagnostics & Validation","text":"Use an irreducible infeasible subsystem (IIS) if your solver supports it:\n\ncompute_conflict!(model)\nif get_attribute(model, MOI.ConflictStatus()) == MOI.CONFLICT_FOUND\n    iis, _ = copy_conflict(model)\n    print(iis)\nend\n\nWhen no IIS is available, the penalty relaxation locates the binding constraints by letting them be violated at a cost:\n\nmap = relax_with_penalty!(model)   # adds slacks + penalty to the objective\noptimize!(model)\nfor (con, slack) in map\n    value(slack) > 1e-6 && @info \"violated\" con value(slack)\nend\n\nwarning: Distinguish modeling infeasibility from physical infeasibility\nrelax_with_penalty! does not relax variable bounds or integrality. More important here: in a constant-power model an infeasibility may be physical — you are past the loadability / collapse boundary (§4), not mis-modeled. If the IIS centres on the power-balance and voltage-bound constraints of a heavily loaded sub-network, suspect collapse, and confirm with a continuation sweep rather than editing constraints.","title":"Symptom 2 — model is infeasible and you do not know why"},{"category":"section","location":"bounds/diagnostics/#Symptom-3-—-is-my-relaxation-exact?-(the-central-validation-check)","page":"Diagnostics & Validation","text":"Evaluate the relaxed solution against the nonconvex AC constraints and read off the violation. JuMP's primal_feasibility_report does this against any model:\n\n# Solve the relaxation, then test its point against the AC model:\nreport = primal_feasibility_report(ac_model, Dict(v => value(v) for v in all_variables(relaxed_model)))\nisempty(report) ? @info(\"AC-feasible: relaxation exact\") :\n                  @info(\"AC-infeasible: relaxation INEXACT\", report)\n\nIn BMOPFTools the same check is available natively for a four-wire network: pin the candidate dispatch into the case and call solve_feasibility_opf, which minimises an injected slack current. A near-zero slack certifies the point is a valid AC power flow; a non-zero slack is the violation, localised to the terminals where it concentrates:\n\nres     = solve_feasibility_opf(net; optimizer = Ipopt.Optimizer)\nslack_A = res[\"total_slack_magnitude_A\"]\nslack_A < 1e-3 ? @info(\"AC-feasible: candidate is a valid power flow\") :\n                 @info(\"AC-infeasible: relaxation/candidate INEXACT\", slack_A)\n\nFor the branch-flow relaxation you can also read the gap directly per branch:\n\ntextgap_ij = ell_ij - fracS_ij^2v_i\n\nwhich is approx 0 on every branch when exact and strictly positive where the cone is slack. For the SDP / BIM relaxation, check the per-clique voltage matrix rank (or the ratio of the two largest eigenvalues): a rank-1 matrix (eigenratio to infty, second eigenvalue to 0) means exact (Lupien & Lesage-Landry, 2023).\n\ntip: Where to expect inexactness\nEmpirically the relaxation goes inexact precisely when upper voltage bounds bind and their duals exceed a threshold — and exact solutions reliably show binding upper bounds on active/reactive withdrawals instead (Gan et al., 2015; Bobo et al., 2020). If your validation diverges from the benchmark, check whether you are in a loss-rewarding objective (list) or against an upper voltage bound first.","title":"Symptom 3 — is my relaxation exact? (the central validation check)"},{"category":"section","location":"bounds/diagnostics/#Symptom-4-—-I-think-I-am-on-the-wrong-(low-voltage)-branch","page":"Diagnostics & Validation","text":"A nonconvex solver can converge to the low-voltage solution, especially under a loss-rewarding objective or a poor start. Checks:\n\nVoltage profile. Operational solutions sit near 1 p.u.; a solution with a cluster of buses well below underline v-class values (e.g. 05–08 p.u.) on a normally loaded feeder is the low-voltage branch.\nRestart from a high-voltage start. Flat start or warm-start from a linear-model solution; the high-voltage solution is the one fixed-point and relaxation methods converge to (Dvijotham et al., 2017). If the two starts give different solutions, you have multiplicity.\nCross-solver / cross-formulation. Solve the relaxation too; if it is exact, its (unique, global) solution is the high-voltage one and is the reference your nonconvex solution should match (§2).","title":"Symptom 4 — I think I am on the wrong (low-voltage) branch"},{"category":"section","location":"bounds/diagnostics/#Validation-checklist-against-the-benchmarks","page":"Diagnostics & Validation","text":"Reproduce the benchmark's reported objective with your formulation and the same objective and bounds.\nIf you use a relaxation, run Symptom 3 and confirm exactness; only compare the value if exact.\nIf values differ, classify before debugging code: is the row in the decision matrix a ✗ cell? Is the objective on the loss-maximization list? Are upper voltage bounds binding? Expected divergence is not a bug.\nFor collapse/loadability instances, compare margins from a continuation sweep, not single-point objective values.","title":"Validation checklist against the benchmarks"},{"category":"section","location":"bounds/diagnostics/#See-also-—-general-optimization-debugging-guides","page":"Diagnostics & Validation","text":"Model debugging is an established craft, not specific to this library:\n\nJuMP — Debugging tutorial and Solutions / infeasibility certificates.\nKalvelagen — \"The best way to debug infeasible models\".\nYALMIP — Infeasible or unbounded and Debugging unbounded models.\nGAMS — Execution errors & performance (the most complete cross-tool treatment).\nPyomo — Model debugging.\nAIMMS — Debug infeasible or unbounded results.\n\n\n\nSee also: Bounds, Branches, and Feasibility · Decision matrix · Objectives that imply loss maximization · Known traps · References","title":"See also — general optimization-debugging guides"},{"category":"section","location":"findings/#Finding-code-reference","page":"Finding-code reference","text":"The complete catalogue of finding codes, grouped by family. Codes are stable identifiers — filter on f.code, never on message text. Severity prefix: E. error, W. warning, I. info (see Analysis & reports for the severity semantics).","title":"Finding-code reference"},{"category":"section","location":"findings/#COMP-—-completeness","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nE.COMP.MISSING_REQUIRED E A component lacks a field the data model marks required (incl. the seven transformer required fields per subtype). The case cannot be instantiated as an OPF without it.","title":"COMP — completeness"},{"category":"section","location":"findings/#SCHEMA-—-unknown-fields-and-metadata-validation","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nI.SCHEMA.UNKNOWN_FIELDS I Fields present that the data model does not define (underscore-prefixed extension keys are exempt). Catalogued rather than rejected: they are either converter passthrough or schema-evolution candidates — but a spec-conformant consumer will ignore them, so nothing essential should live there.\nE.SCHEMA.REQUIRED E JSON Schema validation: a required field is missing at the reported path.\nE.SCHEMA.TYPE E JSON Schema validation: a field has the wrong JSON type (e.g. string where a number is expected) at the reported path.\nE.SCHEMA.ENUM E JSON Schema validation: a value is not among the allowed enumerated values.\nE.SCHEMA.RANGE E JSON Schema validation: a numeric value violates a minimum/maximum (or exclusive-bound) range constraint.\nI.SCHEMA.OTHER I JSON Schema validation: a schema violation not covered by the specific codes above (catch-all, carrying the raw reason and path).\nI.SCHEMA.VERSION_UNKNOWN I The declared spec version has no bundled JSON Schema document, so structural schema validation was skipped for this case.\nW.SCHEMA.META_SCHEMA_URI W meta.$schema is present but does not look like an https:// URI. The field is intended to point to the versioned BMOPF JSON Schema document.\nW.SCHEMA.META_DATE_FORMAT W meta.created or meta.modified is not a recognisable ISO 8601 datetime string (expected YYYY-MM-DD or YYYY-MM-DDTHH:MM:SSZ).\nI.SCHEMA.META_LICENSE_URI I meta.license is a long string that does not look like a URI. Short SPDX identifiers (e.g. CC-BY-4.0) are fine; longer values should be a https:// URI pointing to the licence text.\nW.SCHEMA.META_ORCID_FORMAT W An entry in meta.authors has an orcid field that does not match the standard ORCID format XXXX-XXXX-XXXX-XXXX.\nW.SCHEMA.META_SOURCE_URL W A url field in meta.sources is present but does not look like an https:// URI.","title":"SCHEMA — unknown fields & metadata validation"},{"category":"section","location":"findings/#CONN-—-connectivity-and-topology","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nE.CONN.DISCONNECTED E More than one connected component (over lines, closed switches and transformers). Buses without a path to a source have no defined operating point.\nE.CONN.SELF_LOOP E A line, switch, or transformer has identical bus_from and bus_to — a zero-length branch that creates a degenerate KVL constraint and is almost always a wiring error.\nW.CONN.MESHED W Physical branch count exceeds the spanning-forest count — cycles exist. Counted over branch elements, so electrically parallel lines are correctly detected as meshes. Not an error (the spec supports meshes) but radial-only methods will fail.\nW.CONN.DANGLING W Degree-1 buses with no load, generator or shunt attached — dead ends that contribute variables and constraints but no physics; often conversion artifacts (e.g. switch far-ends).","title":"CONN — connectivity & topology"},{"category":"section","location":"findings/#VOLT-—-voltage-levels","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nE.VOLT.LEVEL_MISMATCH E BFS voltage propagation reaches a bus with two inconsistent nominal voltages (beyond 5 %). The network's transformer ratios and topology contradict each other.\nE.VOLT.LINE_CROSSING E A line (not a transformer) connects buses assigned to different voltage levels. Only transformers may cross levels; this is almost always a wiring error in the data.\nW.VOLT.UNASSIGNED W Buses unreachable from any voltage source during propagation — likely islanded; their nominal voltage is unknown.\nW.VOLT.XFMR_RATIO W A transformer's v_ref turns ratio disagrees (>10 %) with the voltage ratio of the levels it actually connects — ratio and placement are inconsistent.","title":"VOLT — voltage levels"},{"category":"section","location":"findings/#DIV-—-diversity-and-symmetry","page":"Finding-code reference","text":"Symmetries in data create symmetric optima and degrade NLP convergence ref. 2; these findings flag suspiciously templated parameterisation.\n\nCode Sev Trigger & rationale\nW.DIV.LOAD_SYMMETRIC W More than half of the loads share identical (p_nom, q_nom) tuples — copy-paste parameterisation; dispatch among them is interchangeable.\nI.DIV.LOAD_CV_LOW I Load p_nom coefficient of variation < 0.05 across ≥3 loads — essentially uniform loading.\nI.DIV.LOAD_PF_DSS_DEFAULT I Load power factor mean is within 1 % of 0.88 with CV < 0.05 — strongly suggests reactive power was never explicitly set and the OpenDSS default PF was inherited throughout. Compare with I.PROV.DSS_DEFAULT_PF, which detects the exact 0.88 value per load; this finding detects the statistical signature across all loads.\nI.DIV.LOAD_IMBALANCE I A multi-phase load with >20 % spread between its phase setpoints — noteworthy unbalance (often intended; this is context, not criticism).\nI.DIV.LOAD_PHASE_BALANCED I Aggregate load across all phase terminals in a galvanic zone is balanced within 2 % (max − min spread relative to max). The network is effectively balanced and a single-phase equivalent model would suffice; the unbalanced OPF formulation adds no value here.\nI.DIV.LOAD_UNIFORM_MODEL I Across ≥3 loads, every load uses the same load model. When that model is constant_power (the default), no load exercises voltage dependence (ZIP/exponential) — the case does not test voltage-dependent load behaviour. Observational coverage signal, not a defect.\nI.DIV.LOAD_UNIFORM_CONFIG I Across ≥3 loads, every load shares the same configuration (e.g. all WYE) — no connection diversity. Observational; uniform connection is common and often legitimate.\nI.DIV.LINE_SYMMETRIC I ≥80 % of the lines sharing a linecode have lengths within ±10 % of the median — electrically near-identical sections.\nI.DIV.BUS_UNIFORM_VMIN I Every bus that has v_min has the same value — no spatial differentiation of the lower voltage envelope.\nI.DIV.BUS_UNIFORM_VMAX I Same for v_max.","title":"DIV — diversity & symmetry"},{"category":"section","location":"findings/#OPS-—-operational-loading","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nW.OPS.IMPORT_DEPENDENT W Local generation capacity below 5 % of total load — the case is a pure import feeder; with only a slack source the dispatch problem is loss minimisation at best.\nW.OPS.XFMR_OVERLOADED W Estimated downstream apparent load exceeds 90 % of a transformer's rating at nominal setpoints — little OPF headroom, or a rating entered on the wrong base (see the regulator/autotransformer discussion in methodology).\nW.OPS.LINE_UNCONSTRAINED W Lines without any thermal limit (i_max/s_max on the line or its linecode) — the OPF will have no flow constraints there.\nI.OPS.UNLOADED_PHASE I A phase terminal is present on buses in a galvanic zone (connected via lines and closed switches; transformers are boundaries) but no load connects to it anywhere in that zone. Reported per zone and per terminal. Common in partial-phase feeders; worth reviewing before interpreting unbalance results.\nI.OPS.FEEDER_LONG I A galvanic zone's electrical reach (longest Σ line length from its source/anchor bus to its farthest bus) exceeds the typical maximum feeder reach for its voltage band (LV/MV/HV/EHV, thresholds in [operational.feeder_length]). Either a genuinely long rural feeder worth noting for voltage-drop reasons, or a length-unit slip (km entered as m, or the OpenDSS default length=1).\nI.OPS.FEEDER_SHORT I A galvanic zone's electrical reach is below the typical minimum for its voltage band — electrically it is a stub, service drop, or substation interconnect rather than a feeder. Off by default for HV/EHV. Observational.","title":"OPS — operational loading"},{"category":"section","location":"findings/#PRE-—-infeasibility-pre-flight","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nE.PRE.VBOUND_CONFLICT E A bus voltage bound pair with lower > upper (checked elementwise for all four flavours: v, vpn, vpp, vpos). The feasible set is empty before the solver starts.\nE.PRE.PBOUND_CONFLICT E Generator p_min > p_max — infeasible by construction.\nE.PRE.QBOUND_CONFLICT E Generator q_min > q_max — same.\nI.PRE.NO_VOLT_BOUNDS I Buses with no voltage bounds at all — voltages are unconstrained there (spec semantics for absent optional bounds).\nI.PRE.SINGLE_SOURCE I Exactly one voltage source. The spec requires this in the current version; operationally it is still a single point of failure worth knowing about.\nW.PRE.SOURCE_VOLTAGE_OOB W A voltage source setpoint (v_magnitude) falls outside the bus's declared v_min/v_max. The source pins that voltage as a hard equality in the OPF, so the bound is trivially violated before the solver starts — a guaranteed infeasibility. Common cause: v_magnitude set in kV while bounds are in V, or an augmented bound tighter than the actual supply voltage.\nW.PRE.SOURCE_BUS_GENERATOR W A generator without p_max/q_max sits at a voltage-source bus. The voltage source is itself the network's current slack, so two unbounded current injections share one fixed-voltage bus — the dispatch split is degenerate (non-unique). Remove the generator and express its role as flow bounds/cost on the voltage source instead.\nI.PRE.SOURCE_BUS_GENERATOR I A bounded generator sits at a voltage-source bus. Well-posed (the generator is bounded, the source takes the remainder), but if its bounds/cost are meant to limit or price grid import, set them on the voltage source (p_min/p_max/q_min/q_max/cost) instead.","title":"PRE — infeasibility pre-flight"},{"category":"section","location":"findings/#DOM-—-domain-plausibility","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nE.DOM.VMIN_NEGATIVE E A negative per-phase entry in v_min — magnitudes are nonnegative by definition. (v_min is a per-phase array, phase-to-ground.)\nE.DOM.VMAX_NONPOSITIVE E A per-phase v_max entry ≤ 0 — forces zero voltage; almost certainly a unit/typo error.\nE.DOM.VNMAX_NEGATIVE E Negative vn_max (the optional, maximum-only neutral-to-ground cap).\nE.DOM.NEGATIVE_VALUE E Negative value in an inherently nonnegative field (length, diagonal resistance).\nW.DOM.LOAD_PF_LOW W Load power factor below 0.70 — plausible but unusual for aggregated demand; often a P/Q unit mix-up.\nW.DOM.GEN_COST_NEGATIVE W Negative generation cost — the optimizer will dispatch it to its bound; verify it is intended (e.g. must-run subsidy).\nW.DOM.GEN_COST_HIGH W Cost above 10 $/kWh — beyond any realistic tariff; suspect units.\nW.DOM.COST_PHASE_NONUNIFORM W A dispatchable element (generator or voltage_source) has a per-phase cost vector whose entries differ across phases. Costs are normally a single $/W price applied symmetrically; a non-uniform vector is more often a data-entry slip than an intended per-phase price signal. Scalar costs are uniform by definition and never flag.\nW.DOM.LC_ZERO_R W Near-zero or negative self-resistance on any linecode diagonal — a superconducting conductor, usually a placeholder.\nE.DOM.XFMR_VREF_INVALID E A transformer has v_ref_from ≤ 0 or v_ref_to ≤ 0. The turns ratio N = v_ref_from / v_ref_to is undefined or infinite; the OPF cannot be built. Usually caused by a missing field defaulting to zero or a unit error (kV entered as 0.0).\nW.DOM.XFMR_RATIO_OOB W Direction-agnostic transformer step ratio max(r, 1/r) above 1000:1. Calibrated so standard distribution step-downs (e.g. 11 kV/433 V ≈ 25:1) do not flag.\nW.DOM.ZERO_LIMIT W An i_max/s_max entry exactly 0. Read literally this forces zero flow; in source tools 0 usually means \"no limit\" — classic semantic abuse. Drop the field instead.\nW.DOM.ZERO_LENGTH W A zero-length line — degenerate impedance; the spec's lossless switch object is the right model for such sections (ref. 2).\nW.DOM.ANGLE_UNITS W A source v_angle entry with magnitude > 2π — angles are radians in the data model; this is almost certainly degrees.\nW.DOM.SOURCE_V_NEAR_BOUND W A voltage source's fixed v_magnitude sits within source_v_margin_frac (default 5 %) of the v_max − v_min band from either bound, on its own bus or a same-voltage-base neighbour (reachable via lines/switches; transformers are not crossed). The source pins that voltage as a hard equality, so little headroom remains and the OPF risks infeasibility. The stricter sibling W.PRE.SOURCE_VOLTAGE_OOB fires when the setpoint is already outside the bounds; this one warns before it crosses.\nW.DOM.SHUNT_ON_GROUNDED W A shunt connects to a terminal whose voltage is pinned to 0 V — either declared in the bus's perfectly_grounded_terminals or the neutral of a voltage-source bus (pinned to system ground). The shunt then draws I = G·V = 0 current and is completely inert. Usually a redundant element, or a sign that impedance grounding was intended where a hard V=0 ground was actually declared.\nI.DOM.NEGATIVE_LOAD I Loads with negative p_nom — embedded generation hiding as negative load; skews adequacy statistics and dodges the generator model.\nW.DOM.LINE_LOW_IMPEDANCE W A line whose absolute series impedance ‖Z‖F = ‖(R+jX)‖F × length is below 10⁻⁴ Ω. Near-zero impedance makes the KVL constraint nearly rank-deficient; model the section as a switch instead.\nW.DOM.LINE_IMPEDANCE_SPREAD W The worst adjacent-line ‖Z‖_F ratio (two lines sharing an interior bus, excluding voltage-source, transformer, and switch buses) exceeds 10⁵. At this contrast the NLP Jacobian loses roughly 5 decimal digits of precision; consider per-unit scaling or network reformulation.\nI.DOM.LINE_IMPEDANCE_SPREAD I Same as above but ratio is between 10³ and 10⁵ — common at MV/LV boundaries and usually benign, but worth reviewing if solvers struggle to converge. The result dict key max_adjacent_impedance_ratio always carries the worst observed value.\nE.DOM.INV_P_BOUNDS E Inverter p_min > p_max — the active-power box is empty; infeasible by construction.\nE.DOM.INV_Q_BOUNDS E Inverter q_min > q_max — the reactive-power box is empty.\nE.DOM.INV_SMAX_NONPOSITIVE E Inverter s_max has a non-positive entry — the apparent-power circle is empty, so no operating point exists.\nW.DOM.INV_BOUND_EXCEEDS_SMAX W An inverter P or Q box-bound magnitude exceeds s_max — that box bound can never bind because the apparent-power circle dominates; usually a units or sizing mistake.\nW.DOM.INV_PV_ABSORBS W A prime_mover=PV inverter has p_min < 0, i.e. it is allowed to absorb real power — physically implausible for PV; usually a sign error.","title":"DOM — domain plausibility"},{"category":"section","location":"findings/#LOAD-—-load-model-validation-and-analysis","page":"Finding-code reference","text":"Emitted by domain_rules_check (coefficient plausibility, DOM pass) and load_model_analysis (load_models pass).","title":"LOAD — load model validation & analysis"},{"category":"section","location":"findings/#Validation-(domain-rules)","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nE.LOAD.VNOM_MISSING E A voltage-dependent load (model ≠ constant_power) has no v_nom field. The reference voltage is required to evaluate any voltage-dependent power expression; the OPF cannot be constructed without it.\nE.LOAD.VNOM_ARITY E v_nom is an array whose length is neither 1 nor the number of sub-loads. Each entry must broadcast to exactly one sub-load.\nE.LOAD.VNOM_NONPOSITIVE E One or more v_nom entries are ≤ 0. Voltages are strictly positive; a non-positive value is unphysical and would produce division by zero in the OPF.\nE.LOAD.ZIP_ARITY E A ZIP coefficient array (alpha_z/i/p or beta_z/i/p) has length that is neither 1 nor the number of sub-loads.\nE.LOAD.EXP_ARITY E gamma_p or gamma_q has length that is neither 1 nor the number of sub-loads.\nW.LOAD.ZIP_SUM W For a ZIP load, the active (alpha^Z + alpha^I + alpha^P) or reactive (beta^Z + beta^I + beta^P) coefficients do not sum to 1. At nominal voltage the load will not consume its nominal power; usually a data entry error.\nW.LOAD.GAMMA_NEGATIVE W An exponential exponent gamma  0 — power increases as voltage falls. Physically possible for some device classes but extremely unusual in distribution-network demand models; almost always a sign error.\nW.LOAD.MODEL_MIXED W A zip load carries gamma_p/gamma_q fields, or an exponential load carries ZIP coefficient fields. The extra fields are ignored; this finding flags the likely copy-paste error.\nW.LOAD.VNOM_MISMATCH W A load's v_nom differs from the BFS-inferred bus nominal voltage by more than 25 %. For WYE loads v_nom is compared against the inferred phase-to-neutral voltage; for DELTA loads against phase-to-neutral × √3 (line-to-line). A large deviation means the power setpoint and voltage sensitivity are referenced to the wrong operating point — a common OpenDSS conversion error where the load kV field is left at a default or is set to the wrong voltage level.\nI.LOAD.GAMMA_RANGE I An exponential exponent gamma notin (0 2) — outside the range typical of distribution loads (motors ≈ 0.08, constant-impedance = 2). Still valid; flagged as context.\nI.LOAD.MODEL_FIELDS_IGNORED I A constant_power, constant_current, or constant_impedance load carries ZIP or exponential coefficient fields. These fields are redundant for named degenerate models and will be ignored by the OPF.","title":"Validation (domain rules)"},{"category":"section","location":"findings/#Analysis-(load-model-pass)","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nI.LOAD.EXP_ZIP_EQUIVALENT I One or more exponential loads have all exponents in 0 1 2. These can be represented losslessly as zip (or the named constant_power/constant_current/constant_impedance models), keeping the OPF quadratic. The loads detail key lists the affected load IDs.\nW.LOAD.NL_NO_VMIN W One or more voltage-dependent loads sit on buses without any lower voltage magnitude bound (v_min, vpn_min, or vpp_min). The OPF squared-voltage variable W will rely on the default floor bound (05V^textnom) rather than an engineering limit. For loads with gamma  2 or alpha^Ialpha^Z neq 0 the power expression grows unboundedly as voltage falls; an explicit lower bound is strongly recommended.","title":"Analysis (load model pass)"},{"category":"section","location":"findings/#RED-—-redundancy","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nW.RED.ZERO_LOADS W Loads with p_nom = q_nom = 0 — electrically inert objects that still create variables/constraints.\nI.RED.LOAD_SPARSE_PHASES I WYE loads where at least one phase has p≈0 and q≈0 while another is active. Each dead phase still generates a current variable and two bilinear constraints in the OPF; splitting into per-phase SINGLE_PHASE loads eliminates them. SINGLE_PHASE and DELTA loads are excluded (no clean per-phase equivalent).\nI.RED.LOAD_MERGEABLE I Groups of loads on the same bus sharing the same configuration and terminal_map (WYE/SINGLEPHASE keys are phase-order-insensitive; DELTA keys are normalised to the smallest cyclic rotation). Each group can be collapsed into one load with summed `pnom/qnom. Loads withtimeseries` references are excluded (merging profiles is non-trivial).\nI.RED.ZERO_SHUNTS I Shunts whose every G/B matrix entry is zero — same.\nI.RED.MERGEABLE_LINES I Chains of series lines whose interior buses have line-degree 2 and no other attachment (loads, generators, shunts, switches, transformers all counted as blockers). Merging removes superfluous buses that slow solvers (ref. 2).\nI.RED.PARALLEL_LINES I Two or more lines sharing the same bus pair (direction-agnostic). Parallel lines are unusual in distribution networks and more commonly indicate a data conversion artefact than a genuine double-circuit feeder.\nI.RED.UNUSED_LINECODES I Linecodes never referenced by a line — a cable library shipped with the case; harmless, but distinguishes library data from network data.\nI.RED.DUPLICATE_LINECODES I Groups of linecodes with identical R/X_series_1_1 fingerprints (codes lacking impedance data are excluded — absence is not evidence of duplication).","title":"RED — redundancy"},{"category":"section","location":"findings/#PROV-—-provenance-and-conventions","page":"Finding-code reference","text":"The largest family; full derivations in the methodology notes.","title":"PROV — provenance & conventions"},{"category":"section","location":"findings/#Impedance-matrix-structure","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nE.PROV.NONRECIPROCAL E An impedance/admittance block (linecode R/X/G/B or bus-shunt G/B) is not symmetric — reciprocity is violated; passive RLC networks cannot do that. Catches, e.g., delta-bank admittances built from the incidence matrix instead of Y·(M∆)ᵀM∆.\nE.PROV.NONPASSIVE E The R block has a negative eigenvalue — the line would generate power. PSD of R is invariant under Kron reduction (Schur complements of accretive matrices stay accretive), so this is always an error.\nW.PROV.X_NONINDUCTIVE W Non-positive series self-reactance — series compensation does not exist inside linecodes; almost always a sign flip or X/B confusion.\nW.PROV.X_NOT_PSD W The X block has a negative eigenvalue — the implied inductance matrix is not realisable (energy argument; also Kron-invariant via sectorial Schur closure).\nI.PROV.NEGATIVE_MUTUAL_R I Negative off-diagonal resistance. Carson's earth-return term makes mutual R positive for geometry-derived matrices; a negative entry signals processed/fitted provenance.\nE.PROV.NEGATIVE_G E A conductance block (line shunt or bus shunt) that is not PSD / has negative diagonals — an active element.\nW.PROV.B_SIGN W A susceptance block that is not PSD or has negative diagonals — not a physical capacitance matrix.\nI.PROV.B_OFFDIAG I Positive mutual susceptance with PSD intact — deviates from the Maxwell sign pattern. Clean electrostatic pipelines (including grounded-screen elimination and bundling) preserve the pattern, so this marks fitted/averaged provenance rather than an error.","title":"Impedance matrix structure"},{"category":"section","location":"findings/#Parameterisation-provenance","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nI.PROV.SEQ_DERIVED I Exactly balanced impedance matrices (equal self, equal mutual): constructed from sequence parameters (r1,x1,r0,x0) or a transposition assumption — not from conductor geometry. The implied Z₁/Z₀ are recovered and reported.\nI.PROV.DECOUPLED_PHASES I Diagonal impedance matrices — positive-sequence-only data; the phases decouple into independent single-phase networks (maximal redundancy/symmetry).\nI.PROV.LINE_SWITCH_LIKE I A line has near-zero series impedance and may be better represented by the spec's lossless switch object.","title":"Parameterisation provenance"},{"category":"section","location":"findings/#Bound-and-limit-completeness","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nE.PROV.INCONSISTENT_BOUNDS E A bus has a voltage-bound pair with min > max — the feasible set is empty (the provenance-pass counterpart of the pre-flight E.PRE.VBOUND_CONFLICT).\nI.PROV.OVERLAPPING_VOLTAGE_BOUNDS I A bus has several voltage-bound types active at once (e.g. both v and vpn) — overlapping envelopes; confirm they are meant to co-apply.\nW.PROV.REDUNDANT_VOLTAGE_BOUNDS W A bus declares both phase-to-ground (v_min/v_max) and phase-to-neutral (vpn_*) bounds that encode the same limit — redundant duplication.\nW.PROV.INAPPLICABLE_VOLTAGE_BOUNDS W A bus carries voltage bounds that cannot be enforced for its terminal structure and will be ignored by the OPF (e.g. a phase-to-neutral bound on a bus with no neutral).\nW.PROV.I_MAX_INCOMPLETE W One or more lines have fewer i_max entries than conductors — the thermal limit is only partially specified; the unspecified conductors are left unconstrained.\nW.PROV.I_MAX_INCOMPLETE_SWITCH W Same as above for switches.\nW.PROV.I_MAX_INCOMPLETE_XFMR W Same for transformers (i_max_from/i_max_to shorter than the winding conductor count).\nW.PROV.I_MAX_ABSENT W One or more lines have no i_max on their linecode (or no linecode) — the series current is left entirely unconstrained in the OPF, so no thermal limit is enforced on the branch at all (distinct from I_MAX_INCOMPLETE, which is a partial limit).\nW.PROV.I_MAX_ABSENT_SWITCH W Same for closed switches with no i_max. Open switches are excluded — their current is fixed to zero regardless.","title":"Bound & limit completeness"},{"category":"section","location":"findings/#Zone-phase-topology","page":"Finding-code reference","text":"Emitted by the :connectivity pass after classifying each galvanic zone. Informational tags, not defects.\n\nCode Sev Trigger & rationale\nI.PROV.SPLIT_PHASE_ZONE I A galvanic zone is fed by a center_tap transformer — a split-phase section (NA 120-0-120, AU 230-0-230 downstream of SWER). The two legs are anti-phase about the centre-tap neutral; the OPF warm-start initialises them 180° apart accordingly.\nI.PROV.SWER_ZONE I A galvanic zone is single-wire (one phase conductor across all its buses) and transformer-isolated — a Single-Wire-Earth-Return section. Distinguished from a single-phase lateral, which shares its three-phase feeder's zone.","title":"Zone phase topology"},{"category":"section","location":"findings/#Grounding-and-reduction-conventions","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nI.PROV.KRON_LIKELY I A 3-wire LV level (LV is physically 4-wire); the neutral was probably Kron-eliminated under an every-bus-grounded assumption. 3-wire MV is physical and never flags.\nI.PROV.KRON_REDUCIBLE I A 4-wire network whose every neutral is perfectly grounded — Kron reduction would be exact, so the explicit neutrals are numerically redundant.","title":"Grounding & reduction conventions"},{"category":"section","location":"findings/#Impedance-transformation-type-(3-wire-LV-only)","page":"Finding-code reference","text":"When a 3-wire LV network is detected, the structure of each linecode's R and X blocks is compared against three known impedance-transformation signatures from Geth, Heidari & Koirala (ACM e-Energy 2022, doi:10.1145/3538637.3538844):\n\nCode Sev Trigger & rationale\nI.PROV.IMPEDANCE_TRANSFORM_KR I Kron-reduced: R and/or X off-diagonals are non-uniform (distinct matrix structure) and/or Rmutual/Rself ≪ 0.5. The neutral row/column was eliminated from the original Carson 4-wire matrix via Schur complement. Exact when every neutral is perfectly grounded; approximate otherwise. Zero-sequence behaviour is not captured.\nI.PROV.IMPEDANCE_TRANSFORM_PN I Phase-to-neutral approximation: R block is circulant (all diagonals equal, all off-diagonals equal) with mutual ≈ ½ self; X block retains the original geometric structure (off-diagonals vary). Neutral resistance has been folded into phase self-terms. Valid approximation for equal phase/neutral conductor resistance; error grows with grounding impedance.\nI.PROV.IMPEDANCE_TRANSFORM_MPN I Modified phase-to-neutral approximation: both R and X blocks are circulant with mutual ≈ ½ self. X is further symmetrised relative to the standard phase-to-neutral form, introducing additional modelling error, particularly for asymmetric cable geometries.\nW.PROV.IMPLICIT_GROUNDING W Neutral terminals exist and are referenced by components, but no branch carries a neutral conductor — the dataset uses the implicit \"n = local ground\" convention. Made explicit so 4-wire consumers don't misread it.\nE.PROV.FLOATING_NEUTRAL E A neutral section (continuity graph over lines/closed switches) with no path to ground and loads/generators using it — the zero-sequence path is undefined; 4-wire analysis is ill-posed.\nW.PROV.FLOATING_NEUTRAL W Same, but unused — latent rather than active.\nI.PROV.WYE_NEUTRAL_UNGROUNDED I A three-phase wye winding brings out its star-point neutral at a bus that has no local grounding (no perfect ground and no grounding impedance). The wye star point is the natural earthing point; its zero-sequence potential is then set only by what the neutral conductor reaches elsewhere. Single-phase transformers (phase-to-neutral / phase-to-phase) are exempt.","title":"Impedance transformation type (3-wire LV only)"},{"category":"section","location":"findings/#OpenDSS-default-fingerprints","page":"Finding-code reference","text":"Values matching documented OpenDSS defaults indicate the source .dss files likely omitted the field (see methodology for the table).\n\nCode Sev Trigger & rationale\nW.PROV.DSS_DEFAULT_Z W Recovered Z₁/Z₀ match the default line constants (r1=0.058, x1=0.1206, r0=0.1784, x0=0.4047 Ω/kft) — a fictitious 60 Hz overhead line in your data.\nW.PROV.DSS_DEFAULT_SOURCE_Z W Source Thévenin Z₁ matches MVAsc3=2000 with X1R1=4 — the fault level was never specified; arguably the most consequential default of all.\nI.PROV.DSS_DEFAULT_AMPS I i_max exactly 400 A (the normamps default; 600 A is excluded as a common genuine rating).\nI.PROV.DSS_DEFAULT_XFMR I Transformer per-unit impedance equal to xhl = 7 % / %r = 0.2 per winding.\nI.PROV.DSS_DEFAULT_PF I Loads at power factor exactly 0.88 — reactive demand defaulted.\nI.PROV.DSS_DEFAULT_KV I Components sitting exactly at 115 kV or 12.47 kV — US defaults, glaring outside US test feeders.\nI.PROV.DSS_DEFAULT_LENGTH I A minority of lines with length exactly 1.0 among varied lengths. (Universal 1.0 is detected as a deliberate length-normalised convention and reported in the convention statement instead.)","title":"OpenDSS default fingerprints"},{"category":"section","location":"findings/#Control-devices","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nI.PROV.NO_PI_SHUNT I All linecodes have π-shunt admittance keys present but every entry is zero — the shunt capacitance/conductance of every cable was not populated. Line charging is absent from the model; this is common for short LV cables but is worth confirming for longer MV feeders.\nI.PROV.PARTIAL_PI_SHUNT I Some linecodes have non-zero π-shunt admittance and others do not — mixed shunt population. May be intentional (e.g. short spurs vs long trunk cables) but is worth reviewing for consistency.\nW.PROV.REGULATOR_PATTERN W A transformer that looks like a voltage-regulator/autotransformer encoding: either both windings on one bus (the explicit EPRI autotransformer form) or a near-1:1 wye unit with same-level endpoints / very low impedance / non-unity tap. The data model has no regulator object — a control device has been frozen into a fixed branch.","title":"Control devices"},{"category":"section","location":"findings/#INT-—-structural-integrity","page":"Finding-code reference","text":"Motivated by the benchmark-pitfall catalogue of (ref. 2).\n\nCode Sev Trigger & rationale\nE.INT.UNKNOWN_BUS E A component references a bus id that does not exist.\nE.INT.UNKNOWN_LINECODE E A line references a linecode that does not exist (distinct from unused linecodes).\nE.INT.UNKNOWN_TERMINAL E A terminal-map entry is not a terminal of the referenced bus — typos, or attempts to connect nodal elements directly to ground (forbidden by spec Table 10).\nE.INT.UNKNOWN_CONTROL_PROFILE E An inverter references a control_profile id that does not exist in the network's control_profile table.\nW.INT.DIM_MISMATCH W Terminal-map arity vs linecode matrix size, i_max length vs conductor count, setpoint length vs configuration, source vm/va vs map length.\nW.INT.PADDED_MATRIX W All-zero row/column pairs in linecode impedances — padded conductors demonstrably wreck NLP performance (22 → 590 Ipopt iterations in (ref. 2) Table 3); shrink the matrix and use terminal maps.\nE.INT.NO_VOLTAGE_REFERENCE E A galvanic island (transformer windings are separations) with no source, perfect grounding, or grounding shunt — voltages there are defined only up to a shift (the IEEE-123 \"bus 610\" rank deficiency (ref. 2)). A shunt counts only if its admittance has nonzero row sums, so a pure delta capacitor bank correctly does not anchor an island.\nW.INT.WYE_WITHOUT_NEUTRAL W A wye-configured load/generator at a bus with no identifiable neutral — implies an undeclared ground return; in 3-wire sections only delta connections are expected.\nW.INT.FLOATING_LOAD_TERMINAL W A load or generator references a phase terminal that no branch (line, switch, or transformer winding) uses on the same bus. The voltage at that terminal is decoupled from the rest of the network — KCL is trivially satisfied there and the power balance constraint is degenerate. Common cause: a 3-phase load connected to a 2-wire section, or a terminal number typo. Terminals at voltage-source buses and neutral terminals are excluded (sources pin voltages; neutrals are often grounded implicitly).\nW.INT.UNUSED_BUS_TERMINAL W A bus declares a terminal in terminal_names that is not referenced by any component at that bus (no branch end, load, generator, shunt, or voltage source uses it). The terminal adds a free voltage variable with no KCL constraint — pure numeric overhead. Almost always a conversion artifact or a missing connection. Voltage-source buses are excluded (the source pins every declared terminal regardless).\nW.INT.LOW_IMPEDANCE_LINE W Lines whose total series impedance is below 10⁻³× the network median — they degrade conditioning; the spec's lossless switch object is the intended model (ref. 2).\nI.INT.UNIFORM_GEN_COST I Groups of generators with identical cost vectors — any dispatch split among them is optimal (degeneracy); diversify costs for benchmark use.","title":"INT — structural integrity"},{"category":"section","location":"findings/#TMAP-—-terminal-map-conventions","page":"Finding-code reference","text":"Checks on how component terminal_maps reference bus terminals.\n\nCode Sev Trigger & rationale\nE.TMAP.PHASE_TO_NEUTRAL E A component's terminal_map contains no phase terminal (e.g. [\"n\"]) — it connects only to neutral, leaving no phase to inject into or draw from.\nI.TMAP.CROSS_PHASE_LINE I A line or switch has different from/to terminal maps — the conductors are cross-connected between phases across the branch. Valid (e.g. an intentional phase swap) but flagged as context.\nI.TMAP.PERMUTED_ORDER I A component's terminal_map is a permutation of the bus's nodal terminal order — non-canonical ordering; verify the swap is deliberate rather than a data-entry slip.","title":"TMAP — terminal-map conventions"},{"category":"section","location":"findings/#SPEC-—-TF-spec-conformance","page":"Finding-code reference","text":"Rules the JSON Schema cannot express.\n\nCode Sev Trigger & rationale\nW.SPEC.N_SOURCES W Voltage-source count ≠ 1 (spec Eq. 17 requires exactly one in this version).\nW.SPEC.BAD_CONFIG W A configuration string outside SINGLE_PHASE/WYE/DELTA.\nW.SPEC.CONFIG_ARITY W Terminal-map arity inconsistent with the configuration (SINGLE_PHASE = 2, WYE = 4, DELTA = 3).\nE.SPEC.DUPLICATE_TERMINAL E A component's terminal_map (or terminal_map_from/terminal_map_to for lines/switches) contains the same terminal label more than once — a degenerate connection that collapses two distinct conductors onto one.\nI.SPEC.LOAD_PHASE_TO_PHASE I A SINGLE_PHASE load/generator whose two terminals are both phase conductors (neither is the bus neutral) — a phase-to-phase (delta-connected) single-phase element. Valid per spec; flagged as context because the modelling is distinct from the more common phase-to-neutral case.\nE.SPEC.WYE_MISSING_NEUTRAL E A WYE load/generator whose last terminal is not the neutral of its bus — the return path is not the neutral conductor, which violates the spec's WYE connection semantics.\nE.SPEC.WYE_DUPLICATE_PHASE E A WYE load/generator has duplicate phase terminals in the non-neutral slots.\nE.SPEC.DELTA_HAS_NEUTRAL E A DELTA load/generator includes the bus neutral in its terminal map — delta elements must be phase-to-phase only.\nE.SPEC.DELTA_DUPLICATE_PHASE E A DELTA load/generator has duplicate phase terminals.\nW.SPEC.XFMR_TMAP_ARITY W Transformer terminal-map lengths off the per-subtype spec values — also the deliberate tripwire for unconverted wye-wye units.\nW.SPEC.INV_TOPOLOGY W An inverter topology outside the spec-allowed set (FOUR_LEG/THREE_LEG/SINGLE_PHASE).\nW.SPEC.INV_TMAP_ARITY W An inverter's terminal_map length does not match the arity its topology requires.\nW.SPEC.INV_PRIME_MOVER W An inverter prime_mover is outside the spec-allowed set.\nW.SPEC.TERMINAL_TYPES W The source file used non-string terminal identifiers; they were coerced at parse (aliases or verbatim — the finding says which).\nI.SPEC.MATRIX_TRIANGULAR I Impedance matrices stored upper-triangular; the spec defines full row-first storage. Read fine; normalise before publishing.","title":"SPEC — TF-spec conformance"},{"category":"section","location":"findings/#SOL-—-solution-profiling","page":"Finding-code reference","text":"Produced by profile_solution when checking an OPF result dict against its network. See SolutionReport and render_solution.\n\nCode Sev Trigger & rationale\nE.SOL.INFEASIBLE E Solver termination status is not LOCALLY_SOLVED, OPTIMAL, or ALMOST_LOCALLY_SOLVED. All subsequent bound and residual checks are skipped.\nE.SOL.NAN_IN_RESULT E One or more numeric fields in the result dict contain NaN or Inf. Indicates a solver failure or extraction bug even when the termination status appears feasible.\nE.SOL.VOLT_VIOLATION E A bus terminal voltage magnitude (vm, vpn, vpp, or sequence component) lies outside its declared bound.\nW.SOL.VOLT_ACTIVE W A voltage magnitude is within 1 % of its bound — the constraint is near-active (binding at the tolerance level).\nE.SOL.THERMAL_VIOLATION E A line or switch current magnitude exceeds the component's or linecode's i_max limit.\nW.SOL.THERMAL_ACTIVE W Current magnitude is within 1 % of i_max — the thermal limit is near-active.\nE.SOL.GEN_VIOLATION E A generator's active or reactive dispatch (pg/qg per terminal) falls outside its declared p_min/p_max/q_min/q_max bounds.\nW.SOL.GEN_ACTIVE W Generator dispatch is within 1 % of a bound — the bound is near-active.\nE.SOL.INV_VIOLATION E An inverter's solved pg/qg (per phase) falls outside its declared p_min/p_max/q_min/q_max bounds.\nW.SOL.INV_ACTIVE W An inverter dispatch is within 1 % of a P bound — the bound is near-active.\nW.SOL.INV_PF_DEVIATION W A constant-power-factor inverter's solved operating point deviates from its commanded PF beyond tolerance — the PF-coupling constraint residual is non-trivial.\nW.SOL.LOAD_RESIDUAL W For a constant_power load, solved pd/qd differs from p_nom/q_nom by more than 1 W / 1 var — the bilinear constant-power constraint has a non-trivial residual; the solver may not have converged tightly. Not emitted for voltage-dependent models (where pd ≠ p_nom is expected).\nW.SOL.LOAD_MODEL_RESIDUAL W For a voltage-dependent load, the realised pd/qd is inconsistent with what the load model predicts at the solved terminal voltage by more than 1 W / 1 var. Indicates the load model constraint was not satisfied — a solver convergence or result extraction issue.\nI.SOL.LOAD_VD_SUMMARY I Aggregate realised vs nominal P/Q across all voltage-dependent sub-loads. Quantifies the total demand shift due to voltage sensitivity at the solved operating point.\nW.SOL.POWER_BALANCE W Network-wide active power balance error (Σpg − Σpd − Σp_loss) exceeds 1 % of total load — a significant mismatch that may indicate a lossy model, a missing component, or a result extraction issue.\nI.SOL.BINDING_SUMMARY I Summary count of violated and near-active bounds across all categories (voltage, thermal, generator). Always emitted for feasible solutions.\nI.SOL.LOSS_FRACTION I Line losses exceed 20 % of total generation — unusually high; may indicate a high-impedance feeder, a model issue, or an extreme operating point.\nW.SOL.NEG_LOSS W A line or transformer dissipates negative active power (p_loss < 0) beyond numerical noise — non-physical for a passive branch, which cannot generate active power. Tolerance is throughput-relative (p_loss < −max(1 W, 1e-4·|S_through|)). Signals a non-converged / ill-conditioned solution, a sign error, or a negative-resistance input. Reactive loss is excluded, as line charging / capacitive shunts make q_loss legitimately negative.\nI.SOL.NEUTRAL_SHIFT I Maximum neutral terminal voltage magnitude across all buses, with the bus identifier. Non-zero neutral shift indicates load unbalance or grounding impedance.\nW.SOL.INIT_LEVEL_MISMATCH W One or more terminals have vm_init / vm_solved outside [0.1, 10] — the initialisation used the wrong voltage level (e.g. source voltage applied to an LV bus via flat warm-start). Solver may still converge but local-minimum risk is elevated. Only emitted when result[\"initialisation\"] is present.\nW.SOL.INIT_LARGE_ERROR W One or more phase terminals have an initialisation error exceeding 20 % of the solved voltage magnitude — the start point was a poor approximation of the solution.\nI.SOL.INIT_NEUTRAL_NONZERO I One or more neutral terminals were initialised with non-zero voltage. Neutral start values should be zero; non-zero values indicate an initialisation inconsistency.","title":"SOL — solution profiling"},{"category":"section","location":"findings/#BENCH-—-benchmark-readiness","page":"Finding-code reference","text":"Code Sev Trigger & rationale\nI.BENCH.AUGMENTATION I The case is not yet a non-trivial OPF benchmark; the message lists the concrete augmentation steps: no costed generation (degenerate objective), slack-only generation (trivial dispatch), absent voltage bounds, absent vpn/vpos bounds (which also aid solver robustness (ref. 3)), missing thermal limits.\nW.BENCH.GEN_NO_DOF W One or more generators have p_min ≈ p_max on every phase — fixed output, not dispatchable. These generators consume variables and constraints but cannot move in the optimal solution, and may mask the true binding constraints.\nW.BENCH.GEN_ZERO_COST W One or more dispatchable generators (p_max > p_min on at least one phase) have a cost vector of all zeros — the objective is flat in their dispatch direction, making the optimal solution primal non-unique. Assign a non-zero cost to each dispatchable unit.\nW.BENCH.GEN_DEGENERATE_COST W Two or more dispatchable generators on the same bus or one line/switch hop apart share an identical cost coefficient. The solver can redistribute power between them freely without changing the objective, producing primal non-uniqueness and benchmarks that are sensitive to solver tolerances.\nI.BENCH.LOAD_ZERO_PNOM I One or more loads have p_nom = 0 on all phases — they impose no real power demand and are electrically inert. These loads may indicate missing data or placeholder entries that should be populated before benchmark use.","title":"BENCH — benchmark readiness"},{"category":"section","location":"validation/#Validating-the-OPF-engine","page":"Validating the OPF","text":"BMOPFTools ships a reference nonlinear four-wire IVR-EN OPF (solve_opf) used to validate cases and profile solutions. A reference solver is only as useful as its own validation, so this page documents how that engine is tested — both so you can judge the results and so you can reuse the setup for your own tool. \n\nThe goal of our implementation is to be consistent with generally accepted circuit modeling practice in the presence of nonlinear elements, including but not limited to constant power loads. We generally trust OpenDSS's implementation correctness, however, modelers may disagree on what appropriate models are. In this context, we recommend to study the paper \"A perspective on transformer  modeling for distribution system analysis\", with full bibliographic details below. \n\nTwo questions every distribution-OPF engine must answer, and the suite tests each separately:\n\nIs the solution a valid power flow? — feasibility: do the solved voltages and currents actually satisfy Kirchhoff's laws and the component models? We check this against OpenDSS.\nIs it the optimum? — optimality: given the objective and the operating bounds, does the optimizer reach the right dispatch? We check this against closed-form solutions and against PowerModelsDistribution (PMD).  Note that we don't claim global optimality, just local. \n\nThe philosophy is the one stated in the OPF formulation: the accuracy of the network physics matters more than any single objective, so feasibility is validated exhaustively and component-by-component, while optimality is pinned to independently-derived optima.\n\nIf you are building your own engine there are two ways to reuse this: adopt the harness (the .dss cases and the comparison method), or adopt the locked-in numbers (the objective and voltage baselines tabulated below) as regression targets. The test files are the source of truth; the tables here reproduce their headers.","title":"Validating the OPF engine"},{"category":"section","location":"validation/#Feasibility-—-agreement-with-OpenDSS","page":"Validating the OPF","text":"Source: test/powerflow_comparison_tests.jl (24 testsets) over the 18 OpenDSS cases in test/data/pf_comparison/.","title":"Feasibility — agreement with OpenDSS"},{"category":"section","location":"validation/#The-method:-a-power-flow-expressed-as-a-feasibility-OPF","page":"Validating the OPF","text":"There is no separate power-flow solver to validate — instead each case is solved with solve_feasibility_opf, which adds an elastic slack current at every non-source terminal and minimises its squared norm. When the optimal slack is ≈ 0, every KCL/KVL and component-model constraint is satisfied exactly, so the solution is a valid power flow:\n\nres     = solve_feasibility_opf(net; optimizer = Ipopt.Optimizer)\nslack_A = res[\"total_slack_magnitude_A\"]\n@test slack_A < 1e-3          # the network balances with no injected slack\n\nThe same network is then solved in OpenDSS through OpenDSSDirect.jl, and the complex node voltages are compared one-to-one. OpenDSS is solved live at test time — there are no stored golden voltages; the .dss files themselves are the immutable baseline (see Voltage source as current slack for how the source/slack convention is matched).\n\nThe node-name mapping bridges the two conventions:\n\nOpenDSS node BMOPF terminal\nbus.1 / bus.2 / bus.3 1 / 2 / 3 (phases)\nbus.4 n (neutral)\nbus.0 earth reference — skipped","title":"The method: a power flow expressed as a feasibility OPF"},{"category":"section","location":"validation/#Tolerances-and-their-rationale","page":"Validating the OPF","text":"The voltage comparison (_cmp_volts) uses, verbatim from the test header:\n\nCase class atol rtol Rationale\nLines, lossless / lightly-loaded transformers 1.0 V 1e-3 sub-volt agreement on a ~240 V base\n3-phase transformers, realistic losses, ≥ 70 % loading 0.1 V 1e-3 at 75 % on a 500 kVA / 0.415 kV unit the LV-side series drop is ≈ 10 V, so 0.1 V demands < 1 % model agreement — any wrong sign, missing factor, or wrong impedance side shows up as > 1 V\n\nTwo further checks back up the voltage match: the feasibility slack (< 1e-3 A), and total transformer losses against OpenDSS (rtol = 0.05). The tolerances are deliberately tight enough to catch a single modelling error, not merely \"close\".","title":"Tolerances and their rationale"},{"category":"section","location":"validation/#What-the-cases-isolate","page":"Validating the OPF","text":"Each .dss file is a minimal 2-bus-style fixture that isolates one modelling concern, so a failure points straight at the responsible component:\n\nCase file Isolates\npf_1ph_line, pf_3ph_line single- and three-phase line series impedance, unbalanced 4-wire loads\npf_1ph_freeneutral, pf_1ph_impedanceneutral, pf_1ph_perfectneutral neutral grounding: floating, grounding-reactor (Z = 0.2 Ω), and perfectly grounded\npf_zip_1ph, pf_zip_3ph, pf_zip_delta, pf_exp_1ph, pf_delta_load voltage-dependent load models — ZIP (distinct P/Q fractions), exponential (γ), and delta (line-to-line) connection\npf_1ph_xfmr, pf_yd_xfmr, pf_dy_xfmr, pf_center_tap_xfmr transformer windings/vector groups: YY, Yd, Dy, and split-phase center-tap\npf_autotransformer, pf_open_delta_reg step-voltage regulators / autotransformers and open-delta (ABBC) regulation\npf_pv_1ph, pf_pv_4leg inverter current injection at a pinned dispatch — single-phase and FOUR_LEG\n\nSingle-phase, FOURLEG per-phase, and FOURLEG AVERAGE-reference smart-inverter droop are additionally validated against OpenDSS InvControl (deadband, slope, and saturation regimes) within the same file.","title":"What the cases isolate"},{"category":"section","location":"validation/#Reusing-the-feasibility-setup","page":"Validating the OPF","text":"The .dss files are portable and engine-agnostic: point your own solver at a case, solve the same .dss in OpenDSS for the reference voltages, and compare node-by-node with the mapping and tolerances above. No BMOPFTools dependency is needed to reuse the baseline.","title":"Reusing the feasibility setup"},{"category":"section","location":"validation/#Optimality-—-agreement-with-known-optima","page":"Validating the OPF","text":"Feasibility proves the physics; optimality proves the optimizer reaches the right point. Three complementary tiers, each with a different source of truth.","title":"Optimality — agreement with known optima"},{"category":"section","location":"validation/#Tier-1-—-closed-form-analytic-targets","page":"Validating the OPF","text":"Source: test/opf_tests.jl. Tiny networks whose optimum is solvable by hand, so the target is exact rather than borrowed. For a single-phase resistive feeder the load-bus voltage is the root of the power-flow quadratic\n\nV = fracV_s + sqrtV_s^2 - 4RP2\n\nand dispatch/cost tests pin the objective directly:\n\nTest Analytic target Tolerance\nT1 — single-phase resistive V = (V_s + √(V_s² − 4RP))/2 ≈ 947.214 V 0.01 V\nT3 — forced generator dispatch V ≈ 974.342 V, objective = cost·P_gen 0.01 V / 0.1\nT4 — negative cost ⇒ p_max binding each phase at p_max = 50 kW, objective = −3·P_max 1.0 W / 10\nT5 — power-balance identity P_source = P_load + P_line_loss 0.1 W\nT10 — sequence voltage bounds tight vneg_max/vzero_max ⇒ balanced V_phase = vpos_max 5.0 V\n\nThe suite also pins per-unit ⇄ SI agreement, immutability of the input network, and the result-dictionary contract (see the result schema).","title":"Tier 1 — closed-form analytic targets"},{"category":"section","location":"validation/#Tier-2-—-golden-objectives-ported-from-PowerModelsDistribution","page":"Validating the OPF","text":"Source: test/pmd_opf_port_tests.jl. These fixtures rebuild PMD's small OPF test networks directly in BMOPF's native schema (no PMD dependency) and assert the published PMD objective — the slack injection summed over phases, reported as voltage_source[...][\"ps\"|\"qs\"]. The 2/3-bus cases target PMD's IVRUPowerModel (the formulation closest to BMOPF's current-voltage engine); the 4/5-bus and delta cases target PMD's AC formulations, whose objectives are formulation-independent.\n\nLocked-in baseline (reuse these as regression targets; atol = 1e-2 kW/kvar unless noted):\n\nCase Σ pg (kW) Σ qg (kvar) PMD reference\n2-bus diagonal 18.209 0.208 opf_iv.jl (IVR)\n3-bus balanced 18.345 9.194 opf_iv.jl (IVR)\n3-bus unbalanced 21.4812 9.27263 opf_iv.jl (IVR)\n4-bus phase drop 18.2632 9.02334 opf.jl (AC) — also pins load-bus voltages\n5-bus phase drop 59.9363 −33.5395 opf.jl (AC) — also pins mid-bus voltages\n3-bus delta + ZIP 42.0464 18.1928 opf.jl (AC) — atol = 1.0 kW/kvar, voltage-dependent\n\nEach fixture rebuilds the network from the underlying OpenDSS source that PMD ships (case{2_diag,3_balanced,3_unbalanced,4_phase_drop,5_phase_drop,3_unbalanced_delta_loads}.dss), so the chain is OpenDSS case → BMOPF schema → BMOPF OPF → PMD objective.","title":"Tier 2 — golden objectives ported from PowerModelsDistribution"},{"category":"section","location":"validation/#Tier-3-—-droop-optimization-correctness","page":"Validating the OPF","text":"Source: test/volt_var_watt_tests.jl. The smart-inverter Volt-var / Volt-watt droop is encoded as constraints inside the OPF (see Smooth droop encoding and the VVWO tutorial). These tests check the optimizer lands on the droop curve in every regime:\n\nVolt-watt — active power binds to the curtailment cap at the solved voltage (atol = 5 W) once the droop region is active (V > 253 V);\nVolt-var — reactive power follows the four-point characteristic, absorbing near V4 (atol = 5 var) and ≈ 0 in the deadband (|Q| < 30 var);\nvoltage reference — PER_PHASE gives three distinct per-phase Q's on an unbalanced bus while AVERAGE gives one common Q (the contrast the VVWO tutorial's appendix shows), plus the exact-vs-smooth encoding agreement as the corner-smoothing ε → 0 (atol = 2e-3).","title":"Tier 3 — droop optimization correctness"},{"category":"section","location":"validation/#Reusing-this-for-your-own-tool","page":"Validating the OPF","text":"You want to … Use What it proves\nvalidate your power-flow / component models the .dss cases + a live OpenDSS solve, compared as above the network physics is correct\nvalidate your optimizer the Tier-1 analytic targets and the Tier-2 PMD objective table as regression checks the optimizer reaches the true optimum\nvalidate smart-inverter control the Tier-3 droop setpoints control laws are enforced as modelled\n\nThe two reuse paths are complementary: the analytic and PMD numbers are self-contained baselines (copy the table, no dependency), while the .dss feasibility cases need an OpenDSS solve to regenerate the reference — which is the point, since OpenDSS is the de-facto distribution power-flow oracle.","title":"Reusing this for your own tool"},{"category":"section","location":"validation/#Running-the-suite","page":"Validating the OPF","text":"The OPF and OpenDSS tests are optional dependencies, guarded in test/runtests.jl:\n\nthe optimality tests (opf_tests.jl, pmd_opf_port_tests.jl, volt_var_watt_tests.jl) run when JuMP + Ipopt are present (_HAS_JUMP_IPOPT);\nthe feasibility comparison (powerflow_comparison_tests.jl) additionally needs OpenDSSDirect.jl (_HAS_ODS).\n\nWhen a dependency is absent the relevant testset is @test_skip-ped rather than failed, so the core suite runs anywhere. Run everything from the package root:\n\njulia --project=. -e \"using Pkg; Pkg.test()\"","title":"Running the suite"},{"category":"section","location":"validation/#refs-validation","page":"Validating the OPF","text":"D. M. Fobes, S. Claeys, F. Geth, C. Coffrin, PowerModelsDistribution.jl: An open-source framework for exploring distribution power flow formulations, Electric Power Systems Research, 2020. (Source of the ported optimality fixtures.)\nR. C. Dugan, T. E. McDermott, An open source platform for collaborating on smart grid research (OpenDSS), IEEE PES General Meeting, 2011; the OpenDSSDirect.jl interface is used for the live reference solves.\nM. Deakin, A. Pandey, F. Geth, Mathematical Model and Data Model for Up-To-Four-Wire Distribution System OPF, IEEE Task Force on Benchmarking Multiconductor OPF for Distribution Systems, draft V0.2, 2026.\nR. C. Dugan, \"A perspective on transformer modeling for distribution system analysis,\"  2003 IEEE Power Engineering Society General Meeting (IEEE Cat. No.03CH37491), Toronto,  ON, Canada, 2003, pp. 114-119 Vol. 1, doi: 10.1109/PES.2003.1267146.","title":"References"},{"category":"section","location":"relu_softplus_encoding/#Tech-note:-smooth-droop-encoding","page":"Smooth droop encoding","text":"The Volt-var (VVC) and Volt-watt (VWC) characteristics of AS/NZS 4777.2 / IEEE 1547 are piecewise-linear (PWL) functions of voltage magnitude. PWL functions are not differentiable at their breakpoints, and primal–dual interior-point solvers such as Ipopt inherit Newton's requirement that the constraint functions be twice continuously differentiable: a kink makes the Jacobian jump and the Hessian a Dirac spike, which degrades the line search near a breakpoint. This note documents how BMOPFTools encodes the droop so that the OPF stays smooth, exact to a controllable tolerance, and numerically robust — and writes out the derivatives and the stable evaluation that the source only summarises.\n\nThe approach follows Mhanna, Geth, Quiertant & Mancarella [1], §II-C.1; the smoothing is the Chen–Harker–Kanzow–Smale (CHKS) softplus [2]. Numbered citations refer to the references below. The implementation lives in control_curves.jl.","title":"Tech note: smooth droop encoding"},{"category":"section","location":"relu_softplus_encoding/#1.-The-ReLU-sum-encoding","page":"Smooth droop encoding","text":"Any continuous PWL function f defined by non-decreasing breakpoints (bar x_1bar y_1)dots(bar x_nbar y_n) and clamped flat outside bar x_1bar x_n can be written as a single baseline plus a sum of shifted/scaled rectified linear units operatornameReLU(z)=max(0z):\n\nf(U) = bar y_1 + sum_i a_i operatornameReLU(U-bar x_i)\n\nEach interior segment i (between bar x_i and bar x_i+1) with slope s_i contributes two terms — (+s_ibar x_i) turns the slope on at the segment start and (-s_ibar x_i+1) turns it off at the end — so the slopes telescope: the running slope below bar x_1 is 0, equals s_i on segment i, and returns to 0 above bar x_n. This is the \"two-triple per segment\" form produced by breakpoints_to_triples.\n\nFor the canonical Volt-watt curve (100 % below 253 V dropping to 20 % at 260 V) the encoding is the two triples (-tfrac087253) and (+tfrac087260) on a baseline of 10. Zero-slope (deadband) segments contribute no triples.\n\nThe encoding is exact for the kinked curve and is used as the reference (curve_value_exact) against which the smooth surrogate is tested.","title":"1. The ReLU-sum encoding"},{"category":"section","location":"relu_softplus_encoding/#2.-The-softplus-surrogate","page":"Smooth droop encoding","text":"The non-smoothness is confined to the ReLU kink at z=0. Replace each ReLU by its softplus surrogate with smoothing parameter varepsilon0,\n\noperatornameReLU^varepsilon(z) = varepsilonlogbigl(1+e^zvarepsilonbigr)\n\ngiving the smooth droop\n\nf^varepsilon(U) = bar y_1 + sum_i a_ivarepsilonlogbigl(1+e^(U-bar x_i)varepsilonbigr)\n\nThis function f^varepsilon is infinitely differentiable, C^infty, and its slopes match the original droop exactly in the limit. Two properties make it a principled choice:\n\nMonotone, bounded error. The softplus brackets the ReLU from above with a uniform bound [1, 2]:\n0 le operatornameReLU^varepsilon(z) - operatornameReLU(z) le varepsilonlog 2 \nThe worst-case error is varepsilonlog 2, attained at the kink, and it shrinks linearly in varepsilon. Summing over triples, the curve error is bounded by bigl(sum_i a_ibigr)varepsilonlog 2.\nExact limit. operatornameReLU^varepsilon(z)tooperatornameReLU(z) pointwise as varepsilonto 0^+, so the smoothing is a controllable approximation.","title":"2. The softplus surrogate"},{"category":"section","location":"relu_softplus_encoding/#3.-Closed-form-first-and-second-derivatives","page":"Smooth droop encoding","text":"Because the operator is a single-argument function, BMOPFTools registers it with JuMP's add_nonlinear_operator together with explicit first and second derivatives, avoiding automatic-differentiation overhead and giving Ipopt an exact Hessian. With z=U-bar x and the logistic (sigmoid) sigma(t)=tfrac11+e^-t,\n\nfracddUoperatornameReLU^varepsilon(z) = sigmaBigl(tfraczvarepsilonBigr)\nqquad\nfracd^2dU^2operatornameReLU^varepsilon(z) = frac1varepsilonsigmaBigl(tfraczvarepsilonBigr)Bigl(1-sigmaBigl(tfraczvarepsilonBigr)Bigr)\n\nThe first derivative is a smooth switch rising from 0 to 1 across a band of width mathcal O(varepsilon) around the breakpoint; the second derivative is a bump of height mathcal O(1varepsilon) and width mathcal O(varepsilon) — a smooth, integrable stand-in for the Dirac curvature of the exact kink. For the full curve the derivatives are the corresponding a_i-weighted sums:\n\nf^varepsilonprime(U) = sum_i a_isigmaBigl(tfracU-bar x_ivarepsilonBigr)\nqquad\nf^varepsilonprimeprime(U) = frac1varepsilonsum_i a_isigma_i(1-sigma_i)quad sigma_i=sigmaBigl(tfracU-bar x_ivarepsilonBigr)\n\nThese are exactly the f, df, d2f closures passed to the operator in relu_operator.","title":"3. Closed-form first and second derivatives"},{"category":"section","location":"relu_softplus_encoding/#4.-Numerically-stable-evaluation-(StatsFuns)","page":"Smooth droop encoding","text":"A literal evaluation of log(1+e^t) and tfrac11+e^-t with t=zvarepsilon overflows or underflows well inside the operating range — and the problem gets worse as varepsilonto 0, because t=zvarepsilon grows like 1varepsilon:\n\nfor large positive t, e^t overflows to Inf, so log(1+e^t) returns Inf instead of approx t;\nfor large negative t, e^-t overflows in the logistic denominator, and 1+e^tto 1 loses all the information in e^t.\n\nBMOPFTools therefore evaluates the surrogate through StatsFuns.jl's log1pexp and logistic, which use the regime-split identities of Mächler [3]:\n\nlog(1+e^t) =\nbegincases\ne^t  t lesssim -37 quad(text1+e^tapprox 1 log 1p)\nlogbigl(1+e^tbigr)  -37 lesssim t lesssim 18\nt + e^-t  18 lesssim t lesssim 333\nt  t gtrsim 333\nendcases\n\nso the result is accurate to full Float64 precision and never overflows: the large-t branch returns t (the ReLU asymptote) directly, and the small-t branch uses log1p to retain precision near zero. logistic applies the companion overflow-safe split, evaluating tfrace^t1+e^t for t0 and tfrac11+e^-t for tge 0 so the exponential argument is always non-positive. The second derivative reuses sigma, so it inherits the same stability. This is what lets the encoding use aggressively small varepsilon without the floating-point failures the naive formulas would suffer [1, §III-C].","title":"4. Numerically stable evaluation (StatsFuns)"},{"category":"section","location":"relu_softplus_encoding/#5.-Choosing-the-smoothing-\\varepsilon","page":"Smooth droop encoding","text":"The smoothing is relative to the voltage scale. breakpoints_to_triples works in model units (per-unit when the OPF is solved per-unit), and the operator uses\n\nvarepsilon = textttrelu_epstimesoverlinexqquad overlinex=tfrac1nsum_i bar x_i\n\ni.e. relu_eps (default 2e-3, exposed as the volt_var_watt_eps keyword of solve_opf) times the mean breakpoint. Scaling varepsilon to the mean breakpoint keeps the corner radius a fixed fraction of the voltage range, so the droop is identical in SI and per-unit solves.\n\nThe tradeoff is the usual one:\n\nsmaller varepsilon ⇒ tighter match to the standard's curve (error proptovarepsilon), but a sharper second-derivative bump (propto 1varepsilon) that can slow or trip the line search if a solution sits exactly on a breakpoint;\nlarger varepsilon ⇒ rounder corners and easier conditioning, at the cost of a visibly smoothed deadband.\n\nMhanna et al. [1, §IV-C] benchmark this sweep and recommend a softplus smoothing small enough to stay well inside the pm 75 voltage-accuracy band of real inverters while preserving reliability; in their normalisation varepsilonle 10^-5 was the sweet spot. The default here is deliberately conservative and can be tightened per study. The same paper presents a quadratic Bézier spline as an alternative smoothing with an equivalent Delta U tolerance; BMOPFTools ships the softplus form, which has the cheaper gradient/Hessian evaluation.","title":"5. Choosing the smoothing varepsilon"},{"category":"section","location":"relu_softplus_encoding/#6.-Provenance-and-related-work","page":"Smooth droop encoding","text":"The encoding above is the composition of two well-established ideas; naming them clarifies what is standard and what is specific to this implementation.\n\nRepresenting a PWL function as a sum of hinges. Writing a continuous PWL function as a baseline plus shifted max(0cdot) terms (§1) is the degree-1 truncated power basis of spline theory. It is the building block of Friedman's multivariate adaptive regression splines (MARS) [7] and of Breiman's hinging hyperplanes, and the canonical-PWL representation theory of Chua & Kang [8] shows that every continuous PWL function is a linear combination of maxima of affine functions — the single-variable case being exactly the hinge sum used here. Equivalently, a one-dimensional ReLU sum is a shallow ReLU network, which is why the encoding reads like one.\n\nSmoothing the hinge with a softplus. Replacing max(0z) by varepsilonlog(1+e^zvarepsilon) (§2) is the smoothing-of-the-plus-function idea of Chen & Mangasarian [9], who obtain the softplus by integrating a sigmoid precisely to convert nonsmooth inequality/complementarity problems into smooth ones solvable by Newton — the same motivation as here. The error bound and the broader smoothing family are the Chen–Harker–Kanzow–Smale (CHKS) line [2]. A unifying view: operatornamesoftplus(x)=varepsilonoperatornamelogsumexp(0xvarepsilon), and log-sum-exp is the canonical entropic (Nesterov) smoothing of max [10], so the surrogate is the simplest smooth-max. More recently, the pattern \"relax to a program, then smooth it\" has been systematised under differentiable programming [11], where softplus is a running example.\n\nWhat is specific here. Neither ingredient is novel in isolation; the contribution is their exact-slope composition for standardised droop — the baseline and triples are placed analytically so the surrogate reproduces the AS/NZS 4777.2 / IEEE 1547 slopes in the varepsilonto 0 limit — together with the numerically stable log1pexp/logistic evaluation (§4) and deployment inside a neutral-explicit four-wire OPF [1]. The same smooth-droop strategy is applied to AC–DC converter droop with saturation in a companion paper [12].","title":"6. Provenance and related work"},{"category":"section","location":"relu_softplus_encoding/#relu-refs","page":"Smooth droop encoding","text":"S. Mhanna, F. Geth, L. Quiertant, P. Mancarella, \"Volt-VAr-Watt Optimization in Four-Wire Low-Voltage Networks: Exact Nonlinear Models and Smooth Approximations,\" IEEE Trans. Power Systems, 2026, doi:10.1109/TPWRS.2026.3677246.\nB. Chen, P. T. Harker, \"Smooth approximations to nonlinear complementarity problems,\" SIAM J. Optim. 7 (2) (1997) 403–420.\nM. Mächler, \"Accurately computing log(1-e^-a) — assessed by the Rmpfr package,\" 2012 (the log1pexp/logistic regime split used by StatsFuns.jl).\nJ. Huchette, J. P. Vielma, \"Nonconvex piecewise linear functions: Advanced formulations and simple modeling tools,\" Operations Research 71 (5) (2023) 1835–1856 (the exact binary/logarithmic PWL alternative).\nA. Wächter, L. T. Biegler, \"On the implementation of an interior-point filter line-search algorithm for large-scale nonlinear programming,\" Math. Program. 106 (1) (2006) 25–57 (Ipopt; the C^2 requirement).\nM. Lubin, O. Dowson, J. Dias Garcia, J. Huchette, B. Legat, J. P. Vielma, \"JuMP 1.0: Recent improvements to a modeling language for mathematical optimization,\" Math. Program. Comput., 2023 (user-defined nonlinear operators).\nJ. H. Friedman, \"Multivariate adaptive regression splines,\" Ann. Statist. 19 (1) (1991) 1–67 (hinge-function / truncated-power basis).\nL. O. Chua, S. M. Kang, \"Section-wise piecewise-linear functions: Canonical representation, properties, and applications,\" Proc. IEEE 65 (6) (1977) 915–929 (canonical PWL representation).\nC. Chen, O. L. Mangasarian, \"A class of smoothing functions for nonlinear and mixed complementarity problems,\" Comput. Optim. Appl. 5 (2) (1996) 97–138 (softplus as the integral of a sigmoid, smoothing the plus function).\nYu. Nesterov, \"Smooth minimization of non-smooth functions,\" Math. Program.  103 (1) (2005) 127–152 (entropic smoothing of max via log-sum-exp).\nM. Blondel, V. Roulet, \"The Elements of Differentiable Programming,\"  arXiv:2403.14606, 2024 (smoothing programs; softplus as a running example).\nG. Mohy-ud-din, R. Heidari, F. Geth, H. Ergun, S. M. M. Uddin, \"AC-DC Power  Systems Optimization with Droop Control Smooth Approximation,\"  arXiv:2409.18376, 2024 (companion smooth-droop application).","title":"References"},{"category":"section","location":"#BMOPFTools.jl","page":"Home","text":"A Julia library for parsing, validating, analysing and reporting on BMOPF-format distribution network datasets — the JSON data model developed by the IEEE Task Force on Benchmarking Multiconductor OPF for Distribution Systems for up-to-four-wire optimal power flow (OPF) benchmarks (ref. 1).\n\nThe term optimal power flow is used throughout, but the Task Force's scope extends well beyond generation cost minimisation.  The unifying requirement across all targeted problem classes — CVR, Dynamic Operating Envelopes, state estimation, maximum load delivery — is a faithful, conductor-level representation of an unbalanced distribution network subject to a selectable set of bounds.  Voltage limits, current ratings and power constraints are therefore optional in the data model; different formulations activate different subsets.  See Optimal power flow for the full motivation.\n\nThe library serves three use cases:\n\nDataset producers converting utility-derived OpenDSS models into clean, spec-conformant BMOPF JSON benchmark cases,\ndataset consumers who want to understand exactly what a case contains — its modeling conventions, hidden assumptions, data-quality issues and OPF-readiness — before building optimization models on it, and\noptimization practitioners who, given a solved BMOPF case and an OPF result dict, want to flag bound violations, near-active constraints, constraint residuals, and solution-quality issues without access to solver internals.","title":"BMOPFTools.jl"},{"category":"section","location":"#Design","page":"Home","text":"The network data model is a plain Dict{String,Any} mirroring the BMOPF JSON structure exactly. There are deliberately no wrapper types: data flows to and from JSON (and out to PowerModelsDistribution via to_pmd) without conversion layers, and the only structs in the library are the outputs — Finding, SummaryReport, and SolutionReport — which need stable shape for rendering and programmatic use.\n\nEvery diagnostic is a Finding with a stable dot-separated code (E./W./I. for error/warning/info) — see the finding-code reference for the complete catalogue. Match on codes, never on message text.","title":"Design"},{"category":"section","location":"#Installation","page":"Home","text":"BMOPFTools requires Julia ≥ 1.10. It is not yet in the General registry, so install it from its Git URL:\n\nusing Pkg\nPkg.add(url = \"https://github.com/frederikgeth/BMOPFTools.jl\")\n\nParsing, validation, analysis, reporting, OpenDSS ingestion (from_dss, via the PowerIO.jl dependency), and the to_pmd exporter work out of the box. One capability pulls in extra tooling, activated only when you load it:\n\nOPF / power flow (solve_opf, solve_pf, solve_feasibility_opf) lives in a package extension that is loaded once JuMP and a solver such as Ipopt are present in the environment:\nPkg.add([\"JuMP\", \"Ipopt\"])\n\nnote: Tracking a moving target\nThe package is under rapid development, with breaking changes landing directly on main. Pin a specific revision when you need reproducibility: Pkg.add(url = \"https://github.com/frederikgeth/BMOPFTools.jl\", rev = \"<commit-sha>\").","title":"Installation"},{"category":"section","location":"#Quickstart","page":"Home","text":"Analysing an existing BMOPF JSON case:\n\nusing BMOPFTools\n\nnet    = parse_bmopf(\"case.json\")\nreport = analyze(net)\n\nrender(report, stdout)              # terminal report\nrender(report, \"case_report.md\")    # Markdown report\n\nerrors(report)                      # findings with ERROR severity\nreport.results[:provenance][\"convention\"]\nreport.results[:benchmark][\"suggestions\"]\n\nProfiling an OPF result dict (requires solve_opf or any compatible solver):\n\nusing BMOPFTools, JuMP, Ipopt\n\nnet    = parse_bmopf(\"case.json\")\nresult = solve_opf(net; optimizer=Ipopt.Optimizer)\n\nreport = profile_solution(net, result)\nrender_solution(report, stdout)              # Markdown to terminal\nrender_solution(report, \"solution.md\")      # Markdown file\n\nerrors(report)     # bound violations and infeasibility findings\nwarnings(report)   # near-active bounds and residual warnings\n\nConverting from OpenDSS (parsed in-process by PowerIO.jl):\n\nusing BMOPFTools\n\nnet = from_dss(\"Master.dss\")\nwrite_bmopf(net, \"case.json\")\nanalyze(net) |> r -> render(r, \"case_report.md\")","title":"Quickstart"},{"category":"section","location":"#The-pipeline","page":"Home","text":"OpenDSS .dss ──(from_dss / PowerIO.jl)──► BMOPF Dict{String,Any} ◄── parse_bmopf ◄── BMOPF JSON\n                                                 │               └──── write_bmopf\n                                                 │ analyze\n                                                 ▼\n                                          SummaryReport ──► render\n                                                 │ fix_case\n                                                 ▼\n                                          net′ (repaired)\n                                                 │ add_generators / add_inverters  (optional DER)\n                                                 ▼\n                                          net′ + DERs\n                                                 │ augment_case\n                                                 ▼\n                                    net″ (benchmark-ready) ──► write_bmopf\n                                                 │ solve_opf / solve_pf / to_pmd\n                                                 ▼\n                                         result Dict{String,Any}\n                                                 │ profile_solution\n                                                 ▼\n                                        SolutionReport ──► render_solution\n\nanalyze runs fifteen passes (see Analysis & reports) and the report renders in nine sections, including a one-line modeling convention statement (wires per voltage level, grounding style, normalisations) so the case's assumptions are explicit rather than implied.","title":"The pipeline"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Positioning & ecosystem — where BMOPFTools fits among distribution optimization and modelling tools, and the benchmarking gap it targets.\nData model conventions — units, terminal names, transformer subtypes, grounding semantics.\nConversion guide — every deliberate decision in to_pmd, with the impedance-base formulas.\nAnalysis & reports — what each pass computes and how to read the report.\nFinding-code reference — complete catalogue of finding codes, with triggers and rationale.\nMethodology notes — the physics and linear algebra behind the provenance checks, with literature references.\nOptimal power flow — running solve_opf, solver options, and the OPF extension model.\nOPF result dictionary — structure of the result dict returned by solve_opf, including the \"initialisation\" diagnostics block.\nCase augmentation — the fix_case → add_generators → augment_case pipeline that turns a faithful import into a meaningful OPF benchmark, why the sequence and the diverse DER strategies matter, and the manifest audit trail.\nAPI reference.","title":"Where to go next"}]
}
